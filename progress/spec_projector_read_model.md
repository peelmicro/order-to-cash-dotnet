# `projector_read_model` (id 24, phase 12, `sdd: true`) — spec pass record

**Deliverables:** `specs/projector_read_model/{requirements,design,tasks}.md`
**Shared requirements cited:** `R50` – `R55` (`specs/shared/requirements.md` §7), verbatim, as the acceptance. `R11`/`R12` (envelope, correlation) and `R17`/`R18` (idempotency) are inherited dependencies, already `DONE`, not re-flipped. `R24` and `R28` are cited as ordering acceptance for `PR10` and are not re-owned.
**Local requirements:** `PR1` – `PR44`, traced in `specs/projector_read_model/requirements.md` §3. `PR1` – `PR28` and `PR30` – `PR35` reuse #7's ids, each checked one by one against #7's committed code; `PR29` is **deliberately not reused** (§2.10); `PR36` – `PR44` are new to #8 and each names why #7 could not have faced it.
**`specs/shared/` edits in this pass:** **none.** `test-matrix.md` §7 already carries `R54`/`R55` in the two-half form (inherited from #7's own gate row 3), so this feature needs no shared edit at spec time; task **N4** flips this assessment's own Status cells at close, which that file's own reuse recipe defines as per-assessment content rather than shared content.
**`requirements.md` amended in this pass:** **two citation corrections only, no requirement reworded** — `PR10` clause 2 cited "Orders' `TransitionContext`", which is #7's type name and does not exist in #8 (the equivalent is `SagaStepTable`/`SagaFactHandler` passing `fact.OccurredAt` straight into the transition); and `PR44` cited `OrderSagaFixture.Outbox`, which is #7's member name — #8's fixture carries `OrdersOutbox`, `FulfillmentOutbox` and `BillingOutbox`. Both corrections point at real files; neither changes what is required.
**Status set:** `pending` → `spec_ready`. **Not** `in_progress` — the human approval gate comes first.
**Not touched:** any file under `src/`, `tests/`, `apps/`, `infra/`, `n8n/`, or `Directory.Packages.props`.

**Scale:** 99 tasks, of which **89 carry the arming flag** — every task whose own prose asserts a count, an identity, an ordering or an absence. The ten that do not are the ten that make no such claim (an options class, a port interface, two build/format runs, `init.sh`, and the four close-out recording tasks). Design §10's ported-idiom ledger enumerates **54 boundaries** and carries **54 rows**.

---

## Open points

> **Type column.** **(a)** inherited from #7's gate, with the citation. **(b)** resolved here from evidence in this repository, with the citation. **(c)** genuinely open because .NET, MS-SQL or MongoDB.Driver differs from #7's stack. **Only a (c) row may be GATE.** One row is GATE; one further row is **RATIFY** — not a question, a signature the matrix's own rule 3(b) requires somebody other than the author to supply.

| # | Open point | Type | Decision taken in the spec | Gate? |
|---:|---|:---:|---|:---:|
| 1 | **`NatsConnection` connects lazily, so a wrong `NATS_URL` produces a projector that projects perfectly and signals nothing, forever.** #7's `main.ts` did `await connect(...)` at boot, so a bad URL failed the boot loudly. #8's other three NATS services never noticed the difference because they are **responders**: their `SubscribeAsync` materialises the connection during startup and a failure surfaces there. The projector only **publishes**, and `PR19` requires publication failures to be logged and swallowed — correctly, for a *transient* failure. Composed with lazy connect, a misconfiguration becomes a total, silent loss of `R55`'s producer half that no test on a good URL can see | **(c)** | **Recommendation: connect eagerly.** `await connection.ConnectAsync()` becomes the third step of `ReadModelBootstrap.StartAsync` (`PR43` already makes that method the boot contract), so a misconfigured broker is a boot failure exactly as it was in #7, and `PR19` is left doing the job it was argued for. Evidence: `src/Orders/Program.cs`, `src/Billing/Program.cs` and `src/Fulfillment/Program.cs` all register `INatsConnection` lazily and all get eager failure for free from a responder they own; the projector owns none. Cost: one `await` and one test. If **approved**, `requirements.md` gains **`PR45`** — *"WHEN the projector starts, THE SYSTEM SHALL establish the NATS connection to completion inside `ReadModelBootstrap`, before the fact consumer's first poll; IF the connection cannot be established, THEN the host SHALL fail to start rather than run with every update signal silently swallowed by `PR19`."* — plus its `test-matrix`-style row in §3 and task **H8**'s guard. If **declined**, task H8 is skipped and the implementer records the consequence explicitly; it must **not** be worked around by making `PR19` rethrow, which would block the partition forever (`PR19`'s own argument) | **GATE** |
| 2 | **`R54` and `R55` cannot both be flipped by this feature, and matrix rule 3 forbids closing a feature with a `TODO` row unless it is *scoped and ratified*** — and rule 3(b) is explicit that a shortfall disclosed only by whoever wrote the row is **not** ratified | **(a)/(b)** | The two-half Status cells are already in the inherited `test-matrix.md` §7 (#7's gate row 3), so **no shared edit is needed**; `requirements.md` §4 states the seam line by line, and task **N4** appends the projector half's evidence while leaving both rows `TODO` and naming feature 25 as the owner of the remainder. What is missing, and what only the gate can supply, is rule 3(b)'s **ratification**: *approving this spec is that ratification*, and `requirements.md` §4 cites this row as where it was given. Nothing is being asked — this row exists so the signature is recorded rather than assumed | **RATIFY** |
| 3 | **`R55`'s transport is left open by the shared spec, and neither column of the decision matrix fits.** An update signal is not a fact (not immutable domain truth, must not be replayable, does not advance the saga) and not a command (no responder, no reply, never awaited) | **(a)** | **NATS core publish**, subjects `readmodel.order.updated.<orderId>` / `readmodel.timeline.appended.<orderId>` — #7's gate row 2, approved there and inherited verbatim (`order-to-cash-nestjs/progress/spec_projector_read_model.md` row 2, `design.md` §7.1). Three reasons, unchanged: its delivery semantics are NATS's and `openapi.yaml` `/orders/stream` already accepts them in writing; it must fan out to **every** Gateway replica, which a shared Kafka consumer group cannot do; the subject hierarchy makes feature 25's `?orderId=` a subscription rather than client-side filtering. It genuinely widens `CLAUDE.md`'s *"Kafka carries facts, NATS carries RPC"* to *"…and publish-only read-model signals"*; the mitigation is that the signal has no responder, no reply subject, is never awaited, and no code path branches on it. **Not a new decision, and not re-opened here** — noted for the wrap-up as a parity trade-off worth a line in `README.md` | No |
| 4 | **`src/Seed` writes the read model, and `R54` says the projector is the only writer** | **(a)** | Not papered over. `PR20` allow-lists `src/Seed` **by name with its reason inside the guard file** — an offline fixture loader, never deployed, run before the system runs, writing documents in exactly the shape this design fixes. #7's gate row 4, approved. #8 strengthens it in one respect: the guard scans for MongoDB.Driver's **write-shaped method names**, not for the package import, because `R54`'s own second half obliges the Gateway to *read* this collection at feature 25 — an import-level guard would have to be relaxed then and would afterwards guard nothing | No |
| 5 | **The `uq_order_reference` index must be partial, or the second placeholder is rejected with `E11000`** | **(a)→(b), already closed** | #7 discovered this at spec time and had to edit `apps/seed` (its gate row 1, plus a manual index drop). In #8 the seed **landed first with the partial index already in place**: `MongoSeedWriter.EnsureIndexesAsync` sets `PartialFilterExpression = Builders<OrderTimelineDocument>.Filter.Type(d => d.OrderReference, BsonType.String)` and says why in its own header. **No seed edit, no manual drop.** The projector must now match *it* — see row 23 | No |
| 6 | **`PR29` (#7's legacy-document backfill) is not ported, and its id is not reused** | **(b)** | `requirements.md` §2.10 carries the argument and its evidence: `git log --oneline -- src/Seed/Infrastructure/Mongo/` → one commit (`181060f`), and that revision already writes `statusRank`, `processedEventKeys`, `timelineOrderVersion`, `events[].causationId` and the partial index. A backfill filtered on `{ statusRank: { $exists: false } }` has no document it could ever match, and a guard for it would have to fabricate a shape no writer produces — a decorative guard by construction. What **is** kept: `PR6`'s pipeline stays total over any document shape (`$ifNull` on every array and rank operand), and `PR32`'s version-stamped migration, which is the forward-looking half, is kept in full. Row 31 below adds the live check | No |
| 7 | **Idempotency mode: a fifth MS-SQL database, or the dedup inside the Mongo document** | **(a)** | **`documented-variant`** — the pair `"projector:<eventId>"` in `processedEventKeys`, written by the *same single* `FindOneAndUpdateAsync` that applies the projection. Chosen because it is **stronger**, not cheaper: an MS-SQL ledger plus a MongoDB effect has no transaction spanning them, so a crash between them leaves an event permanently marked and never projected — a silently missing timeline entry nothing can repair. That is defensible for one lost email and not for a read model. The variant has no window at all (design §6.3) | No |
| 8 | **The dedup key duplicates the `eventId` already sitting in `events[]`** | **(a)** | Deliberate. `R17`'s key is the **pair**, and conformance case 5 checks it; a `TimelineEntry` is a client-facing `openapi.yaml` schema and must not grow a `consumer` field. Cost is at most fourteen short strings per document, `OrderDetail` is untouched, and it decouples the dedup from the incidental fact that today every projected fact happens to append an entry | No |
| 9 | **`IdempotentConsumerParityTests` case 4 has never had a subject, and arms on this feature** | **(a)→(b)** | #7's gate row 7, with one #8-specific correction the implementer must not miss: **#8's case 4 checks only two things** — the canonical path literal and a line beginning `Divergence:` — where #7's checked three. The `Behavioural conformance:` line naming a file that **exists** is therefore asserted by the projector's own `VariantBannerTests` (`PR24`, task D2), not by the Orders guard. Task **D3** re-runs the Orders suite green and confirms it is armed on this subject; task **D5** re-runs the subversion probe (gut `RunOnceAsync`, banner untouched) and requires the Orders text guard to stay **green** while the projector's behavioural suite **fails**. If D5's behavioural half does not fail, the variant guard is text-only and the leader must hear about it before the feature closes | No |
| 10 | **The hazard: an upsert that looks idempotent under test and is not under concurrency or out-of-order delivery** | **(a)+(b)** | `PR6` makes it a requirement rather than a style note: **the filter is the check.** Two operations per fact and no others — a `$setOnInsert` upsert (a no-op by construction on an existing document) and one `FindOneAndUpdateAsync` whose filter is `{ _id, processedEventKeys: { $ne: key } }`. No `Find` first, no `Contains` in C#, no compare-and-set loop. This is also #8's own **check-then-act** lesson from feature 45 (`CLAUDE.md`'s ported-idiom table, row 3: `IF NOT EXISTS (SELECT …) INSERT`) in its MongoDB costume, and it is why task **E3** proves the absence of a read by recording the driver's actual command stream rather than by reading the code | No |
| 11 | **`AutoOffsetReset.Earliest`, the opposite of the service next door** | **(b)** | Deliberate, argued from this service's own obligation, and **asserted three ways** rather than commented. Notifications chose `Latest` because it owns no aggregate and its effect is an external email — a fact its group never saw is an omission (`KafkaFactStreamSubscriber`'s own class remarks). The projector's store is *derived*, and `feature_list.json` id 24's own acceptance bullet 2 is *"replaying a topic reproduces an identical document"*; a projector starting at the log head is permanently and silently incomplete, which is a correctness violation. `PR38` is the #8-specific half: kafkajs made this a **subscribe option** #7 asserted directly, while `Confluent.Kafka` makes it a **config token whose default is `Latest`** — and Notifications shipped exactly this token argued in prose and asserted by nothing (its review D3). Guarded by a reflection read of the private `BuildConsumerConfig`, by an integration case producing before the group first subscribes, and by `OffsetContractTests` reading the committed offset **from the broker** | No |
| 12 | **`updatedAt`'s meaning** | **(a)** | Fixed as *the greatest `occurredAt` applied so far*, never a wall clock (`PR14`) — otherwise replay determinism is impossible. `MongoSeedWriter` already writes exactly this value (`saga.UpdatedAt` is the last fact's `occurredAt`), so nothing changes; the requirement records the agreement so it is not later "fixed" into a clock. #8 adds one clause #7 did not need: the `$max` orders chronologically **only because** every instant is rendered in one fixed-width format (design §10, L21+L28 are one property) | No |
| 13 | **`R52`'s "implied order status precedes" needs an order, and three services publish on three topics with only per-partition ordering** | **(a)** | `PR12`'s fourteen-row table. Ranks 1 – 7 are the state machine's own **linear** progression, so on that segment "precedes" is decidable by `$max`/`$gt` inside the single atomic write; `completed` (98) and `cancelled` (99) are terminal and mutually unreachable, ranked highest and distinct so the projection is total; the five status-less facts contribute rank 0 and touch neither field | No |
| 14 | **What makes a placeholder distinguishable, given `R55` wants "projection pending" surfaced honestly** | **(a)** | Three-way, none of it invented: **no document** → the Gateway answers `202 ProjectionPending` (feature 25's; `openapi.yaml` already has the schema); **`headerComplete: false`** → served as a normal `200 OrderDetail` with the real timeline and null header fields; **`headerComplete: true`** → complete. `headerComplete` already exists on the seed's document and in `openapi.yaml` with exactly this meaning | No |
| 15 | **#7 had to add two projector-owned fields to the seeded document shape** (`statusRank`, `processedEventKeys`) | **(b)** | #8 has to add **none**. `OrderTimelineDocument` already declares `StatusRank`, `TimelineOrderVersion`, `ProcessedEventKeys` and `TimelineEvent.CausationId`, each with a header comment naming the projector as the reason. The projector adopts that shape and does not re-declare the class at all (row 27) | No |
| 16 | **One `ProjectFactCommand`, not fourteen** | **(a)** | The projector's behaviour does not vary with `eventType`; all fourteen variations live inside the pure `FactProjection.Project`. Fourteen command classes would be fourteen empty types routed to identical handlers. The hand-rolled dispatcher (`src/Cqrs`) is still binding and still used (`PR27`), and `AddDispatcher` running last still makes a missing or duplicated handler a boot failure | No |
| 17 | **Fourteen facts, not thirteen** | **(b)** | #7's spec was written against thirteen and grew the fourteenth (`order.saga_failed.v1`) later. #8 starts at fourteen because `FactCatalog.PayloadTypesByEventType` already carries all fourteen and already has a completeness test against `asyncapi.yaml`. `order.saga_failed.v1` contributes a timeline entry, no status and rank `0`; it has **no producer yet** (feature 27's), exactly as in #7, so its only coverage is unit-level and that is stated rather than implied | No |
| 18 | **`$sortArray` needs MongoDB 5.2+; `$unsetField` needs 5.0+** | **(a)** | The compose stack is `mongo:8.3.8` and `MongoDbBuilder("mongo:8.3.8")` drives the same tag (`tests/Seed.IntegrationTests/SeedContainersFixture.cs`). Recorded because these are the version-sensitive constructs, and because a downgrade would silently change `PR10`/`PR15` from "sorted in the document" to "sorted at read time". **No `$function`, no server-side JavaScript** — scripting is not enabled in the compose stack | No |
| 19 | **A signal that fails to publish is logged and swallowed, not retried** | **(a)** | Counter-intuitive, so it is argued in `PR19`: the projection has already applied, so a rethrow produces a redelivery that `PR6`'s filter suppresses — the signal could **never** be emitted on a later attempt, and the partition blocks while failing forever. The loss is bounded and already accepted by `openapi.yaml` `/orders/stream`. Row 1 is the precondition this argument turns out to need in #8 | No |
| 20 | **The projector consumes all fourteen facts — the first consumer that must — and Notifications shipped a two-list routing shape that a deletion could silently defeat** | **(b)** | `PR36`: **no list at all** under `src/Projector/`. The consumer looks the type up in `FactCatalog.PayloadTypesByEventType` — the one registry with its own completeness test against `asyncapi.yaml` — deserialises to that CLR type, and the domain switches on the **payload's CLR type**. `PR2` closes the loop in both directions; `J4`'s structural test **enumerates** every hit of the fourteen literals under `src/Projector/` and classifies each, so a missed hit is an unclassified line rather than an unwritten sentence. #7 itself had `HANDLED_EVENT_TYPES` beside its switch — the same two-list shape Notifications was rejected for, unbitten only by luck | No |
| 21 | **Packages and environment** | **(b)** | **No new `PackageVersion`, no version bump, and no `.env.example` change.** `MongoDB.Driver` 3.11.1, `Confluent.Kafka` 2.15.0, `NATS.Net` 3.2.0, `Testcontainers`/`Testcontainers.MongoDb` 4.14.0 and the three `Microsoft.Extensions.*` 10.0.11 packages are all already pinned in `Directory.Packages.props`. #7 added `PROJECTOR_KAFKA_CLIENT_ID`/`PROJECTOR_CONSUMER_GROUP` to `.env.example`; #8 does not, because its convention (`NotificationsOptions`, `OrdersSagaOptions`, `BillingOptions`) puts service-fixed values in the options class and reads only the shared infrastructure coordinates from the environment — which the projector reads with the *identical* expressions the other four hosts use. Stated so that a phase commit message listing no new packages and no env change reads as a fact rather than an omission | No |
| 22 | **`FindOneAndUpdateOptions<T>.ReturnDocument` defaults to `Before`, and `UpdateOptions.IsUpsert` defaults to `false`** | **(b)** | #7 passed `returnDocument: 'after'` and `{ upsert: true }` explicitly. In #8 the same explicitness is mandatory rather than stylistic, and `ReturnDocument` is the dangerous one: with the default, **duplicate detection still works** (a non-match is still `null`), so every deletion-family probe passes, and every signal silently carries the *previous* status. `PR42` exists as a requirement for exactly this reason, and task **K5** requires the implementer to record the asymmetry — one case fails, every count-based case stays green. This is design §10's headline row (**L9**) | No |
| 23 | **The partial filter expression's rendering — `Builders<…>.Filter.Type(field, BsonType.String)` renders `$type` with BSON's numeric code, where #7 wrote the string alias `'string'`** | **(b), and the row most likely to bite** | The projector must build `uq_order_reference` with the **identical** `Builders<…>.Filter.Type(field, BsonType.String)` call `MongoSeedWriter.EnsureIndexesAsync` uses, not a hand-written `BsonDocument`. If the two stored expressions differ, `createIndex` raises `IndexOptionsConflict` (85) and **neither service can start after the other** — a failure that appears only when both have run, which no single-writer test reaches. Task **H2** proves both orders on one real collection and additionally arms the string-alias form, recording the outcome **whichever way it goes**, because that answer is what design §10's L12 is asking | No |
| 24 | **The C# driver's defaults for element names, `Guid` and dates are all wrong for this collection** | **(b)** | Four silent divergences #7 could not have had: default element names are the **PascalCase property name** (JavaScript keys were already camelCase); the default `Guid` representation is the **BSON UUID binary subtype** (#7 had strings because JavaScript had strings); the default for a `DateTime`/`DateTimeOffset` is a **BSON date** (#7 had `.toISOString()`); and `"O"` on a `DateTimeOffset` writes seven fraction digits and `+00:00`. `PR41` fixes all four to the seed's own rendering and task **C7** arms each of the five type claims separately | No |
| 25 | **Element order is part of the document's shape, and `$set` appends a key the placeholder omitted** | **(b), new to #8** | `BsonDocument` equality is element-order sensitive, `PR15` says "BSON-identical" and `PR44` compares against the seed's own serialisation — so the order is fixed in design §3.1 (the seed class's declaration order) rather than left to whichever stage happens to write a field first. It comes free **only if** `PlaceholderDocument` is total over the shape; a key it forgets lands at the end and silently breaks both. Task **E4** asserts the element-name sequence and arms it by omitting one placeholder key | No |
| 26 | **Standalone MongoDB: no multi-document transactions, and no driver-level retryable writes** | **(b)** | The compose stack and `MongoDbBuilder("mongo:8.3.8")` are a **standalone**. `retryWrites` defaults to `true` in both drivers and **neither** retries on a standalone topology, so the Phase-1 `E11000` retry-exactly-once is hand-written on both sides — not a driver feature #7 had and #8 lost. Transactions are unavailable, which costs the variant nothing (row 7) | No |
| 27 | **The projector does not re-declare `OrderTimelineDocument`, and works in `BsonDocument` throughout** | **(b), a departure from #7** | #7 re-declared the interface in `apps/projector` and explicitly declined a text-parity guard between the two, because a TypeScript `interface` is erased at run time and could not have been the thing that wrote the bytes. #8 has a better option: literal camelCase element names on the write path, and **the seed's real class map as the oracle** — `ProjectionWriteTests › PR41_AStoredDocumentDeserialisesThroughTheSeedsOrderTimelineDocumentClassMapWithNoLoss` deserialises what the projector actually wrote. A twin class nothing compares would replace an explicit constant with an inferred one. `src/Projector` must **not** reference `src/Seed` (which references Orders/Fulfillment/Billing and would make EF Core transitive, against `PR21`); the two **test** projects do, which `requirements.md` §5 already scopes | No |
| 28 | **Every registration in `AddProjector` is a singleton, unlike Notifications' scoped chain** | **(b)** | Notifications is scoped because it holds an EF Core `DbContext`. The projector holds none: `IMongoClient` is thread-safe and internally pooled (one per process), and `PR6`'s whole point is that no state is carried between the two operations. `ValidateScopes` is forced on and would reject a singleton `BackgroundService` consuming a scoped service, so the shape is checked at boot. **One caveat for the implementer:** if `src/Cqrs`'s `AddDispatcher` registers handlers as scoped, `ProjectorFactsConsumer` takes `IServiceScopeFactory` and opens a scope per message, exactly as `NotificationFactsConsumer` does. Follow whatever `src/Cqrs` actually does; **do not change `src/Cqrs`** | No |
| 29 | **`ReadModelBootstrap` must complete before the consumer's first poll, and in .NET that is three properties rather than one `await`** | **(b)** | #7's `main.ts` awaited both in one function before `startAllMicroservices()`. #8 needs **registration order**, `HostOptions.ServicesStartConcurrently == false` (the host default), and `IHostedService` rather than `BackgroundService` for the bootstrap — a `BackgroundService`'s `StartAsync` returns at the first `await` inside `ExecuteAsync` and would give the appearance of ordering with none of the substance. Any of the three can be changed without a compile error, so `PR43`/task **H7** arms all three | No |
| 30 | **Replaying `Earliest` against a database `src/Seed` has already written** — #7's open point 9, which it called the one *"not in the brief"* and *"must not be dropped as just a migration"* | **(b), closed twice over** | The seed writes its outbox rows **already published** (`OutboxFixture.PublishedAt` is set), so the relay never publishes them and **the seeded facts' `eventId`s are not on Kafka at all** — there is no seeded-fact replay to suppress. And even if there were, every seeded document already carries `processedEventKeys` and a non-null `statusRank`, so `PR6`'s filter would suppress it and `PR12`'s `$max` could not regress it. Both halves of #7's hazard are closed by inheritance. **Task M2 verifies this live, by enumeration, before the boot** — and says explicitly that if any seeded `eventId` *is* on a topic, row 6's argument for not porting `PR29` must be re-examined | No |
| 31 | **The seeded fixtures contain no tie group, so `PR44`'s oracle does not exercise `PR10` at all** | **(b)** | `SagaFixtures.BuildCompleted`/`BuildCancelled` place every fact at a distinct instant (`t0`, `+1m`, `+2m`, `+2m30s`, `+3m`, `+4m`, `+1d`, `+1d5s`, `+1d10s`). **Live** facts do tie, in three places per saga, because a derived fact reuses its triggering fact's `occurredAt` verbatim — `SagaStepTable`'s `order.Confirm(fact.OccurredAt, …)`, `SagaFactHandler`'s `order.Cancel(…, fact.OccurredAt, …)`, and Billing's completion pair (`progress/impl_completion_pair_has_no_causal_edge.md`, backlog id 57, closed 2026-09-07). So `PR10`'s proof is `TimelineCausalOrderTests` with adversarially chosen `eventId`s (tasks I5, I6), and the oracle's blind spot is stated in design §5.4.1 and §11.3 rather than left to be discovered | No |
| 32 | **`Earliest` makes every synthetic probe fact ever injected into a dev fact topic permanently materialised into the read model** | **(a)** | #7's review N7, inherited. Placeholders with `orderReference: null` and `headerComplete: false` will exist for any order whose `order.placed.v1` is not on a topic — that is `R53` behaving exactly as specified, and it coexists only because `uq_order_reference` is partial. **Noted for feature 25's list-query fixtures** so it is expected rather than surprising. Not a defect and not attributable to this feature | No |
| 33 | **Backlog ids 58 and 59 are filed against phase 12 and do not ride this feature** | **(b)** | Both are Notifications-file work. `PR37` applies id 58's *lesson* — a hand-written envelope copy whose `correlationId` could be corrupted on a green suite — to the projector's own copy from the start, at all three hops, with a sentinel per field the test supplied. It does **not** fix `NotificationFactsConsumer.ToEnvelope`, and claiming it did would be a scope lie. `requirements.md` §5 says so in those words | No |
| 34 | **Where the two update-signal payload records live** | **(b)** | `src/Projector/Application/Signals/`, **not** `src/Contracts`. `Contracts` is the wire contract versioned by `asyncapi.yaml`, and these two subjects are deliberately absent from `asyncapi.yaml` (row 3); adding them to `Contracts` would assert a fact-stream status they do not have. #7 built them inline in the projector for the same reason. Feature 25 may forward the bytes verbatim or declare its own reader; design §7.4 states the seam | No |
| 35 | **A Phase-2 `E11000` on `uq_order_reference` would be a poison pill** | **(a)** | #7's review N8, carried verbatim into `requirements.md` §5. The retry-exactly-once is scoped to Phase 1's `_id` upsert; every other error propagates so Kafka redelivers. But `uq_order_reference` is unique, so two distinct order ids carrying one `orderReference` would throw on **every** delivery and block the partition forever — redelivery cannot fix a uniqueness violation. Unreachable while `ORD-######` is unique. Recorded as a concrete instance for feature 27's retry/DLQ classification | No |
| 36 | **`PR11`'s test name must describe what it proves** | **(a), inherited as prevention** | #7's review N6: its `PR11` case was named for a behavioural proof (*"a later `invoice.issued.v1` never overwrites an already-set `despatchReference`"*) that it did not perform, because no two of the fourteen facts ever write the same reference field — deleting the `$ifNull` left the file green. The guard is real, one level down, on the emitted stage tree. Task **I3** names the case for what it does prove and says so in the file header; task **C5** owns the `$ifNull` proof | No |
| 37 | **#7 was rejected at review on this exact feature, on bookkeeping only, with zero code defects found** | **(a), inherited as prevention** | Two blocking defects: `tasks.md` at **0 of 63** ticked, and `progress/current.md` two features stale. Both are inherited here as explicit tasks — the "tick as you go" rule at the head of `tasks.md`, and tasks **N7** (`current.md`) and **N5**/**N6** (the flips and the record). #7's third finding, an unrecorded mandated deletion probe, is inherited as group **K**'s requirement that every armed mutation reach `progress/impl_projector_read_model.md` with its verbatim message | No |
| 38 | **The mutation sweep, and its own machinery** | **(b)** | #8's own Notifications review is the evidence: the implementer's first sweep left **45 of 68** survivors; a later, string-scoped sweep reported a zero residual while **14 date-typed sites** survived unmeasured; and the reviewer's first probe of `AutoOffsetReset` hit a `<see cref="..."/>` in a doc comment and reported a false green. Task group **L** therefore fixes the sweep's population (string, integer/money **and** instant-typed sites) and requires **three sentinels before any verdict is believed** — one that must report *not applied*, one that must **survive**, one that must be **caught** — and requires every survivor to be enumerated and classified rather than summarised | No |
| 39 | **The coverage gate is itself a guard, and #7 found its own had been inert for twenty phases** | **(b)** | Task **N2** requires the gate to be seen to fail: drop the domain figure below 80% and confirm `./quality.sh` reports it, then restore. Note for the implementer: `quality.sh` currently *warns* on a missing coverage report rather than failing; if the ≥80%/≥60% thresholds turn out not to be enforced, that is a finding to record in `progress/impl_projector_read_model.md` and raise, **not** to fix inside this feature | No |
| 40 | **The two `requirements.md` corrections made in this pass** | **(b)** | `PR10` clause 2 cited `TransitionContext` (#7's type, absent from #8) and `PR44` cited `OrderSagaFixture.Outbox` (#7's member; #8 has three). Both are **citation corrections to my own prior local `PR` text**, not to `specs/shared/`, and neither changes what is required — a reader following either citation in #8 would have found nothing. Recorded here so the change is visible at the gate rather than discovered in a diff | No |

**40 open points. 1 GATE (row 1). 1 RATIFY (row 2).** Counted off the Type column of the table above: **1** row typed `(c)`, **14** typed `(a)` (twelve plain, two "inherited as prevention"), **21** typed `(b)`, and **4** typed as a transition between the two (`(a)/(b)`, `(a)+(b)`, `(a)→(b)`, `(a)→(b), already closed`) — 12 + 2 + 17 + 4 + 4 + 1 = 40. Every `(a)` row carries a #7 citation; every `(b)` row carries a citation into this repository.

---

## Notes for the gate

- **Row 1 is the only question.** It is a genuine stack difference — `NATS.Net`'s lazy connect versus #7's `await connect()` — and it composes badly with `PR19`'s log-and-swallow, which is otherwise correct. The recommendation is to connect eagerly in the bootstrap; the evidence is that #8's other three NATS services get eager failure for free from a responder the projector does not have. Approving it adds `PR45`, one `await` and one test. Declining it is survivable and the consequence is written down.
- **Row 2 is a signature, not a question.** Matrix rule 3(b) requires ratification by somebody other than the row's author before `R54`/`R55` may close as scoped; approving this spec supplies it, and `requirements.md` §4 already cites this row as where.
- **Rows 22, 23 and 25 are the three most likely to bite in implementation**, and they are one class: a property that is present and correct on the path a deletion probe takes, and wrong only under a condition the happy path never creates. `ReturnDocument` defaulting to `Before` keeps every signal *count* right and every signal *content* stale. Two `$type` renderings both work in isolation and mean neither service can start after the other. A placeholder missing one key produces a correct document with the wrong element order, which only `PR44` and `PR15`'s full-document comparison can see. None of the three is visible to `R<n>` traceability and none is visible to emission-deletion arming — which is exactly why design §10 exists and why its Guard column names a test for each.
- **Row 30 is the one #7 called the hazard nobody briefed.** In #8 it appears to be closed twice over by inheritance, and the spec says so — but "appears to be" is a reading, so task **M2** turns it into an enumeration run against the live topics *before* the boot, with the instruction to stop and report if the enumeration disagrees.
- **Row 9 matters to the harness, not only to this feature.** `IdempotentConsumerParityTests` case 4 has been dormant since it was written; this is its first subject. Task D5 requires the subversion probe to be re-run and recorded. If the behavioural suite does not fail under it, the guard is still text-only and that is a finding about the harness.
