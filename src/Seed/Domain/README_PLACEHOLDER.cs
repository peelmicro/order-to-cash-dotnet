namespace OrderToCash.Seed.Domain;

/// <summary>
/// Placeholder type so the project compiles and the architecture tests have
/// something to select in the OrderToCash.Seed.Domain namespace. Later phases
/// (see feature_list.json) replace this with real Domain types for the Seed
/// service. Keep this file trivial until then.
/// </summary>
public sealed class SeedDomainPlaceholder;
