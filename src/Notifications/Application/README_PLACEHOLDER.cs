namespace OrderToCash.Notifications.Application;

/// <summary>
/// Placeholder type so the project compiles and the architecture tests have
/// something to select in the OrderToCash.Notifications.Application namespace. Later phases
/// (see feature_list.json) replace this with real Application types for the Notifications
/// service. Keep this file trivial until then.
/// </summary>
public sealed class NotificationsApplicationPlaceholder;
