namespace OrderToCash.Billing.Presentation;

/// <summary>
/// Placeholder type so the project compiles and the architecture tests have
/// something to select in the OrderToCash.Billing.Presentation namespace. Later phases
/// (see feature_list.json) replace this with real Presentation types for the Billing
/// service. Keep this file trivial until then.
/// </summary>
public sealed class BillingPresentationPlaceholder;
