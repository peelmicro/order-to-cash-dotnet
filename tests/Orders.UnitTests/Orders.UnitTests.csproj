<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>OrderToCash.Orders.UnitTests</RootNamespace>
    <AssemblyName>OrderToCash.Orders.UnitTests</AssemblyName>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" />
    <PackageReference Include="xunit" />
    <PackageReference Include="xunit.runner.visualstudio">
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
    <PackageReference Include="coverlet.collector">
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
    <!-- OutboxRelayLoopTests (OI6) and OrdersOutboxRegistrationTests (H4)
         only — a real ServiceCollection/ServiceProvider, never a mock
         container, to prove DI registration end to end with no host. -->
    <PackageReference Include="Microsoft.Extensions.DependencyInjection" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="../../src/Orders/Orders.csproj" />
    <ProjectReference Include="../../src/SharedKernel/SharedKernel.csproj" />
    <!-- Test project only — asserts the four EventType literals are keys of
         FactCatalog.PayloadTypesByEventType (design.md §11.4). The barred
         dependency is Domain -> Contracts (OrdersDomainMustNotDependOnContracts,
         design.md §11.2), not test -> Contracts. -->
    <ProjectReference Include="../../src/Contracts/Contracts.csproj" />
  </ItemGroup>

</Project>
