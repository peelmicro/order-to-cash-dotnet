{
  "project": "order-to-cash-dotnet",
  "description": "Order-to-cash lifecycle backbone as event-driven microservices (.NET 10 + Next.js + MS-SQL Server + MongoDB + Kafka + NATS). Assessment #8 of the order-to-cash trilogy, and its first REUSE RUN: specs/shared/, the harness, the n8n workflows and the stack-agnostic infra are copied from #7, not rewritten. progress/history.md must yield an honest #7 vs #8 benchmark.",
  "rules": {
    "one_feature_at_a_time": true,
    "require_tests_to_close": true,
    "require_approved_spec_to_implement": true,
    "require_effort_record_to_close": true,
    "claude_never_commits": true,
    "valid_status": [
      "pending",
      "spec_ready",
      "in_progress",
      "in_review",
      "done",
      "blocked"
    ],
    "sdd_required_when": "feature has \"sdd\": true",
    "state_machine": "pending -> [spec_author] -> spec_ready -> HUMAN APPROVES -> in_progress -> [implementer] -> in_review -> [reviewer] -> done | blocked",
    "shared_spec_is_read_only": "a change to specs/shared/ is a spec amendment: explicit, human-gated, back-ported to #7 — never a silent fork",
    "reused_ids": "R<n> ids are #7's. Reusing one asserts the .NET realisation satisfies the same requirement."
  },
  "features": [
    {
      "id": 1,
      "name": "repo_bootstrap",
      "phase": 1,
      "title": "Repository bootstrap: .NET 10 SDK pin, Node LTS for the web, git hygiene",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "dotnet SDK and runtimes verified by scaffolding, building and testing real templates",
        "Node LTS and pnpm verified (web app only)",
        ".gitignore, .editorconfig, .nvmrc, global.json committed",
        "repo-local git identity + account-explicit remote set before the first push",
        "push to origin verified"
      ]
    },
    {
      "id": 2,
      "name": "harness_layer",
      "phase": 2,
      "title": "Agent harness copied from #7 and re-pointed: AGENTS.md, CLAUDE.md, backlog, init.sh, progress/, agents, CHECKPOINTS.md",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "AGENTS.md, CLAUDE.md, CHECKPOINTS.md exist, re-pointed to .NET",
        "feature_list.json reset to the .NET backlog and validating against its own rules",
        "init.sh exits 0 on a clean clone and exits 1 on each broken invariant",
        "progress/current.md from the template and history.md EMPTY — #8's effort records must be #8's",
        "six agents in .claude/agents/ each declaring its model, pinning copied from #7 unchanged",
        "docs/PROCESS.md copied and re-pointed"
      ]
    },
    {
      "id": 3,
      "name": "shared_spec",
      "phase": 3,
      "title": "specs/shared/ copied VERBATIM from #7: domain model, saga, EARS requirements, AsyncAPI, OpenAPI, test matrix",
      "sdd": false,
      "owner": "spec_author",
      "status": "done",
      "acceptance": [
        "all seven files byte-identical to #7's, proven by diff, except test-matrix.md's Status column reset to TODO",
        "source commit SHA of the #7 checkout recorded in the commit message",
        "n8n/workflows/*.json copied unchanged",
        "specs/shared/ re-read end to end — reuse is not an excuse to not know the spec",
        "any stack leak found in #7's 'reusable part' reported back as a #7 defect"
      ]
    },
    {
      "id": 4,
      "name": "infra_compose",
      "phase": 4,
      "title": "docker-compose.infra.yml: MS-SQL, MongoDB, Kafka, Redpanda Console, NATS, Mailpit, n8n, OTel, Jaeger, Prometheus, Grafana",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "all infra containers healthy",
        "four otc_* databases and the app login created by the MS-SQL init script",
        "NATS runs core-only (no JetStream)",
        "n8n on its SQLite volume (n8n does not support MS-SQL)",
        "OTel Collector, Prometheus and Grafana configs reused from #7 unchanged",
        "MS-SQL container RAM footprint measured and recorded",
        ".env.example complete"
      ]
    },
    {
      "id": 5,
      "name": "messaging_topology",
      "phase": 4,
      "title": "Kafka fact topics + .dlq companions and the NATS subject registry",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "3 fact topics + 3 dlq topics created idempotently by the reused script",
        "topics match asyncapi.yaml exactly",
        "Redpanda Console lists all 6",
        "NATS subject list verified against asyncapi.yaml — none invented, none missing"
      ]
    },
    {
      "id": 6,
      "name": "monorepo_scaffold",
      "phase": 5,
      "title": "OrderToCash.sln scaffold: Directory.Build.props, central package management, NetArchTest architecture tests, quality script",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "dotnet build works from the solution root",
        "global.json honoured; TreatWarningsAsErrors on",
        "NetArchTest fails the build on a deliberate Domain-layer violation of each forbidden reference",
        "NetArchTest fails on a deliberate decimal in domain arithmetic",
        "./quality.sh runs format check + build + test + coverage"
      ]
    },
    {
      "id": 7,
      "name": "shared_kernel",
      "phase": 5,
      "title": "src/SharedKernel: Money (long cents), Quantity, GLN (mod-10), OrderNumber, UniqueId, Entity, AggregateRoot",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "zero PackageReference entries, asserted by an architecture test",
        "GLN check digit validated with real and invalid GLNs",
        "Money rejects cross-currency arithmetic and holds long minor units",
        "pure xUnit unit tests, no framework references"
      ]
    },
    {
      "id": 8,
      "name": "contracts_package",
      "phase": 5,
      "title": "src/Contracts: envelope + event and DTO types generated from AsyncAPI/OpenAPI, and the single shared JsonSerializerOptions",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "types hand-written from the copied asyncapi.yaml and openapi.yaml, with a test asserting every spec-declared fact type and required field is represented",
        "one shared JsonSerializerOptions in Contracts: camelCase, nulls omitted, no $type discriminator, no PascalCase envelope",
        "ENVELOPE byte-exact against the 12 real #7 envelopes captured in tests/Contracts.UnitTests/GoldenEnvelopes/ (field set and order: eventId, eventType, aggregateId, correlationId, causationId, occurredAt, payload)",
        "PAYLOAD semantically equal to the golden envelopes — same keys, values, types and casing; key ORDER deliberately not asserted (see CLAUDE.md: #7's payload order is MySQL json-column normalisation, not a serializer decision)",
        "round-trip: every golden envelope deserialises into the Contracts types and re-serialises to a semantically equal document"
      ]
    },
    {
      "id": 9,
      "name": "db_orders",
      "phase": 6,
      "title": "EF Core model + migrations for otc_orders (incl. outbox, processed_events, saga_commands, saga_ignored_facts, order_number_sequences)",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "migration applies against the composed mssql container and re-applies cleanly from empty",
        "MS-SQL types per the spec: uniqueidentifier, datetime2(3), nvarchar, bigint IDENTITY for outbox.seq",
        "indexes match the shared spec"
      ]
    },
    {
      "id": 10,
      "name": "db_fulfillment",
      "phase": 6,
      "title": "EF Core model + migrations for otc_fulfillment (stock, reservations, despatches, despatch_items + reliability tables)",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "migrations run from empty",
        "round-trip integration test per table"
      ]
    },
    {
      "id": 11,
      "name": "db_billing",
      "phase": 6,
      "title": "EF Core model + migrations for otc_billing, plus the processed_events-only otc_notifications",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "both migrations apply and re-apply cleanly",
        "outbox/processed_events parity test across all four DbContexts passes",
        "otc_notifications contains processed_events and nothing else"
      ]
    },
    {
      "id": 12,
      "name": "seed_job",
      "phase": 7,
      "title": "Deterministic one-shot seed job producing the SAME dataset as #7 (parties, GLNs, credit limits, stock, sample orders)",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "same currencies, products, retailers, companies, GLNs, credit limits and stock as #7",
        "sample completed orders and one cancelled order, with their order_timeline documents",
        "idempotent — running twice is a no-op",
        "row-count and checksum comparison against #7's seeded databases passes"
      ]
    },
    {
      "id": 13,
      "name": "orders_aggregate",
      "phase": 8,
      "title": "Order aggregate + full status state machine",
      "sdd": true,
      "status": "done",
      "acceptance": [
        "every legal transition allowed, every illegal one throws",
        "no empty orders, no line mutation after confirmed",
        "totals always consistent with lines",
        "cancelled requires a reason",
        "pure domain unit tests, zero framework imports"
      ]
    },
    {
      "id": 43,
      "name": "cqrs_dispatcher",
      "phase": 8,
      "title": "In-process CQRS dispatcher: ICommandHandler/IQueryHandler/IEventHandler + startup validation (no MediatR)",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "ICommandHandler<T>, ICommandHandler<T,R>, IQueryHandler<T,R>, IEventHandler<T> + a Dispatcher resolving from IServiceProvider",
        "handlers registered by assembly scan",
        "startup validation FAILS FAST when a command has zero handlers or more than one — proven by a test for each case",
        "no MediatR reference anywhere in the solution"
      ],
      "note": "New in #8. #7 got this free from @nestjs/cqrs; MediatR v13 is commercially licensed, so it is hand-rolled here. Expect this feature to have NO #7 counterpart in the benchmark table."
    },
    {
      "id": 14,
      "name": "outbox_and_idempotency",
      "phase": 8,
      "title": "Transactional outbox + polling relay + idempotent consumer pattern",
      "sdd": true,
      "status": "done",
      "acceptance": [
        "aggregate row and outbox row written in one transaction",
        "relay publishes to Kafka keyed by correlationId and stamps publishedAt",
        "safe under concurrent relay instances",
        "redelivery deduplicated via processed_events"
      ]
    },
    {
      "id": 15,
      "name": "orders_acceptance",
      "phase": 8,
      "title": "Order acceptance: orders.create NATS responder + fulfillment.stock.check client",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "synchronous stock check via NATS fulfillment.stock.check",
        "order id returned synchronously",
        "rejection when stock check fails"
      ]
    },
    {
      "id": 16,
      "name": "order_saga_orchestrator",
      "phase": 8,
      "title": "Saga orchestrator: advances on Kafka facts, commands over NATS, both compensation paths",
      "sdd": true,
      "status": "done",
      "acceptance": [
        "happy path reaches invoiced against stubbed responders",
        "CreditRejected releases stock and cancels the order",
        "StockRejected cancels without a release",
        "idempotent under event redelivery",
        "every step recorded"
      ]
    },
    {
      "id": 17,
      "name": "fulfillment_stock",
      "phase": 9,
      "title": "StockItem aggregate + reservation lifecycle (reserve / release / consume)",
      "sdd": true,
      "status": "done",
      "acceptance": [
        "invariant reservedUnits <= units enforced in the aggregate",
        "StockRejected when insufficient",
        "release is idempotent, cannot release twice",
        "check-then-reserve race covered by an integration test"
      ]
    },
    {
      "id": 18,
      "name": "fulfillment_despatch",
      "phase": 9,
      "title": "DespatchAdvice (DESADV) creation consuming reservations",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "reservations move to consumed",
        "OrderDespatched emitted via outbox"
      ]
    },
    {
      "id": 19,
      "name": "billing_credit",
      "phase": 10,
      "title": "BuyerCredit aggregate + hold/release/consume ledger",
      "sdd": true,
      "status": "done",
      "acceptance": [
        "availableCredit = limit - active holds - open invoices",
        "ledger is append-only",
        "over-limit hold is rejected",
        "release on compensation and on invoice paid"
      ]
    },
    {
      "id": 20,
      "name": "billing_credit_simulator",
      "phase": 10,
      "title": "Credit-check simulator (.99 rule + CREDIT_FAILURE_RATE)",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "rejects totalAmount % 100 === 99 deterministically",
        "CREDIT_FAILURE_RATE defaults to 0",
        "sits behind the credit port"
      ]
    },
    {
      "id": 21,
      "name": "billing_invoicing",
      "phase": 10,
      "title": "Invoice aggregate (INVOIC) with issued -> paid state machine",
      "sdd": true,
      "status": "done",
      "acceptance": [
        "issued on OrderDespatched",
        "paidAt set exactly when status becomes paid",
        "totals consistent with lines"
      ]
    },
    {
      "id": 22,
      "name": "billing_remittance_intake",
      "phase": 10,
      "title": "Remittance intake: PaymentReceived + credit release + invoice paid",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "idempotent by paymentReference",
        "no internal payment timer anywhere",
        "emits PaymentReceived and CreditReleased"
      ]
    },
    {
      "id": 23,
      "name": "notifications_service",
      "phase": 11,
      "title": "Notifications Worker Service: MailKit -> Mailpit, console adapter, durable processed_events ledger",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "real email verified in the Mailpit inbox for each notified fact",
        "console adapter used in tests via the same port",
        "idempotent by eventId"
      ]
    },
    {
      "id": 24,
      "name": "projector_read_model",
      "phase": 12,
      "title": "Projector Worker Service: every fact into order_timeline, idempotent, statusRank, placeholders",
      "sdd": true,
      "status": "done",
      "acceptance": [
        "out-of-order delivery still yields the correct final status",
        "replaying a topic reproduces an identical document",
        "emits the update signal the Gateway turns into SSE"
      ]
    },
    {
      "id": 25,
      "name": "gateway_rest_auth",
      "phase": 13,
      "title": "Gateway REST per the copied OpenAPI + JWT auth + login rate limiting",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "contract test asserts no drift from openapi.yaml",
        "RPC timeouts mapped to HTTP status codes",
        "list/detail served from MongoDB, never from a write DB"
      ]
    },
    {
      "id": 26,
      "name": "gateway_sse_push",
      "phase": 13,
      "title": "SSE push of saga progress with heartbeat and Last-Event-ID reconnect",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "client reconnect resumes without duplicates",
        "heartbeat keeps the connection alive"
      ]
    },
    {
      "id": 27,
      "name": "observability_reliability",
      "phase": 14,
      "title": "Terminus health checks, OTel propagation through NATS and Kafka, structured logging, retry + DLQ, and orders.create requestId idempotent replay",
      "sdd": true,
      "status": "done",
      "acceptance": [
        "one trace spans HTTP -> NATS -> the write database -> Kafka -> consumers (the bullet was inherited from #7 naming MySQL; #8 is MS-SQL and #9 will be a third engine, so the hop is named by role rather than by product)",
        "correlationId in every log line",
        "failed processing lands on <topic>.dlq after N attempts",
        "readiness includes DB, Kafka and NATS indicators",
        "a repeated orders.create request carrying the same requestId returns the ORIGINAL order (same orderId/orderReference/money fields), not a second placement",
        "concurrent duplicate requests for the same requestId never place two orders",
        "requestId remains optional: omitting it places a normal, non-deduplicated order"
      ],
      "notes": "#7 WIDENED this feature before speccing, at its human's request, to fold in a dormant entry (orders_idempotent_replay, its former id 39) — which is why the last three acceptance bullets are about requestId rather than observability. #8 inherited the widened form, so that decision is already made and is not a gate question. #7's counterpart was the largest feature in its project by every measure: 1 spec session + 1 gate + SIX implementer passes + 1 review, approved first time. The first acceptance bullet named MySQL, inherited verbatim; corrected at the phase-14 dispatch to name the hop by role, since #8 is MS-SQL and #9 will be a third engine."
    },
    {
      "id": 28,
      "name": "saga_e2e_verification",
      "phase": 15,
      "title": "End-to-end saga verification before any frontend work",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "happy path reaches completed",
        ".99 order compensates visibly",
        "redelivery causes no corruption",
        "a poisoned message reaches the DLQ",
        "R56's composed-stack trace observation: one trace id spans the inbound request, every command, every write-model transaction and every fact publication/consumption for one order, across the real running services"
      ],
      "notes": " || CLOSED 2026-09-15 by leader (Sonnet 5 session): all 5 acceptance criteria proven against real infrastructure in tests/Gateway.IntegrationTests/SagaEndToEndVerificationTests.cs (Testcontainers MsSql x3 + Kafka + NATS + Mongo, in-process real hosts via each services own XyzHost.CreateBuilder, ported from #7s proven spawn-fleet architecture). Happy path, .99 compensation, redelivery, DLQ all passed on first implementation, confirmed twice. R56 (composed trace) initially found a REAL production gap: SagaCommandDispatchWorkers fast-path channel (backlog id 80) carried no trace context, so Orders/Fulfillment/Billing each saw a different trace id for one orders journey. FIXED IMMEDIATELY per user directive (not filed/deferred): added TraceParent to SagaCommandRef (auto-captured from Activity.Current, zero call-site edits needed) and restored it as a linked span parent in ConsumeLoopAsync, reusing the existing OutboxRelay pattern. All 5 criteria now pass, confirmed on two full runs. Regression check: full solution rebuild 0 warnings/0 errors; all 11 unit-tier projects green, 1467 tests, 0 failed (leader verified independently, not just implementer report); Gateway.IntegrationTests 70/70 (was 69 passed/1 skipped before the fix). progress/impl_id28_saga_e2e_verification.md and progress/impl_saga_trace_context_fast_path_fix.md"
    },
    {
      "id": 29,
      "name": "web_app",
      "phase": 16,
      "title": "Next.js App Router web app: shadcn/ui, TanStack Query, Tailwind v4, live SSE saga timeline",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "types generated from specs/shared/openapi.yaml into apps/web (ORIGINAL BULLET, copied verbatim from #7, read 'types imported from packages/contracts' — #8 has no packages/ workspace; #7's @otc/contracts generated its OpenAPI types from this same file, so this is the faithful translation). The generated file is the ONLY place any page, hook or route handler takes a wire shape from, and a drift check FAILS when the generated types are stale against openapi.yaml — #7 got that property from packages/contracts/scripts/check.mts, and without an equivalent #8 would supply it with nothing (the same ported-idiom row as backlog id 93)",
        "live SSE timeline with reconnect: the browser resumes with Last-Event-ID after a dropped connection and neither loses nor duplicates a frame; order.updated and timeline.appended are de-duplicated SEPARATELY, never through one shared set keyed on eventId (#7 review_web_app.md Pass 3, Finding 1: the projector legitimately gives both frame types the same eventId, and a shared set silently dropped one — a stuck timeline)",
        "Register payment button works: a decimal typed by the user reaches the wire as exact integer minor units with no floating-point arithmetic ('19.99' -> 1999), and an accepted payment is rendered distinctly from a duplicate (R47/R48)",
        "honest waiting-for-projection state: GET /orders/{id} answering 202 (with Retry-After) renders a named waiting state that retries — never an error, never a 404, never an endless spinner (R55)",
        "every error shown to a user is THAT error's own text, read from the RFC 9457 problem document's detail (falling back to title) — never a generic message. Proven against a REAL Gateway response, not only against a mock: #7's closure review records that every page's error text was silently generic from the day its app was built, because the field each page read was always undefined, and four review passes walked past it because every mock encoded the same wrong shape",
        "the JWT never reaches the browser: it is held server-side behind an httpOnly session cookie and every Gateway call is made from a Next.js route handler, including the SSE stream, which is proxied with Last-Event-ID forwarded and the upstream request aborted when the client disconnects",
        "no control is disabled or hidden waiting for hydration in a way a user can get stuck on (#7 Pass 2a and Pass 4: a submit button shipped permanently disabled from first render)",
        "apps/web is wired into ./quality.sh — lint, typecheck, test and build — and each gate is proven to FAIL on a planted violation: #7's review found its web lint script exiting 0 while linting zero component files, with a real bug present",
        "pages: login, orders list, place order (retailer/company/product/currency selection, quantity, unit-price override), order detail with live timeline and causal links, stock (list + replenish), billing (invoices with Register payment, credits); a 409 StockUnavailableProblem renders its shortages",
        "loading-state tests hold the request on an UNRESOLVED promise and assert the loading UI — a loading test that passes with the request already resolved is vacuous (#7 Pass 7 concern 1)"
      ],
      "notes": "REWORKED 2026-09-16 by the leader before dispatch. #7 built its web app in NUXT 4 + shadcn-vue; #8's is Next.js + shadcn/ui BY DESIGN — the trilogy varies the frontend (#9 is Angular) — so this is a genuine rewrite with #7's app as the BEHAVIOURAL specification, never code to copy. The original acceptance was #7's four bullets copied verbatim, one of which ('types imported from packages/contracts') cannot be satisfied in a repository with no packages/ workspace. It is replaced by its faithful translation, and SIX bullets are added from #7's own record of what went wrong: #7's web_app was the most-reviewed feature in that assessment — 14 implementer passes and 7 review passes, 2 REJECTED — and every added bullet names a defect #7 actually shipped or nearly shipped and a review actually caught. They are in the contract, not only in a brief, so that a reviewer must hold them. || ORIGINAL NOTES: "
    },
    {
      "id": 30,
      "name": "web_component_tests",
      "phase": 17,
      "title": "Web component tests: Vitest + React Testing Library (no Jest)",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "timeline, place-order form, stock and billing tables covered",
        "SSE HOOK tested with a fake EventSource (ORIGINAL BULLET said 'SSE composable' — Vue terminology copied from #7; the plan document's own phase-17 row already says 'the SSE hook'), and in addition the stream client exercised against a REAL local SSE server with a forced mid-stream disconnect, asserting the Last-Event-ID resume — #7's strongest test of this behaviour, which the closure review called stronger than the literal criterion"
      ],
      "notes": "REWORKED 2026-09-16: in #7 this phase closed as a byproduct of the web app, because each web_app pass shipped its own component tests. #8 does the same — these tests are written inside id 29's loop, and this entry is verified when id 29 closes. || ORIGINAL NOTES: "
    },
    {
      "id": 31,
      "name": "api_tests",
      "phase": 18,
      "title": "API tests: xUnit + HttpClient black-box through the Gateway — the SAME script #7's API tests prove",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "full happy path",
        "full compensation path",
        "duplicate paymentReference yields one payment",
        "R24's API half — this feature is its named closer (test-matrix.md's R24 cell, until entry 72 corrects it, still says 'the gateway feature', which shipped in phase 13 without closing it): the happy-path scenario asserts the completion triple is present on the timeline AND causally ordered, applied structurally rather than as a hand-written expected sequence — #7's amendment A1 to apps/gateway/src/black-box-api.integration.spec.ts scenario 1, after progress/review_api_tests.md §2 defect D4 found the status-only assertion could rot",
        "R49's API half — this feature is its named closer. test-matrix.md's R49 cell, until entry 72 corrects it, says 'no Gateway yet', which is false: POST /invoices/{id}/payments ships at src/Gateway/Presentation/Endpoints/InvoicesEndpoints.cs:32 from feature 25 (done, phase 13), and the four Gateway tests that exercise it (tests/Gateway.IntegrationTests/InvoicesHttpTests.cs) all stub IRpcClient, so none reaches Billing. Assert through the REAL Gateway that a mismatched amount, a mismatched currency and a different paymentReference against an already-paid invoice are each rejected with a machine-readable code, and that Billing's OWN database shows nothing changed. #7 never closed this at API level either — its own R49 cell still cross-references a 'same caveat as R48' that its R48 stopped carrying when feature 31 corrected it (8a3a3d3) — so #8 closes it first rather than inheriting the residue"
      ]
    },
    {
      "id": 32,
      "name": "e2e_playwright",
      "phase": 19,
      "title": "Playwright end-to-end tests against the composed stack",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "order reaches completed in the UI",
        ".99 order reaches cancelled with compensation visible"
      ]
    },
    {
      "id": 33,
      "name": "n8n_workflows",
      "phase": 20,
      "title": "Four n8n demo workflows, Gateway REST API only, exported as JSON",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "order generator, bank robot, stock replenishment, burst",
        "auto-imported on container startup",
        "removing n8n does not break the stack"
      ]
    },
    {
      "id": 34,
      "name": "sonarqube_quality_gates",
      "phase": 21,
      "title": "Quality gates: analyzers as errors, dotnet format, coverlet thresholds, SonarQube optional profile",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "coverage gate proven to FAIL when breached, not merely configured",
        "dotnet format clean at solution level",
        "SonarQube runs under its optional compose profile"
      ]
    },
    {
      "id": 35,
      "name": "observability_dashboards",
      "phase": 22,
      "title": "Observability verification: the REUSED Grafana dashboard shows live data; one Jaeger trace spans the whole saga",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "panels: saga duration, per-service latency, consumer lag, outbox lag, DLQ depth",
        "dashboard auto-provisioned",
        "single distributed trace screenshot captured"
      ]
    },
    {
      "id": 36,
      "name": "full_docker_compose",
      "phase": 23,
      "title": "Full stack compose (~18 containers) with healthcheck-gated dependencies",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "cold start reaches a demoable state within ~2 minutes",
        "infra-only mode still works with apps run from the CLI"
      ]
    },
    {
      "id": 37,
      "name": "documentation_demo",
      "phase": 24,
      "title": "Documentation + demo + the #7 vs #8 BENCHMARK section",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "README per the Task, architecture and saga diagrams, Kafka-vs-NATS matrix, trade-offs",
        "AI-process section and the demo GIF (same script as #7)",
        "benchmark table from both repos' progress/history.md, with an honest reading of what was NOT faster",
        "spec amendments listed with their back-port status to #7"
      ]
    },
    {
      "id": 38,
      "name": "final_checkpoint",
      "phase": 25,
      "title": "Final checkpoint: backlog all done, every EARS R# traced, amendments back-ported",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "feature_list.json all done",
        "every EARS requirement traced to a green xUnit/Vitest/Playwright test",
        "CHECKPOINTS.md C1-C7 fully walked",
        "spec amendments back-ported to #7 or deferred with a reason",
        "stack-comparison document fully promoted from pre-resolved to confirmed"
      ],
      "notes": "CLOSED 2026-09-18 (phase 25, the trilogy final checkpoint). All five acceptance bullets satisfied: (1) feature_list.json all done -- 110/110, this entry included; (2) every EARS requirement traced to a green test -- specs/shared/test-matrix.md Coverage summary corrected to 63/0/0 (two stale rows, R48 and R56, were already proven by shipped tests but never had their Status cells updated -- caught and fixed at this checkpoint); (3) CHECKPOINTS.md C1-C7 walked by an Opus reviewer, 40 boxes, 7 initially empty (D1-D7), all seven dispositioned -- two mechanical text fixes, seven missing progress/history.md effort records written, a mislabeled README benchmark table corrected against each phase real total (the real phase-14 gap, ~3.1x, was the builds largest and had gone missing from the table entirely), a git-history ordering gap accepted with evidence and one line added to docs/lessons.md for #9, and the three previously-untested n8n workflows fired for real against the live Gateway; (4) all five spec amendments (SA-1 through SA-5) confirmed back-ported to #7 by commit, done at phase 24; (5) the external Stack Comparison study document promoted from pre-resolved to confirmed, section 9 closed to a single legitimately #9-gated item. Also resolved at this same close-out, ahead of the checkpoint walk: the six pre-existing pending backlog entries found at phase 24s own close (ids 52, 106, 107, 108, 109, plus two new ones the id-52 audit surfaced, 110 and 111) -- all disposed with a fix, an acceptance-with-evidence, or a leader-direct config edit. Full record: progress/history.md, Id 38 entry."
    },
    {
      "id": 40,
      "name": "orders_catalog_responder",
      "phase": 13,
      "title": "Orders: catalog.reference.list NATS responder for products/retailers/companies",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "GET /catalog/products, /catalog/retailers, /catalog/companies return real data through the Gateway",
        "no new bounded context — domain-model.md already assigns the reference catalogue to Orders",
        "reuses the same reference-data lookup PlaceOrderHandler already calls, exposed as a query rather than duplicated"
      ]
    },
    {
      "id": 41,
      "name": "orders_cancel_responder",
      "phase": 13,
      "title": "Orders: orders.cancel NATS responder for operator-initiated cancellation",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "POST /orders/{id}/cancel succeeds through the Gateway for a cancellable order",
        "reuses the Order aggregate's existing cancelled state and cancellationReason invariant — no new domain modeling",
        "an order in a terminal state (invoiced, completed, already cancelled) is rejected with a domain error, not a 503",
        "the operator note from CancelOrderRequest lands on the read-model timeline"
      ]
    },
    {
      "id": 42,
      "name": "orders_saga_terminal_rejection_classification",
      "phase": 8,
      "title": "NatsSagaCommandsAdapter: distinguish a terminal RpcError (business rejection) from a retryable transport error",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "a PRECONDITION_FAILED (or other terminal-business-outcome) RpcError reply from a responder is classified as terminal, not retried at capped backoff forever",
        "a genuinely retryable transport failure (timeout, connection refused, no responder) is unaffected — still retried exactly as today",
        "a saga_commands row that receives a terminal RpcError reaches a resolved end state (parked-with-reason, or equivalent) rather than retrying indefinitely"
      ]
    },
    {
      "id": 45,
      "name": "order_number_allocator_seed_race",
      "phase": 8,
      "title": "EfCoreOrderNumberAllocator: lock the self-seed existence check so two genuinely concurrent first-ever ORD allocations cannot both lose the seed race",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "the self-seeding branch's IF NOT EXISTS ... INSERT no longer races: either it takes a lock (WITH (UPDLOCK, HOLDLOCK)) or it handles the duplicate-key violation and retries",
        "sixteen genuinely concurrent first-ever allocations against a never-seeded sequence table all succeed and yield sixteen distinct ORD-###### references",
        "OrderNumberAllocatorTests.AllocateNextAsync_ConcurrentFirstEverAllocations_CanRaceTheSelfSeedInsertAndFail is INVERTED, not deleted — it currently asserts the race reproduces, and must become an assertion that it no longer can",
        "the fix is armed: reverting the lock (or the retry) makes the inverted test fail"
      ],
      "notes": "A defect in closed, committed feature 15 (orders_acceptance), found by feature 16's review and confirmed by an integration test written during it. Exposure is narrow — only concurrent allocations against a sequence table that has never been seeded — but the failure is a primary-key violation surfaced to the caller. Recorded here rather than left in a test comment so it cannot evaporate; see progress/review_order_saga_orchestrator.md advisory A1 and progress/impl_order_saga_orchestrator.md §12. RESTORED 2026-09-04 after a reviewer's `git checkout --` on this uncommitted file destroyed the original entry; wording is byte-identical to the original."
    },
    {
      "id": 46,
      "name": "orders_stock_check_rpc_error_discriminator",
      "phase": 9,
      "title": "NatsStockAvailabilityChecker: an RpcError reply body must map to a typed error, not deserialise to a null Lines and throw NullReferenceException",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "an RpcError-shaped reply to fulfillment.stock.check is discriminated before the typed deserialisation, exactly as NatsSagaCommandsAdapter does for saga commands",
        "the resulting error carries the responder's own code and message, and orders.create maps it to a meaningful RPC error rather than INTERNAL_ERROR",
        "no path through the checker can throw a bare NullReferenceException on a well-formed error reply",
        "armed: removing the discriminator makes a named test fail"
      ],
      "notes": "Found by feature 42's review (advisory A2). The saga-command adapter grew an RpcError discriminator in feature 42; the acceptance-path checker never had one, so an error reply deserialises to Lines = null and the next line throws NullReferenceException out of order acceptance. Unreachable until a real Fulfillment responder exists, which is why it is phase 9 rather than phase 8 — it should close alongside the responder that first makes it reachable, not be discovered by an NRE in an integration run. See progress/review_orders_saga_terminal_rejection.md advisory A2."
    },
    {
      "id": 47,
      "name": "order_number_allocator_scan_cost",
      "phase": 21,
      "title": "EfCoreOrderNumberAllocator: the atomic seed scans dbo.orders on every allocation, not only when seeding",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "the MAX(order_reference) scan over dbo.orders is evaluated only when the sequence row does not yet exist, not on every allocation",
        "the seeding branch stays atomic — whatever shape is chosen must still make two concurrent first-ever allocations safe, and feature 45's deterministic two-session guard must still pass",
        "the actual execution plan is captured before and after, and the report states the row count scanned in each",
        "armed: the concurrency guard from feature 45 still fails when the lock hint is removed"
      ],
      "notes": "Found by feature 45's review (advisory A1) with the actual plan attached: Clustered Index Scan over dbo.orders, Rows = 5000, Executes = 1, with an anti-semi-join discarding the row afterwards. Not blocking and not a regression against #7, whose own seeding runs an unconditional max() too — but #8's table will grow and #7's would have as well. Deferred to phase 21 rather than phase 9 because it is a performance concern with a correctness constraint attached, and phase 21 is where measurement work belongs. See progress/review_order_number_allocator_seed_race.md advisory A1."
    },
    {
      "id": 48,
      "name": "saga_command_headers_thread_safety_guard",
      "phase": 10,
      "title": "NatsSagaCommandsAdapter: the \"a fresh NatsHeaders per call\" property is structural and guarded by nothing",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "a named test in tests/Orders.UnitTests/NatsSagaCommandsAdapterTests.cs captures the NatsHeaders instance passed to RawRequester on two consecutive SendAsync calls and asserts they are reference-distinct",
        "armed: hoisting the instance to a field and clearing/re-adding per call makes that test fail while every existing header-value assertion stays green"
      ],
      "notes": "Found by feature 17's review (advisory A2). NatsHeaders is documented not thread-safe; the adapter is AddScoped and the sweeper dispatches sequentially, so the hazard is latent today — but the class remark asserts a property no test holds, and five more services will copy this shape."
    },
    {
      "id": 49,
      "name": "stock_release_deterministic_event_id",
      "phase": 10,
      "title": "OrderStockReservation.Release allocates its fact's EventId with UniqueId.New() while Reserve takes a newId delegate",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "Release takes the same Func<UniqueId> newId seam as Reserve, and its one caller in StockReservationService.ReleaseAsync supplies it",
        "a named unit test asserts the released fact's EventId is the one the delegate returned"
      ],
      "notes": "Found by feature 17's review (advisory A3); a deviation from specs/fulfillment_stock/design.md §3.3's \"no ids beyond those newId supplies\", disclosed after the fact. Harmless today and asymmetric. Cheapest to close in feature 18, which opens the same file for Consume."
    },
    {
      "id": 50,
      "name": "rpc_responder_shutdown_fault_isolation",
      "phase": 10,
      "title": "StockRpcResponder.StopAsync rethrows out of host shutdown when a drained request's ReplyAsync faults",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "a faulted in-flight task no longer propagates out of StopAsync",
        "the drain still waits for every in-flight request",
        "a named test proves shutdown completes with one faulted and one healthy in-flight task"
      ],
      "notes": "Found by feature 17's review (advisory A5), cosmetic today. Worth doing BEFORE Billing and Despatch copy the responder rather than after — the fix belongs wherever the responder family is consolidated."
    },
    {
      "id": 51,
      "name": "rpc_payload_schema_parity_from_asyncapi",
      "phase": 10,
      "title": "StockRpcPayloadTests and SagaCommandPayloadTests compare wire records against hand-retyped key lists while three documents claim they read asyncapi.yaml as text",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "both instruments derive their expected property names by parsing specs/shared/asyncapi.yaml — the pattern RpcSubjectsTests and OrdersFactTopicTests already use — covering every fulfillment.stock.* and saga-command request and reply schema",
        "armed: editing one schema's property name in a scratch copy makes the named test fail",
        "the three documents' sentences are made true or corrected — design.md §6.3, tasks.md C3, and the test's own XML doc",
        "also derives asyncapi.yaml's twelve-value RpcError.code enum by parsing the spec, and asserts the three hand-retyped copies of it agree with the parsed set — OrdersCreateErrorMapper._contractRpcErrorCodes, its test's own copy, and NatsSagaCommandsAdapter's terminal-code set"
      ],
      "notes": "Found by feature 17's review round 3 (advisory A6). NOT a defect today: the review parsed all sixteen schemas and confirmed every key set matches, and the retyped lists do catch unilateral drift of the code (a renamed reply property was killed by probe). What is missing is the correlated authoring error — a schema and a test changed together, wrongly. Filed because feature 19 copies this instrument, and the false sentence, into three more services. Cheapest at the relay-family consolidation. Widened by feature 46's review round 2: the D2 fix added a THIRD hand-retyped copy of the same enum, and deleting one value from the mapper's set left all 262 unit tests green because the test holds its own retyped copy. The cure carried the disease."
    },
    {
      "id": 52,
      "name": "retroactive_ported_idiom_ledger_decode_boundaries",
      "phase": 10,
      "title": "Retroactive ported-idiom ledger for the value-crossing boundaries in services ported before the ledger was adopted",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "every value-crossing boundary in the pre-ledger services is walked: each NATS RPC client reply decode, each RPC responder request decode, the Kafka consumer envelope decode, the outbox relay's payload read-and-republish, and the Mongo projector's read path",
        "for each boundary, #7's counterpart is read and one question answered in one line: what did #7's version guarantee here, and what supplies that in #8",
        "output is a retroactive ledger section per service, plus a SEPARATE backlog entry for every gap found",
        "NOTHING is fixed in place — a fix inside a sweep is a change nobody reviewed against a spec",
        "the read is time-boxed; a boundary taking more than ten minutes to compare is itself recorded as a finding"
      ],
      "notes": "Recommended by feature 46's review, which produced the evidence that decides it. Feature 46 was dispatched specifically to port a missing guard from #7's stock-availability adapter; the implementer read that file; and a SECOND missing guard from the same thirty-line method (#7's malformed-JSON catch) went unported until the fix round closed it in-feature — advisory A1. Two unported guards in one method, one still unported after a dedicated repair pass, says the class is denser than its four known instances and that reading a file for one gap does not surface the next. The class has a cheap signature: every instance so far sits at a boundary where a value crosses into or out of the process, and those sites are enumerable rather than open-ended — roughly eight in the shipped services. The ledger protects new translations only; four services were ported before it existed. AUDIT COMPLETE 2026-09-18 (phase 25 close-out): the notes field speculating \"four services\" was wrong -- only Orders (all five phase-8 features) predates the ledger convention; Fulfillment/Billing/Notifications/Projector/Gateway already carry one from their original port. Five value-crossing boundaries walked for Orders; retroactive ledger rows appended to progress/impl_orders_acceptance.md, progress/impl_order_saga_orchestrator.md, progress/impl_outbox_and_idempotency.md. Two gaps surfaced, filed separately as ids 110 and 111 rather than fixed in place, per this entry's own acceptance bullet 4. Full record: progress/impl_retroactive_ported_idiom_ledger_decode_boundaries.md."
    },
    {
      "id": 53,
      "name": "test_reply_shape_assertions_that_only_throw",
      "phase": 10,
      "title": "Three integration tests detect a wrong-shaped RPC reply only by a null dereference, or not at all",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "tests/Orders.IntegrationTests/OrdersCreateAcceptanceTests.cs:159 asserts something: it deserialises an OrdersCreateReplyPayload and asserts NOTHING — the comment \"// the request succeeded\" is the whole claim, and an RpcError body constructs an all-defaults object without throwing",
        "tests/Fulfillment.IntegrationTests/StockReplenishTests.cs:52 and StockListTests.cs:27 assert the reply's own discriminating field before touching a collection that is null for any error body",
        "each uses the shape FS22 adopted after feature 46's D1 — read the body, then deserialise",
        "armed: making the responder answer a real RpcError makes each named test fail on its own assertion rather than on a dereference"
      ],
      "notes": "Found by feature 46's review round 2 with ONE grep over every typed deserialiser call site in tests/, after a prose sweep of the same question had been reported clear. Nine other sites pass the same check because they open with an Assert.Equal on a string field. Not a production defect — the responders answer correctly, and each test does fail today, opaquely or (in OrdersCreateAcceptanceTests' case) not at all."
    },
    {
      "id": 54,
      "name": "fulfillment_ledger_row_for_unhinted_in_transaction_reread",
      "phase": 10,
      "title": "The ported-idiom ledger has no row for the un-hinted in-transaction re-read in DespatchCreationService, whose correctness depends on two unstated properties",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "specs/fulfillment_stock/design.md §15 gains a row for the un-hinted re-read at src/Fulfillment/Application/DespatchCreationService.cs:67 (the F8 in-flight-race decision)",
        "the row names both properties its correctness rests on: that the read is issued only after LockForOrderAsync is granted, and that EfCoreUnitOfWork opens IsolationLevel.ReadCommitted under RCSI, which makes the snapshot statement-scoped rather than transaction-scoped",
        "the row states the consequence for #9 explicitly: under a transaction-scoped snapshot (PostgreSQL REPEATABLE READ) the same code returns a stale read and turns a correct idempotent repeat into a permanent UNAVAILABLE"
      ],
      "notes": "Found by feature 18's review round 2. Not a defect in #8 — the code is correct here, for two reasons neither of which is written down. It is a ledger GAP, which is the ledger's own failure mode: the row nobody wrote. Text ready in progress/review_fulfillment_despatch.md §5, row \"L1, second face\". CLOSED: the row landed as L13 in specs/fulfillment_stock/design.md, added by feature 18 fulfillment_despatch (not 19 as the attachment map guessed). It names both properties its correctness rests on, states the #9 consequence explicitly, and — correctly — records itself as a claim to verify rather than a tested property, since no #8 guard exists for the statement-scoped argument. Certified by feature 22's review; the coordinator verified the artefact before flipping the status."
    },
    {
      "id": 55,
      "name": "bc32_universal_claim_is_false_at_six_more_sites",
      "phase": 10,
      "title": "BC32 claims every integration test that deserialises an RPC reply asserts its discriminating field first; six sites still do not",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "the six sites assert the reply's own discriminating field before touching a collection: tests/Billing.IntegrationTests/CreditListTests.cs lines 39, 73 and 77, and tests/Fulfillment.IntegrationTests/StockListTests.cs lines 33, 38 and 44",
        "OR BC32's wording is narrowed to the scope it actually holds, and the requirement text stops making a universal claim",
        "whichever is chosen, an enumerating command and its complete output are recorded — the claim is a search result, not a reading"
      ],
      "notes": "Found by feature 19's review round 2 (advisory A6) with an enumerating script. These are the OPAQUE-failure tier — a dereference, so the test fails loudly but uninformatively — not the silent-pass tier that earned backlog id 53 its requirement id. Round 1 had fixed the scope at three sites and said nothing else changes, so rejecting again would have been goalpost-shifting; filed instead. A DONE traceability row resting on a false universal claim is the shape this feature already spent a round on. Advisory A7 records a smaller adjacent thing: design.md §10.5 named total/paging as StockList's first assertion and Assert.NotNull(Items) landed instead — unachievable as written for StockReplenishReplyPayload, which has no other field."
    },
    {
      "id": 56,
      "name": "composition_root_env_reads_are_unguarded",
      "phase": 13,
      "title": "Every environment-variable read in every Program.cs can be deleted with the whole suite green — no test project compiles a composition root",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "deleting any single environment-variable read reachable from a composition root makes a named test fail — including reads that live in an Options class rather than in Program.cs",
        "the mechanism is chosen once and applied to every composition root — an options-binding test that drives the real Program.cs path, or a startup smoke test per service, not a per-variable assertion",
        "the guard covers EVERY environment-variable read REACHABLE FROM A COMPOSITION ROOT, wherever the read physically lives, and is inherited by later services rather than re-invented. The population is NOT confined to Program.cs and the entry's original wording was wrong about that: as of phase 13 there are 102 reads across 18 files, of which only 52 are in the seven Program.cs files — the Gateway deliberately moved its reads into Options classes (JwtOptions, GatewaySseOptions, GatewayMongoOptions, LoginThrottleOptions, OperatorIdentityLoader), which is the SHAPE THIS ENTRY WANTS, not an evasion of it. Re-enumerate before claiming coverage; the count is a reading, not a constant",
        "armed: the report names which read was deleted and the verbatim failure"
      ],
      "notes": "Found by feature 20's review (advisory A1) with a concrete probe: deleting the CREDIT_FAILURE_RATE read from src/Billing/Program.cs:23 leaves the whole suite green. NOT that feature's defect — all 34 reads across the three composition roots are equally unguarded, because no test project compiles a Program.cs. Filed against phase 13 rather than 10 because three more composition roots arrive in phases 11-13 and the fix should be chosen once and inherited, not applied four times. The shape this repository keeps paying for: a property that is real, correct, and guarded by nothing. SCOPE CORRECTED at the phase-13 dispatch: the original text said 'in every Program.cs', which is narrower than the defect — 20 of the 102 reads sit in EF Core DbContextFactory classes used by design-time tooling rather than by the host, and the reviewer should rule on whether those are in scope or are a documented exclusion. Deciding that is part of the feature, not a precondition for it."
    },
    {
      "id": 57,
      "name": "completion_pair_has_no_causal_edge",
      "phase": 11,
      "title": "credit.released.v1 carries the same causationId and an identical occurredAt as payment.received.v1, so the completion pair is a tie group with no recorded causal edge",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "credit.released.v1's causationId is the eventId of payment.received.v1, not the request id both currently share",
        "the two facts no longer carry an identical occurredAt, or the ordering is otherwise recoverable from the envelope alone",
        "a consumer reading only the two envelopes can determine which caused which, without relying on outbox sequence or arrival order",
        "armed: reverting the causal stamp makes a named test fail on the envelope, not on row order"
      ],
      "notes": "Found by feature 22's review (finding N1). NOT blocking: R47-R49 and id 22's acceptance bullets are all met, and #7's own feature 22 shipped without it. But this is exactly what #7's spec amendment A1 exists to prevent, and #8 inherited the amended spec — so #8 is shipping against the letter of an amendment it was given for free. #7 fixed it in this same file eight phases later as rework (bf59af9). Filed against phase 11 because it is one entry away from free here and a cross-service reopen in phase 12, when the projector starts reading causal chains to build the timeline."
    },
    {
      "id": 58,
      "name": "notification_envelope_copy_is_unguarded",
      "phase": 12,
      "title": "NotificationFactsConsumer.ToEnvelope copies envelope fields by hand and corrupting the correlationId copy leaves the whole suite green",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "corrupting the correlationId copy in NotificationFactsConsumer.ToEnvelope makes a named test fail",
        "the same holds for every field the hand-written copy touches, not only correlationId",
        "armed: the report names each field, its mutation and the verbatim failure"
      ],
      "notes": "Found by feature 23's review round 2 (R2-D5, medium, not blocking). Round 1 had verified the field correct against real Mailpit bytes and prescribed the guard shape that was built, so this is a residual rather than a re-rejection. One assertion inside the existing theory. Filed against phase 12 because the projector reads envelopes and will have these files open."
    },
    {
      "id": 59,
      "name": "date_typed_payload_sites_survive_mutation",
      "phase": 12,
      "title": "14 date-typed payload sites survive mutation — the string-scoped template sweep did not reach them",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "each of the 14 date-typed sites either fails a named test under mutation, or is classified as a field no requirement constrains, with the reason given per site",
        "the sweep that measures this covers date and money sites as well as string sites",
        "the sweep is proved with sentinels before its result is believed: a mutation that must be caught, one that must survive, and one that must report not-applied"
      ],
      "notes": "Found by feature 23's review round 2 (R2-D6, low) with the reviewer's own independently-written sweep: 89 sites, applied=True verified by SHA-256 on every one, 70/70 string sites caught, 5 money sites caught, 14 DATE sites survived. The implementer's sweep was string-scoped and could not see them. Same class as id 58 and the same files are open in phase 12."
    },
    {
      "id": 60,
      "name": "envelope_fixture_collisions_defeat_provenance_assertions",
      "phase": 13,
      "title": "Test fixtures that give two envelope fields the same value make a transposition undetectable, and two more consumers carry the same latent collision",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "every fixture building an Envelope for a consumer test gives each of eventId, aggregateId, correlationId and causationId a DISTINCT value, so a positional transposition cannot pass",
        "the two known sites are fixed: tests/Orders.UnitTests/SagaFactsConsumerTests.cs:106 and tests/Projector.UnitTests/ProjectorFactsConsumerTests.cs:130",
        "the set of such fixtures is ENUMERATED with a path-excluding command whose complete output and per-hit classification are in the report — not swept in prose",
        "armed: for each site, two same-typed fields are transposed at the point the fixture feeds, and the report names the test that fails and its verbatim message",
        "a collision that is deliberate is allowed to stay, provided the report says which field pair and why no requirement distinguishes them"
      ],
      "notes": "Found by the review of ids 58/59. Latent rather than live: grep finds exactly ONE hand-written envelope copy in the solution (Notifications' ToEnvelope), so no transposition is currently undetected — but these two fixtures would not catch one if it were written, and the Envelope constructor takes four Guid arguments in a row. This is the feature-18 lesson one level down: Assert.NotEqual proves non-collision and never provenance, and a fixture whose values collide defeats an assertion that was written correctly. Filed against phase 13 because the Gateway adds the next envelope-reading surface."
    },
    {
      "id": 61,
      "name": "catalog_ordering_claim_guarded_for_products_only",
      "phase": 13,
      "title": "EfCoreOrderReferenceCatalog orders four listings by code and only the products one is guarded — deleting the other three .OrderBy calls leaves the suite green",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "deleting the .OrderBy from EACH of the four list methods in EfCoreOrderReferenceCatalog makes a named test fail — products is already guarded, the other three are not",
        "each guard seeds rows OUT of code order so the assertion cannot pass on insertion-order luck, which is how the products guard was itself proved falsifiable",
        "armed: the report names each deleted .OrderBy and the verbatim failure",
        "if any of the three carries no ordering requirement, the .OrderBy is DELETED rather than guarded, and the report says which and why"
      ],
      "notes": "Found by the round-2 review of feature 40 as advisory A5, with a probe: deleting .OrderBy from the retailers, companies and currencies list methods left 13 integration and 307 unit tests green. Not feature 40's defect — it guarded the products path and proved that guard falsifiable. Filed against phase 13 because the Gateway's list endpoints (id 25) are the ordering claim's first real consumer, and the file is open in this phase. The last acceptance bullet matters as much as the others: an unguarded .OrderBy that no requirement asks for is cheaper deleted than guarded."
    },
    {
      "id": 62,
      "name": "operator_cancel_races_saga_forward_progress",
      "phase": 14,
      "title": "An operator cancel reads the order status and enqueues compensation in one transaction while the saga advances the same order in another — nothing serialises the two",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "the race is reproduced by a test that FAILS on today's code — a released resource stranded on an order that kept progressing — before any fix is written",
        "the chosen defence is stated with its cost: whatever serialises the operator decision against the saga's own forward progress, and what it does to the fact-driven path's throughput",
        "R25's precondition-unmet check keeps ignoring facts whose precondition genuinely no longer holds — the fix must not turn a correct no-op into a retry storm",
        "armed: the fix is removed and the reproducing test fails again, with the verbatim message",
        "the operator-note lookup id 71 added (ISagaCommandStore.FindOperatorCancelNoteAsync, which picks the note by envelope CONTENT across the order's compensation rows) is exercised UNDER the race: when an operator cancel and the saga's forward progress interleave, the note that reaches order.cancelled.v1 is the operator's own, never absent and never taken from a saga-decided row — review advisory A3 of id 71 (progress/review_operator_note_survives_the_compensation_branches.md), unprobed there; armed by a mutation that makes the lookup select a row by position rather than by its operator-cancel envelope",
        "SA-4 (ruled at the human gate 2026-09-11; specs/shared/saga.md §4.3 'The despatch already requested' and 'A credit approval that arrives after the cancellation'): from credit_approved/confirmed the operator cancellation enqueues stock.release FIRST and replies compensationPlanned ['stock_release', 'credit_release']; stock.released.v1 at those statuses issues credit.release; credit.released.v1 at those statuses cancels with operator_cancelled. The old credit-first chain (CancelOrderCommandHandler.cs:128-134, SagaStepTable.cs:113-146, :191-197, :228-237) is replaced, and every test asserting the old order is updated after enumerating them by content",
        "the confirmed branch is proven in BOTH outcomes, with a stand-in Fulfillment that arbitrates stock.release against despatch.create for one order and RECORDS both: release wins → despatch.create is refused PRECONDITION_FAILED and resolved as a terminal rejection with no dead-letter and no order.saga_failed.v1, then credit.release is issued, then order.cancelled.v1 (operator_cancelled); despatch wins → stock.release releases nothing, NO credit.release is ever issued, order.despatched.v1 advances the order, and no order.cancelled.v1 is emitted",
        "the stock_reserved branch is proven in BOTH orderings of stock.released.v1 and a late credit.approved.v1 (the hold issued before the cancellation), with a stand-in Billing that RECORDS holds and releases: in each ordering exactly one credit.release is issued for the held credit, no order.confirmed.v1 and no despatch.create are issued, and the order ends cancelled (operator_cancelled) with the held credit released",
        "the first-pass supersede guard is removed (SagaFactHandler.cs:134-151, SagaIgnoredFactMarker.Superseded, ISagaCommandStore.HasPendingCompensationAsync and its test doubles), and the in-flight refusal recorded at progress/impl_operator_cancel_races_saga_forward_progress.md:356-367 is NOT built — refusing a confirmed cancellation contradicts openapi.yaml's 409 text. Any check for 'an operator cancellation already accepted' selects operator-cancel rows by their synthetic orders.cancel.requested envelope or reads the order's operator_cancelled reason, never any release row, because R27's credit_rejected path also enqueues stock.release; that selection is armed by substituting 'any stock.release row'",
        "no dispatch gate is added: under SA-4, Fulfillment's lock arbitrates the contested stock and the late-approval release unwinds any hold issued after the cancellation, so a gate would only save a wasted command. The order-row lock (EfCoreOrderRepository UPDLOCK, ROWLOCK, proven by OperatorCancelRowLockConcurrencyTests) stays, and the defence bullet 1 asks for is stated as SA-4's arbitration plus that lock, with the lock's cost"
      ],
      "notes": "Disclosed, not introduced, by feature 41 — and independently by #7's own feature 41, which reproduced it live and also declined to fix it in scope. Both assessments therefore ship it, so it is parity rather than a #8 regression. Mechanism: CancelOrderCommandHandler reads status and enqueues a compensating command in ONE transaction, while the saga's forward progress (credit.approved.v1 advancing stock_reserved -> credit_approved -> confirmed) runs in an INDEPENDENT transaction on an INDEPENDENT consumer. If the order advances past the observed status before the compensating fact lands, SagaStepTable.ForStatus's equality-only R25 check correctly ignores that fact and a genuinely released resource is stranded. Worse on the credit_approved/confirmed branch than on stock_reserved. Confirmed present in #8's own integration harness: OrdersCancelAcceptanceTests isolates it by never starting a despatch.create stand-in, the same technique #7's suite used. Filed against phase 14 because reliability and retry semantics are that phase's subject and a fix here without R29/DLQ context would be guessing. Existed only in prose in two implementation reports until this entry; the reviewer flagged that and deliberately did not file it, to keep the backlog single-writer."
    },
    {
      "id": 63,
      "name": "readiness_retry_loops_are_paced_by_a_timeout_that_never_elapses",
      "phase": 13,
      "title": "Four readiness waits retry a fixed number of attempts with no delay between them, and the error they retry on returns in about a microsecond — so 100 attempts can expire in ~1ms",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "each of the SIX readiness loops paces its retries explicitly rather than relying on the request timeout to consume wall-clock: BillingHostFixture.WaitUntilReachableAsync, FulfillmentHostFixture, OrdersCreateAcceptanceTests, StandInRpcResponder, CatalogReferenceListAcceptanceTests.cs:351 and StandInFulfillmentStockCheckResponder.cs:194",
        "the pacing is proved by a DETERMINISTIC test, not by observing that flakes stopped: delay the subscription by a controlled interval and show the unpaced loop loses every time and the paced loop wins every time",
        "armed: the pacing delay is removed and the deterministic test fails again, with the verbatim message",
        "if any of the SIX turns out not to need pacing, say which and why — a loop whose retried error genuinely costs wall-clock does not have this defect"
      ],
      "notes": "Found during feature 41's third fix round while arming the fix for a real flake in SagaIntegrationTestSupport. The insight generalises beyond these four files: a retry budget expressed in ATTEMPTS silently assumes each attempt costs time. NatsNoRespondersException is returned by the server almost instantly rather than by timing out, so a loop of 100 attempts paced only by its request timeout can exhaust the whole budget in about a millisecond and report 'not ready' while the subscription was 1ms away. Latent in all four: each is protected today by accident, because callers happen to warm up first. Filed against phase 13 because the Gateway (id 25) adds the next host fixture and will copy whichever shape it finds. WIDENED from four sites to six by feature 41's round-2 review: the original enumeration hand-excluded two hits on a READINESS ground orthogonal to the PACING claim being made — an enumeration filtered by the wrong predicate, which is the same class of error as post-filtering a search by content instead of path. Both halves matter — pace the loop, and prove the pacing with a change of KIND rather than of probability."
    },
    {
      "id": 64,
      "name": "orders_wire_key_theory_compares_a_hand_typed_list_to_itself",
      "phase": 13,
      "title": "Orders' BC23 wire-key theory compares a hand-typed key list against the spec and never against the record, so a required field missing from the C# type passes",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "Orders' BC23 theory derives its key set from the payload RECORD by reflection, the way tests/Billing.UnitTests/CreditRpcPayloadTests.cs:29-35 already does, rather than from a hand-typed list",
        "armed: a required field is removed from a payload record and a named test FAILS — the exact mutation that left Orders.UnitTests 342/342 green when the review ran it",
        "every payload record the theory is meant to cover is enumerated as a search result, with one classification line per hit, so a record with no case is visible as an unclassified line rather than absent",
        "if Billing's form does not transfer cleanly, say what differs rather than reproducing the hand-typed shape"
      ],
      "notes": "Found by feature 41's round-2 review as finding B1, and the reviewer noted its own round-1 prescription had shaped the narrow fix that exposed it. The guard cannot fail on the defect it was added for: removing availableCreditAfter from the record again (call sites adjusted) left Orders.UnitTests 342/342 green. Not a code defect — the payloads are correct and nobody claimed a guard they lack. The pattern is this build's most-repeated one: a test that compares a hand-maintained restatement of a contract against the contract, never against the thing that implements it, so both sides can agree while the code diverges. Billing already has the reflection-over-the-record form; this is a port, not a design."
    },
    {
      "id": 65,
      "name": "gateway_order_detail_totals_transposition_survives",
      "phase": 13,
      "title": "OrderReadModelMapper.ToOrderDetail transposes InitialAmount and InitialDiscount with the whole suite green — the sibling method of a defect already fixed",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "transposing InitialAmount and InitialDiscount in OrderReadModelMapper.ToOrderDetail makes a named test fail",
        "the detail fixture carries three DISTINCT non-zero values so a transposition cannot pass on equal values, and each of initialAmount, initialDiscount and totalAmount is asserted",
        "both mapper methods are covered — the list path was fixed as review defect D5 and the detail path is this entry; say so explicitly rather than fixing one and assuming the other",
        "armed: the report names the transposition and the verbatim failure"
      ],
      "notes": "Found as finding E1 by feature 25's round-2 review, which also recorded that its own round-1 prescription had scoped D5 to the list method at OrderReadModelMapper.cs:76 and thereby missed the sibling at :84 — the reviewer's under-scoping, disclosed as such, not an implementer shortfall. The fixture already carries (124950, 700, 124250), so the remedy is three assertions. The general shape is one this build keeps meeting: a defect fixed at the site where it was FOUND rather than across the class it belongs to, so the sibling survives with the suite green. When a review prescribes a fix, the prescription should name the class, not the line."
    },
    {
      "id": 66,
      "name": "operator_note_reaches_the_timeline",
      "phase": 13,
      "title": "Carry the operator's cancellation note from the RPC request through order.cancelled.v1 to the read-model timeline entry — the acceptance bullet SA-2 makes satisfiable",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "POST /orders/{id}/cancel with a note results in that note on the order's read-model timeline entry, proved end to end through the real Gateway, Orders, Kafka and Projector",
        "the note is optional everywhere it appears: a saga-decided cancellation (stock_rejected, credit_rejected) carries none and the timeline entry is still correct",
        "the Contracts record, the Orders emission and the Projector projection all move together, and a replay of an OLD envelope without the field still projects",
        "armed in BOTH directions: deleting the note from the emission fails a named test, and CORRUPTING it on the wire fails a named test — the second is the one that matters, since the first passes on any string"
      ],
      "notes": "This is feature 41's acceptance bullet 4, unmet in BOTH assessments and disclosed by both. It was unsatisfiable until SA-2, because openapi.yaml promised the note reached the timeline while asyncapi.yaml's OrderCancelledPayload had no field to carry it. #7's reviewer ruled that the next feature touching OrderCancelledPayload must close it; #8's id 41 was that feature and disclosed it a second time rather than closing it, because specs/shared/ is read-only without a human-gated amendment. SA-2 is that amendment. Filed against phase 13 so the bullet is closed in the same phase that re-disclosed it rather than routed around again — a defect both runs disclose and neither fixes is one the process routes around, and this entry exists to stop that happening a third time in #9."
    },
    {
      "id": 67,
      "name": "design_time_dbcontext_factory_env_reads_are_unguarded",
      "phase": 14,
      "title": "Twenty environment-variable reads in the four EF Core DbContextFactory classes are live tooling inputs with no guard — excluded from id 56 by a ruling, not by absence",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "deleting any one of the 20 reads in OrdersDbContextFactory, FulfillmentDbContextFactory, BillingDbContextFactory or NotificationsDbContextFactory makes a named test fail",
        "the guard drives the factory the way dotnet ef does — through IDesignTimeDbContextFactory<T>.CreateDbContext — rather than calling a helper the factory happens to share",
        "the variable NAME the factory chooses is guarded, not only that some variable is read: a factory repointed at another service's database must fail a named test",
        "armed: each deleted read and each repointed name is named with its verbatim failure"
      ],
      "notes": "Ruled OUT of scope for id 56 by the implementer and upheld by the review, on a reachability claim verified as a search result: all four classes have zero call sites and are reached only by dotnet ef's reflection-based discovery. That makes the exclusion correct AND makes these reads live — dotnet ef is tooling this team runs to generate migrations, so a wrong or missing read here produces a migration against the wrong database. An exclusion is a decision, not an absence, and this entry is the artefact that decision leaves behind. Bullet 3 exists because id 56's D1 showed the shape: when a test supplies the variable NAME, it guards the reading mechanism and never the caller's choice of key."
    },
    {
      "id": 68,
      "name": "composition_root_delegation_and_wiring_are_unguarded",
      "phase": 14,
      "title": "Nothing drives Program.cs or the wiring between a read and its use — a service can read the right variable and hand it to the wrong dependency with the suite green",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "the population is RE-DERIVED by this loop's own enumeration before any test is written, as a search result with bin and obj excluded by path, and every delegating call site it finds is covered: replacing any one with a no-op makes a named test fail. The filed figure of 'ten delegating call sites — the six Program.cs -> ProgramConfiguration.Configure calls' DOES NOT RECONCILE against the tree and must not be inherited: checked by the leader on 2026-09-12, there are seven Program.cs files, and per-file counts of a 'configure*:' argument are Billing 3, Gateway 1, Fulfillment 3, Projector 3, Notifications 3, Orders 5, Seed 0. Gateway's count of 1 is an artefact of counting LINES — src/Gateway/Program.cs:9 packs TWO arguments, configure: and configureTelemetry:, onto one line (CORRECTED 2026-09-12 by the leader, who first wrote 'three arguments including configureHealth:' here: that was wrong and was inferred from a grep output truncated at 150 characters rather than read. GatewayHost.Build at src/Gateway/GatewayHost.cs:150 declares only Build(string[] args, Action<GatewayOptions> configure, Action<TelemetryOptions>? configureTelemetry = null) — there is no configureHealth parameter to pass, so the Gateway total is 2 and the population is 19, not 20. Found by Slice A's review as D4; the implementer had it right and the entry was wrong) — and ORDERS HAS NO PLAIN configure: AT ALL, passing configureOutbox, configureAcceptance, configureSaga, configureTelemetry and configureHealth at :18-22. So 'six Configure calls' is wrong on both halves and the counting rule that produced 'ten' cannot be reconstructed. Per CLAUDE.md a number that does not reconcile is a finding: state the true population, by name, and say what the unit is (a delegating ARGUMENT, not a line)",
        "substituting one service's connection string for another's at src/Seed/Presentation/SeedRunner.cs:26 makes a named test fail — the measured case: it currently leaves Seed.UnitTests 44/44 green while Orders fixtures go into the Billing database. NOTE the path: the entry originally said 'SeedRunner.cs:26', and the file is at src/Seed/Presentation/SeedRunner.cs, not src/Seed/SeedRunner.cs (leader, 2026-09-12). The line is right — :26 is 'await using var ordersDb = OrdersSeedWriter.OpenDb(ordersConnectionString);', with the three connection strings taken at :21-23 — so the measurement stands and only the path was wrong",
        "the mechanism is chosen once and inherited, as id 56's was, rather than one bespoke test per call site",
        "armed: each substitution and each no-op is named with its verbatim failure, and the failure message is checked to name the intended reason rather than an incidental one"
      ],
      "notes": "Routed from feature 56's review rather than discharged in prose: D2 disclosed the residual honestly and R2-5 then MEASURED a consequence of it. Two independent measurements are the acceptance: P17 (a no-op configure lambda leaves Billing 232/232 green) and R2-5 (a substituted connection string leaves Seed.UnitTests 44/44 green and produces id 56's D1 consequence by another route). Seed.IntegrationTests builds its own connection strings and never calls SeedRunner.RunAsync, so it cannot see it either. Id 56 guarded WHICH VARIABLE each writer reads; this entry guards WHERE THE VALUE GOES. Filed against phase 14 because the reliability phase is where host wiring gets its own tests, and because a mechanism chosen here should be the one later services inherit."
    },
    {
      "id": 69,
      "name": "gateway_readiness_pacing_is_unguarded",
      "phase": 14,
      "title": "The two Gateway readiness loops were paced during the guard-hardening loop but nothing proves the pacing — deleting both Task.Delay calls leaves 48/48 green",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "deleting the pacing delay in tests/Gateway.IntegrationTests/StandInResponder.cs and in FulfillmentStockEndToEndTests.cs each makes a named test fail",
        "the proof is a DETERMINISTIC test in the shape the other six sites already use — delay the subscription by a controlled interval, show the unpaced loop loses every time and the paced loop wins every time — not an observation that flakes stopped",
        "the failure message names the pacing as the reason, not an incidental timeout: a swap or deletion that fails for the wrong reason is not evidence",
        "armed: each deleted delay is named with its verbatim failure",
        "WIDENED by the leader at the SA-3 commit (2026-09-11), same class — a retry budget counted in attempts against an error that returns without consuming wall-clock: the four committed-offset read helpers retry consumer.Committed a fixed 5 attempts with a 300 ms sleep and catch EVERY KafkaException — tests/Orders.IntegrationTests/SagaIntegrationTestSupport.cs:425-436 (whose own comment at :418-423 names the 'Broker: Not coordinator' race), tests/Notifications.IntegrationTests/NotificationDeadLetterTests.cs:419-430, tests/Projector.IntegrationTests/ProjectorDeadLetterTests.cs:329-340 and tests/Projector.IntegrationTests/OffsetContractTests.cs:54-65. That is ≈1.2 s of sleeps against a brand-new single-node broker whose coordinator can take longer to load. OffsetContractTests.PR38_AThrowingHandlerLeavesTheCommittedOffsetUnchanged_ReadFromTheBroker went red on exactly this in the SA-3 verification run (KafkaException 'Broker: Not coordinator' from OffsetContractTests.cs:58; 1 failed of 1833, where the 13:34 run on the same tree minus SA-3's five YAML description lines was green at 1833 with identical per-project counts). Enumerate first (content search for '.Committed(' under tests/, bin/obj excluded by path); then pace each helper against a wall-clock deadline — #7's shape is apps/projector/src/test-support/kafka-test-fixture.ts:93-126 (waitForConsumerGroupReady: 60 000 ms deadline, 300 ms interval, a loading coordinator treated as not-ready) — or share one helper; retry only on the coordinator error codes rather than every KafkaException, so a genuine failure is not retried and then reported as the last attempt's exception; and prove it by a change of KIND (a coordinator withheld for a controlled interval longer than the old budget: unpaced loses every time, paced wins every time), or state why that proof cannot be constructed here and what evidence replaces it"
      ],
      "notes": "RE-OPENED 2026-09-13 by maintainer ruling: the 2026-09-13 dispositioning below is SUPERSEDED. The maintainer rejected accepting these and directed that phase 14 be finished and correct, not closed by disposition. Status is therefore `pending` again and the ACCEPTED, NOT FIXED note that follows is retained as history, not as the current disposition. || SUPERSEDED DISPOSITION FOLLOWS || DISPOSITIONED 2026-09-13 — ACCEPTED, NOT FIXED, at the human gate. Phase 14 was closed to scope after the audit filed more entries than it closed; the maintainer ruled that entries which do not change shipped behaviour are closed with documented evidence rather than worked. Status is `done` because the backlog has no `accepted` status — it means DISPOSITIONED, not implemented. Reason: the two Gateway readiness loops ARE paced and correct by reading (StandInResponder.cs:157 and FulfillmentStockEndToEndTests.cs:89, Task.Delay(50ms) inside a 100-attempt loop, each with the reasoning in a comment). What is missing is a guard PROVING the pacing, which is a test-only property; the code it protects is already right. Re-open if a readiness loop is added without pacing. || ORIGINAL ENTRY FOLLOWS || Found as probe P10 by the review of the guard-hardening loop. These are the 7th and 8th unpaced readiness sites — beyond the six id 63 names — discovered by the implementer during id 63's enumeration, paced, and honestly flagged as not fully proven rather than folded in as done. The reviewer confirmed the gap: grep -ln for the deterministic-race test name returns three files, none of them in Gateway, and deleting both Task.Delay calls leaves Gateway.IntegrationTests 48/48 green. In scope for the class, not proved, and therefore routed here rather than discharged in prose. Worth noting for the benchmark: id 63's population grew four -> six -> eight, each step because somebody enumerated instead of assuming, which is the entry's own argument made three times."
    },
    {
      "id": 70,
      "name": "retyped_key_list_guards_survive_in_three_more_payload_test_files",
      "phase": 14,
      "title": "The hand-typed key list id 64 retired is alive in three more payload test files under the same test name — an undeclared property leaves both suites fully green",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "each of tests/Orders.UnitTests/OrdersCancelPayloadTests.cs, tests/Orders.UnitTests/CatalogReferenceListPayloadTests.cs and tests/Fulfillment.UnitTests/StockRpcPayloadTests.cs derives its key set from the payload RECORD by reflection, as tests/Billing.UnitTests/CreditRpcPayloadTests.cs:29-35 and now Orders' BC23 theory do. LINE NUMBERS IN THE ORIGINAL FILING ARE STALE — re-locate by content, do not trust them: checked by the leader 2026-09-12, OrdersCancelPayloadTests' InlineData rows are at :64-65 with the hand-retyped list at :92 (filed as :66), CatalogReferenceListPayloadTests' at :96-100 with its retyped list at :125 (filed as :101), and StockRpcPayloadTests' at :151-156 (filed as :162)",
        "armed on the measured mutations: an undeclared property on OrdersCancelReplyPayload currently leaves Orders.UnitTests 362/362 green, and the same on DespatchCreateReplyPayload leaves Fulfillment.UnitTests 124/124 green — both must fail a named test. WARNING, the ground moved after this was measured: feature 76 (closed 2026-09-12) relocated these types. DespatchCreateReplyPayload and StockReserveReplyPayload now live in src/Contracts/Rpc/, while OrdersCancelReplyPayload and CatalogReferenceListReplyPayload each have TWO definitions — src/Gateway/Application/Rpc/GatewayRpcPayloads.cs plus the owning service's own src/<Service>/Presentation/Rpc/ copy. The arming must therefore NAME WHICH definition it mutates, or the mutation may never reach the type the test parses, and the measured baselines above predate the move and must be re-measured before being relied on. This overlaps backlog id 84 (the Gateway's third copy of the now-canonical RPC payloads): sequence the two deliberately rather than running them blind into each other",
        "the schema NAME each row claims is guarded too, not only the key set: substituting a real sibling schema name must fail a named test naming what was broken",
        "the enumeration is repository-wide and is a search result: every payload-key theory in the solution is listed with one classification line each, so a file with this shape and no entry here is visible as an unclassified line"
      ],
      "notes": "RE-OPENED 2026-09-13 by maintainer ruling: the 2026-09-13 dispositioning below is SUPERSEDED. The maintainer rejected accepting these and directed that phase 14 be finished and correct, not closed by disposition. Status is therefore `pending` again and the ACCEPTED, NOT FIXED note that follows is retained as history, not as the current disposition. || SUPERSEDED DISPOSITION FOLLOWS || DISPOSITIONED 2026-09-13 — ACCEPTED, NOT FIXED, at the human gate. Phase 14 was closed to scope after the audit filed more entries than it closed; the maintainer ruled that entries which do not change shipped behaviour are closed with documented evidence rather than worked. Status is `done` because the backlog has no `accepted` status — it means DISPOSITIONED, not implemented. Reason: a hand-typed key list is a WEAKER guard, not a wrong one, and the wire itself is guarded elsewhere — the golden-envelope parity tests and the AsyncAPI schema-parsed theories both assert the real contract. Note that id 76 moved DespatchCreateReplyPayload and StockReserveReplyPayload into src/Contracts/Rpc, so any future work here must name WHICH definition it mutates; it overlaps id 84. || ORIGINAL ENTRY FOLLOWS || Found as F1 by round 2 of the guard-hardening loop's review, measured rather than argued. Outside id 64's acceptance and outside the scope round 1 set, so routed as an entry rather than folded in or narrated. The reviewer also added a probe the loop had not run — substituting the row's literal schema name for a real sibling — which is why bullet 3 exists: a key-set guard can be correct and still not guard which schema the row claims. Bullet 4 carries the condition the same review drew for #9: when work targets a defect CLASS, the first task is one repository-wide enumeration of that class, before any fix — id 64 fixed the instance it was filed for and the class survived in three more files."
    },
    {
      "id": 71,
      "name": "operator_note_survives_the_compensation_branches",
      "phase": 14,
      "title": "Carry the operator's cancellation note across the async saga boundary so stock_reserved, credit_approved and confirmed cancellations reach the timeline with it — the unqualified half of id 66's bullet 1",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "POST /orders/{id}/cancel with a note against an order in stock_reserved lands that note on the read-model timeline entry after the real compensation chain completes, proved end to end",
        "the same for credit_approved and confirmed",
        "a saga-decided cancellation (stock_rejected, credit_rejected) still carries no note and no note key on the wire",
        "armed in all three families: deleting the retention, corrupting the retained text, and substituting the persisted column or field for a real sibling — the substitution must fail with a message naming what was broken",
        "while the saga_commands row is open, store the triggering fact's BYTES and not only its id — R29's dead-letter clause (test-matrix.md:127, feature 27) cannot republish an envelope verbatim without them, and one column serves both needs",
        "parity with #7 on the RPC-triggered path, which feature 27's A2 left null: both operator-cancel compensation enqueues (src/Orders/Application/Commands/CancelOrderCommandHandler.cs:167 credit.release, :194 stock.release) store a synthetic orders.cancel.requested envelope — eventId and causationId the request id, aggregateId and correlationId the order id, payload {orderId, reason: operator_cancelled, note when present} — and the orders facts topic, exactly as #7's apps/orders/src/application/cancel-order.handler.ts:175 and :234 do through buildTriggeringEnvelope (:272); #7's port makes that envelope non-nullable (apps/orders/src/application/ports/saga-command-store.port.ts:40, :51), which is the property #8's nullable port silently dropped",
        "the divergence's guards are ported, not only its code: CancelOrderCommandHandlerTests.cs:146-148's Assert.Null on envelope and topic flips to #7's assertions (cancel-order.handler.spec.ts:178 and :220 — eventType orders.cancel.requested); the 'design.md §4.2' attributions at CancelOrderCommandHandler.cs:167/:194 and the test's :144-145 are corrected, since §4.2 (design.md:191-193) speaks only of fact-triggered threading; a named integration test proves a parked operator-cancel compensation command publishes a .dlq copy byte-equal to the stored envelope, armed by restoring the null; and progress/impl_operator_note_survives_the_compensation_branches.md carries the ledger row for it"
      ],
      "notes": "NO SA-3 IS REQUIRED, and that is the part got wrong twice — by the implementer AND by the leader's own brief, both of which placed the root cause in specs/shared/. It is not there: grep -rn 'saga_commands|saga command' specs/shared/*.md returns five hits (saga.md:31, requirements.md:564, domain-model.md:275 and :475, test-matrix.md:127), all prose about saga progression and DLQ policy, NONE describing a column. The table is implementation-owned and per-assessment. #7 already solves this without a wire change: apps/orders/src/application/cancel-order.handler.ts:272-283 puts the note into the saga command's triggering envelope on both compensation branches (:175, :234), persisted at saga-commands.schema.ts:61. The #8 shape is an Orders-local column plus SagaFactHandler.cs:187 (was :167 when filed; feature 27's A2 added lines above it) feeding the note: parameter feature 66 already added to Order.Cancel — wholly inside src/Orders/. Bullet 5 folds in review finding F3: grep -rn TriggeringEvent src/Orders returns seven hits, all the ID, so #8 stores the triggering fact's bytes nowhere."
    },
    {
      "id": 72,
      "name": "test_matrix_rows_that_outlived_their_named_closer",
      "phase": 14,
      "title": "Close the test-matrix rows whose deferral named 'the gateway feature' after that feature shipped without closing them, and give the coverage summary the standing paragraph its own legend points to",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "R61's API half is cited as tests/Gateway.IntegrationTests/FulfillmentStockEndToEndTests.cs › ReplenishStock_ThroughTheRealGatewayPipeline_ActuallyAddsUnitsInFulfillmentsRealDatabase only AFTER that assertion is armed: corrupting the replenished quantity on the Gateway's side, and substituting the RPC subject for a real sibling, each fail the named test with a message naming what was broken; then the cell flips to DONE",
        "R1's API half exists: a Gateway integration test ported from #7's apps/gateway/src/money-representation.integration.spec.ts — a sweep that DISCOVERS money-shaped fields in every money-bearing Gateway response rather than listing expected field names — armed by making one real response carry a money amount without its currency and by emitting a non-integer amount, each failing with a message naming the endpoint and the field; #7's test and its test-support/money-field-sweep.ts are enumerated assertion by assertion and each classified ported / not ported (reason) / not applicable",
        "R24's cell names feature 31 api_tests as its closer instead of 'the gateway feature', and id 31's acceptance carries the R24 bullet (already added by the leader) — a deferral that names someone",
        "test-matrix.md carries the paragraph under the coverage summary that its legend already points to: every Scoped row by id, who ratified it and where, and what closing it would take — the shape of #7's own paragraph",
        "the coverage summary is recounted row by row from the Status column after these flips, the enumeration (one classification line per R-row) recorded in progress/impl_test_matrix_rows_that_outlived_their_named_closer.md, and every section row plus Total reconciles against it",
        "the defect CLASS is enumerated repository-wide BEFORE any cell is edited, as a search result — the command, its complete output, one classification line per hit — over all 63 Status cells as the population, on the wording of the claim being RETIRED rather than the one being written, in both its shapes: (a) a deferral naming a closer that has since shipped ('does not exist yet', 'no Gateway', 'the gateway feature'), and (b) a cell stating a shortfall against the requirement's own wording while still counted Green, which the legend at test-matrix.md:66-67 defines as Scoped. R48 and R49 are known instances of both: their premise is false (the endpoint ships at src/Gateway/Presentation/Endpoints/InvoicesEndpoints.cs:32, feature 25 done) and their stated one-layer-down shortfall makes them Scoped, not Green. Every count that moves is re-derived row by row"
      ],
      "notes": "Found by the leader during feature 27's Group N pre-checks, 2026-09-10. Three rows (R1, R24, R61) said their API half was 'closed by the gateway feature' because 'no Gateway/API surface exists yet'; id 25 gateway_rest_auth shipped in phase 13 and closed none of them, because no bullet of its acceptance named them — CLAUDE.md's 'the next feature that touches X names no one' failure, inside the traceability file itself. Resolved against #7 before filing: #7 closed R61's API half in its own gateway feature (billing-fulfillment.integration.spec.ts › 'R61 — POST /stock/replenish translates to fulfillment.stock.replenish'), R24's in api_tests (d19342c, tightened by amendment A1), and R1's only in its Phase 25 traceability closure (8a3a3d3), which #8's backlog has no counterpart for. The same pass found the coverage summary stale in §2 and §3 (R16 and R29 flipped by feature 27, counts unmoved) — that recount belongs to feature 27's Group N, since its own flips moved it; this entry recounts again after its own. #7 recorded the identical defect in the same file ('Why the numbers moved': it once claimed 47 green because it had never been recomputed as rows were filled), and init.sh §5d exempts test-matrix.md from parity by design, so nothing mechanical sees the arithmetic. Deliberately NOT a new init.sh guard: classifying a free-text Status cell into Green/Scoped/Not-yet-green is judgement, and a checker that only verified green+scoped+notyet=rows would have passed this exact drift (7+0+1=8)."
    },
    {
      "id": 73,
      "name": "notification_send_degrades_on_permanent_failure",
      "phase": 14,
      "title": "Port #7's degrading notification sender — a permanent send failure renders to the console and acknowledges instead of retrying into the DLQ — a #7 fix made outside its backlog, so #8's copied backlog never carried it",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "a DegradingNotificationSender decorator wraps MailKitNotificationSender with ConsoleNotificationSender as its fallback, wired only when SMTP is configured (a console-only binding stays unwrapped); on a TRANSIENT failure it rethrows the original exception unchanged, so FactRetryDispatcher's retry-then-DLQ path behaves exactly as it does today",
        "on a PERMANENT failure it renders the same message through the console fallback, logs one structured error line carrying a stable event field plus correlationId and traceId, and returns normally — no retry, no .dlq copy, the idempotency ledger row stays committed and the offset commits",
        "the classifier is rebuilt on MailKit's own signals, not transliterated from nodemailer's: #7 (apps/notifications/src/infrastructure/notification/send-failure-classifier.ts) treats an SMTP responseCode 5xx and code EAUTH as permanent and everything else, unknown included, as transient; #8 names the MailKit equivalents it relies on and proves each against a real SMTP failure (a real Mailpit or equivalent), never a hand-constructed exception of its own choosing — and progress/impl_notification_send_degrades_on_permanent_failure.md carries the ledger row stating which MailKit signal supplies the property #7 got from nodemailer's error fields",
        "#7's tests are enumerated assertion by assertion and classified ported / not ported (reason) / not applicable: send-failure-classifier.spec.ts and degrading-notification-sender.spec.ts, the latter in three layers (in isolation, composed with the real FactRetryDispatcher, composed with the real NotificationDispatchService)",
        "armed in all three families: deleting the transient rethrow (so every failure silently degrades) fails a named test; corrupting one permanent status to transient fails a named test; and replacing the permanent branch's normal return with a rethrow fails a test asserting that no .dlq publication happened"
      ],
      "notes": "Found by #8's feature-27 review, round 1 (2026-09-10), and confirmed by the leader against #7's checkout. #7 commit ad90de6 ('fix(notifications): degrade to console on a permanent send failure', 2026-08-30) built this as an operational hardening task, explicitly 'Not a feature_list.json entry' (order-to-cash-nestjs/progress/impl_notification_degradation.md:3). #8's backlog was copied from #7's, so a fix #7 never wrote down as backlog work never became #8 work — a new shape of the routing failure CLAUDE.md names: not a deferral that named no one, but a fix that was never an entry. #8's src/Notifications/Infrastructure/Notification carries only ConsoleNotificationSender and MailKitNotificationSender. #7's decorator semantics, read from apps/notifications/src/infrastructure/notification/degrading-notification-sender.ts:1-33: transient rethrows the original error unchanged; permanent renders via the fallback, logs one loud structured line and returns normally so no caller retries or dead-letters. The live incident in #7 was an SMTP provider's quota exhaustion; the classifier's signal is the part that cannot be ported literally, because #7 read nodemailer's responseCode/code fields, which MailKit does not have."
    },
    {
      "id": 74,
      "name": "dead_letter_tests_select_their_message_by_content",
      "phase": 14,
      "title": "Dead-letter integration tests take the FIRST message on a shared .dlq topic rather than the one they produced — a positional read that let two test classes collide and can assert against another test's message",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "the class is enumerated repository-wide FIRST, as a search result: every test that reads a Kafka topic positionally (ConsumeOneAsync and any equivalent helper, found by content, bin/obj excluded by path), one classification line per call site — the three dead-letter tests named below are where it was noticed, not where it ends",
        "each positional read selects its message by content the test controls — the eventId or correlationId it produced — instead of taking the first message on the topic",
        "armed by change of kind, per site: publish a decoy message with a different eventId to the same topic before the real one; the test must still assert the real message, and the pre-fix positional version must be shown to assert the decoy (or fail for the decoy's reason) against the same mutation",
        "joins the guard-hardening loop with ids 67–70, whose first task is already one repository-wide enumeration of a defect class",
        "the sibling exclusivity class feature 27's review round 3 named (advisory A13, progress/review_observability_reliability.md from :1580) is enumerated and closed in the same loop: every Assert.Single (or equivalent exactly-one assertion) over a SHARED capture — a MeterListener/ActivityListener collection, a shared topic, a process-wide sink — that other concurrently-running tests can also write to; the review counted nine such sites beyond the one feature 27 fixed. Each site either scopes its capture to what the test itself produced (by tag, trace id, correlationId or instrument instance) or proves exclusivity, armed by a concurrent writer that emits a matching-shaped measurement during the test",
        "the consumer-group clearance feature 27 added (StopHostAndWaitForGroupToClearAsync, applied at 12 Notifications, 25 Orders and 4 Projector teardown sites) is made STRUCTURAL rather than per-site — review round 4's advisory A16: today a future bare host.StopAsync() in any of those projects silently reintroduces the shared-consumer-group race, so the clearance belongs where a host is built or disposed (a test-host wrapper or fixture), with a guard that fails when a Kafka-group host is torn down without it; and because the helper currently throws from a finally block, a test that fails AND whose teardown also throws must still report its OWN failure, armed by making both happen in one test and asserting the original assertion message survives. COUNTS CORRECTED BY THE LEADER 2026-09-12 — the filed figure of '12 Notifications, 25 Orders and 4 Projector teardown sites' (41) does not reconcile against the tree: the helper has THREE definitions (tests/Orders.IntegrationTests/SagaIntegrationTestSupport.cs:203, tests/Notifications.IntegrationTests/NotificationConsumptionTestSupport.cs:225, tests/Projector.IntegrationTests/TestSupport/ProjectorTestHost.cs:61) and 51 call sites excluding those definitions. Re-derive the population before writing anything and state the UNIT (a call site, not a file). Note also that a bare .StopAsync( appears 128 times across tests/, and that is NOT a population of escapes — most are hosts with no Kafka consumer group at all, so the guard's scope must be decided by which hosts actually join a group, determined by content. The three-project scope WAS checked and is correct: the only group ids outside those projects are at tests/Billing.IntegrationTests/BillingOutboxRelayTests.cs:75 and tests/Fulfillment.IntegrationTests/FulfillmentOutboxRelayTests.cs:73, and both are test-side verification consumers built with GroupId = test-{Guid.NewGuid():N} — a fresh unique group per run, which by construction cannot join the shared-group race and needs no clearance. Recorded so the next reader does not re-open a closed question; re-verify only if outbox-relay tests stop using a per-run group id",
        "the test-harness delivery of test.runsettings is guarded against a PER-PROJECT escape (review advisory A1 of id 77, progress/review_test_hosts_exhaust_the_per_user_inotify_limit.md): today all 18 test projects inherit RunSettingsFilePath from the root Directory.Build.props and none override it, so HostInotifyReloadGuardTests in Orders.UnitTests alone is sufficient — but a future project that sets its own RunSettingsFilePath, sets ImportDirectoryBuildProps=false, or is run with its own --settings would lose the reload-on-change setting invisibly, because the one guard lives in a different project; add a structural check over a LITERAL list of the test projects (never discovered by a predicate a violating project could escape) that fails naming the project, armed by adding each escape shape to one project in turn"
      ],
      "notes": "RE-OPENED 2026-09-13 by maintainer ruling: the 2026-09-13 dispositioning below is SUPERSEDED. The maintainer rejected accepting these and directed that phase 14 be finished and correct, not closed by disposition. Status is therefore `pending` again and the ACCEPTED, NOT FIXED note that follows is retained as history, not as the current disposition. || SUPERSEDED DISPOSITION FOLLOWS || DISPOSITIONED 2026-09-13 — ACCEPTED, NOT FIXED, at the human gate. Phase 14 was closed to scope after the audit filed more entries than it closed; the maintainer ruled that entries which do not change shipped behaviour are closed with documented evidence rather than worked. Status is `done` because the backlog has no `accepted` status — it means DISPOSITIONED, not implemented. Reason: positional .dlq reads are a test-cross-talk risk, never a production one. The one real collision this caused was already resolved in feature 27 by moving the new tests to other topics. Two class-level ConsumeOneAsync(topic, timeout) helpers remain positional (NotificationDeadLetterTests.cs:327, ProjectorDeadLetterTests.cs:237) against Orders’ correlation-matching one; both are documented in place. || ORIGINAL ENTRY FOLLOWS || Advisory A8 of feature 27's review, round 2 (progress/review_observability_reliability.md, Round 2 section). The positional ConsumeOneAsync is what caused the cross-test .dlq collision feature 27's review-round-1 fix round found and worked around by moving the new log-correlation tests to other topics (ProjectorFactTopics.FulfillmentFacts, NotificationFactTopics.BillingFacts) — the collision was avoided, the positional read that produced it was left in place. Sites named by the review: tests/Orders.IntegrationTests/SagaDeadLetterTests.cs, tests/Notifications.IntegrationTests/NotificationDeadLetterTests.cs, tests/Projector.IntegrationTests/ProjectorDeadLetterTests.cs (all reading OrdersFacts' .dlq). Filed as an entry rather than folded into feature 27 because it is a test-shape CLASS whose population is not yet enumerated, which is exactly the loop's remit; it stays in phase 14."
    },
    {
      "id": 75,
      "name": "dead_letter_first_failed_at_semantics",
      "phase": 14,
      "title": "#7 renders the dispatch ENTRY instant into x-first-failed-at at three sites, which SA-3's wording now explicitly forbids — #8 conforms, #7 does not, and two of #7's three copies have no spec asserting the header at all",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "NO AMENDMENT IS OWED AND NO GATE IS HELD — CORRECTED BY THE LEADER 2026-09-13, and this bullet previously asked the human to rule on an amendment that had ALREADY BEEN MADE. specs/shared/asyncapi.yaml:2227-2229 now reads: 'x-first-failed-at is the instant the FIRST processing attempt failed — never the instant processing began; it equals x-failed-at only when a single attempt was made. x-failed-at is the instant the final attempt failed.' That wording was applied by SA-3 on 2026-09-11, in the same session this entry was filed, which is why the entry was written as though the contract were still silent. The properties at :2247 and :2249 remain bare $ref: Instant, but the meaning is defined in the schema's own prose immediately above them, so the contract is NOT silent. The entry is therefore ordinary cross-repository alignment work of the same shape as id 79 (#7's SA-4 code alignment), not a human-gated spec amendment",
        "recommended wording: x-first-failed-at is the instant the FIRST processing attempt failed; x-failed-at is the instant the final attempt failed — this matches the header's own name, matches #8's current behaviour (src/*/Infrastructure/Messaging/FactRetryDispatcher.cs `firstFailedAt ??= clock.UtcNow` inside the catch), and costs #7 a change at THREE sites, not one (corrected by the leader at the SA-3 commit, 2026-09-11, after reading all three copies): apps/orders/src/infrastructure/messaging/fact-retry-dispatcher.ts:135-136 (`const firstFailedAt = enteredAt;`), apps/projector/src/infrastructure/messaging/fact-retry-dispatcher.ts:126 and apps/notifications/src/infrastructure/messaging/fact-retry-dispatcher.ts:123 (`const firstFailedAt = this.clock.now();`, both before the retry loop) — plus the orders spec's expectation (apps/orders/…/fact-retry-dispatcher.spec.ts:89); the projector and notifications copies have NO spec asserting the header, so #7's guard for this must be written there, not only updated",
        "whichever meaning is ruled, both repositories assert it with a test whose clock advances between dispatch entry and the first failure, so the entry instant and the first-failure instant are DIFFERENT values and the test can tell them apart — #8's DeadLetterPublication_FirstFailedAt_IsTheFirstAttemptsClockReading_NeverALaterOne and #7's spec both pass today only because their clocks make the distinction invisible or were never asked",
        "armed in both repositories by rendering the other meaning's instant into the header"
      ],
      "notes": "Found by the leader on 2026-09-11 while reading #7's fact-retry-dispatcher.ts at HEAD bf45af0 for feature 27's L29 ledger row. #7: firstFailedAt = enteredAt (:136), rendered by kafka-dlq-publisher.ts:49, asserted as the entry instant at fact-retry-dispatcher.spec.ts:89. #8: the instant of the first caught failure, added under test by feature 27's review-round-2 fixes. specs/shared/asyncapi.yaml:2240-2243 declares both headers as bare `$ref: Instant` with NO description, while every neighbouring header (x-attempts, x-error, x-original-topic) has one; the only other occurrence (:1578-1579) is an example payload, which is not normative. Routed per CLAUDE.md's rule that a gap whose root cause is specs/shared/ becomes a numbered backlog entry, never a sentence in a record. Not a feature-27 blocker: the contract is silent, so neither assessment violates it; what is lost is parity, and a redrive operator reading the header would get different instants from #7 and #8 for the same failure. The amendment itself is human-gated; this entry carries a recommendation, not a menu."
    },
    {
      "id": 76,
      "name": "application_layer_depends_on_infrastructure_unguarded",
      "phase": 14,
      "title": "Application-layer code imports Infrastructure namespaces across Billing, Fulfillment and Orders, against CLAUDE.md's inward-dependency rule — and no architecture test forbids it, because NetArchTest only enforces domain purity",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "the class is enumerated FIRST, as a search result: every file under any src/*/Application/ that references an OrderToCash.<Service>.Infrastructure namespace (using directives AND fully-qualified names, excluding cref-only doc comments, bin/obj excluded by path), one classification line each — the leader's 2026-09-11 sweep found Billing 10 and Fulfillment 10 files (all Infrastructure.Messaging.Rpc), Orders ISagaCommands, SagaCommandRequestFactory and CancelOrderCommandHandler (Infrastructure.Messaging.Rpc), plus id 71's OperatorCancelRequestedEnvelope, whose Infrastructure.Outbox import id 71's fix round removed but which STILL imports OrderToCash.Orders.Infrastructure.Messaging.Rpc (line 2) and is therefore in this entry's population too (amended after id 71's review round 2, advisory A1)",
        "the remedy follows #7's own placement: #7 kept every RPC payload type in its shared contracts package, generated from asyncapi.yaml (order-to-cash-nestjs/packages/contracts/src/generated/asyncapi.types.ts at HEAD bf45af0), while #8 put them under each service's Infrastructure/Messaging/Rpc — so the RPC request/reply payload records move into src/Contracts (the project CLAUDE.md already designates for wire contract types), and Application code references them there; any reference that is not a wire payload is resolved by a port in Application implemented in Infrastructure",
        "the existing RPC payload schema-parity guards (feature ids 51 and 70's retyped-key-list class) still pass against the moved records, and their wire JSON stays byte-identical — asserted by the golden and parity tests already in the suite, not assumed",
        "tests/Architecture.Tests gains a NetArchTest rule: no type in any *.Application namespace depends on any *.Infrastructure namespace, in every service — armed by re-adding one Application→Infrastructure using in each of two services and confirming the rule fails naming the offending type",
        "the rule's population is a literal list of the six service assemblies, never discovered by a predicate that a violating assembly could escape"
      ],
      "notes": "Found by the leader on 2026-09-11 while verifying review advisory A1 of id 71 (progress/review_operator_note_survives_the_compensation_branches.md), which flagged only the one new import id 71 added. The sweep found the class is pre-existing and systemic, silent since phase 8: CLAUDE.md's Clean Architecture section says dependencies point inward and Infrastructure implements the ports the Application declares, but the only NetArchTest rules enforce Domain purity, SharedKernel's zero package references and Cqrs's exclusion from Domain — grep over tests/Architecture.Tests for any Application→Infrastructure constraint returns nothing. Honest parity note: this is NOT a guard dropped in translation — #7 has application→infrastructure imports of its own (apps/orders/src/application/saga-fact-handler.ts:17-18, apps/orders/src/application/commands/saga-dispatch.handlers.ts:8, apps/notifications/src/application/commands/notify.command-handlers.ts:14-20, and two trace-context imports) and no lint rule against them. It is #8's own stated convention, unenforced. The RPC-payload half, which is most of the population, is also a placement divergence from #7, whose payloads lived in the contracts package. Filed in phase 14 per the standing instruction not to defer issues to a later phase."
    },
    {
      "id": 77,
      "name": "test_hosts_exhaust_the_per_user_inotify_limit",
      "phase": 14,
      "title": "Every real host built in a test watches its configuration files for reload, one inotify instance each, and a parallel quality.sh run exhausts this machine's per-user limit of 128 — fixed on the test side during id 71, filed here as its own work with the guard it still lacks",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "the change already in the tree is recorded as this entry's work, not id 71's: root test.runsettings sets DOTNET_hostBuilder__reloadConfigOnChange=false under RunConfiguration/EnvironmentVariables, and Directory.Build.props carries <RunSettingsFilePath>$(MSBuildThisFileDirectory)test.runsettings</RunSettingsFilePath>, so both quality.sh's solution-level dotnet test and a bare single-project dotnet test pick it up",
        "a guard that FAILS when the setting stops reaching test hosts: a test that builds a real host the way the suite does (a *Host.CreateBuilder composition root) and asserts the host's own configuration reports hostBuilder:reloadConfigOnChange as false — reading the host's IConfiguration, never the process environment the test itself set — armed by deleting the RunSettingsFilePath line, and separately by deleting the environment entry from test.runsettings, each failing the named test",
        "the setting is confirmed to reach test hosts under quality.sh's own coverage-collecting invocation too (review round 2 of id 71 observed it only under plain dotnet test): read a live test host's environment, or run the guard above inside quality.sh, and record which",
        "the measurement stays recorded with the entry: fs.inotify.max_user_instances = 128 on this machine; 113–115 instances held by desktop processes at rest; Gateway.IntegrationTests alone peaked at 123 without the setting and 118 with it; the full quality.sh run peaked at 118 — leaving ~10 spare, which the entry states as a known thin margin whose remedy beyond the test side (raising the system limit) is the user's decision, never the harness's",
        "no test depends on configuration hot-reload: the search that id 71's review round 2 ran (no appsettings*.json under src/ or tests/, no reload-token reader, no test writing a config file mid-run) is re-run and pasted as a search result"
      ],
      "notes": "Found on 2026-09-11 when id 71's review-round-1 fix round ran a red full quality.sh: Billing.UnitTests.BillingDispatcherRegistrationTests.InvoicingPorts_EachResolve_AndAreEachRegisteredScoped threw IOException 'The configured user limit (128) on the number of inotify instances has been reached' from HostApplicationBuilder's configuration plumbing. The agent first attributed it to accumulated session state a reboot would clear; the leader measured instead, right after the reboot and before any test, 113 of 128 instances already held by desktop processes (VS Code and its extensions, kwallet, wireplumber), and no code anywhere disabling reload-on-change — so the red was structural, not residue. The test-side fix was built inside id 71's close-out, proved by change of kind (one host-heavy project's peak with and without the setting) and ended in a green full run at 1820. Id 71's review round 2 ruled it (a) safe, (b) reaching test hosts under plain dotnet test, (c) unguarded, and (d) deserving its own entry — which this is. The system limit was deliberately never touched."
    },
    {
      "id": 78,
      "name": "doc_comment_crefs_are_never_compiler_checked",
      "phase": 14,
      "title": "Unresolved <see cref> targets in XML doc comments are never compiler-checked, because Directory.Build.props sets GenerateDocumentationFile=false — so every 'the build verifies the doc comments' claim in this repository cannot fail",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "the class is enumerated FIRST, as a search result: a documentation-enabled build (GenerateDocumentationFile=true, NoWarn=CS1591 so missing-comment noise is excluded) of EVERY project in the solution, one classification line per CS1573/CS1574/CS1584/CS1734 site — naming ISagaCommandStore.cs:26 (FindOperatorCancelNoteAsync, rewritten in id 71's round-2 fixes) and SagaFactHandler.cs:183 (commandStore, a primary-constructor parameter, id 71 round 1) as id 71's, and CancelOrderCommandHandler.cs:34 (OrderNotCancellableError) as feature 41's; id 71's review round 3 counted 12 in the Orders build closure alone, before the other five services were looked at",
        "every unresolved cref is fixed to a resolvable target or rewritten as <c>…</c>, per site; a cref that pointed at a non-existent <remarks> is corrected to what the target actually carries",
        "the check is made STANDING, not one-off: documentation generation enabled under TreatWarningsAsErrors with CS1591 suppressed (Directory.Build.props), or an equivalent quality.sh step that fails the run — and the choice states its build-time cost",
        "armed: adding one unresolved cref to a src/ file fails the standing check with a message naming that cref and file, and removing it restores green",
        "while in ISagaCommandStore.cs and SagaCommand.cs, correct review round 3's advisory R3-A1: the triggering-event-topic docs (ISagaCommandStore.cs:77, SagaCommand.cs:55) say the envelope was 'consumed from' the topic, which is untrue for operator-cancel compensation rows — they STORE the orders facts topic for dead-letter routing, they never consumed from it",
        "correct the stale remark in src/Orders/Application/Commands/PlaceOrderCommand.cs (<remarks>, :18-27): it says RequestId is 'carried and deliberately IGNORED by this feature' and that 'this handler never looks at it again', which feature 27 made untrue — PlaceOrderCommandHandler.cs:39-41 now replays by requestId (RI1–RI5). Enumerate on the retired wording first ('IGNORED', 'never looks at it again', 'out of scope here by the orders_acceptance brief') across src/ and tests/, excluding bin/obj by path, and classify every hit",
        "correct the stale remark in src/Billing/Domain/Invoice.cs:24 (advisory A5 of id 72's re-review, routed here 2026-09-12 rather than left unstated): it calls MarkPaidInput 'feature 22's seam. Delivered and unit-tested here; uncalled until billing.payment.register exists', which has been false since feature 22 shipped in phase 10 — src/Billing/Application/PaymentRegisterService.cs:12 calls itself 'the sole LIVE caller of Invoice.MarkPaid', calling it at :131 and MarkPaidAsync at :144, wired at BillingRpcResponder.cs:68/:214/:326 and proven against real infrastructure by tests/Billing.IntegrationTests/PaymentRegisterTests.cs. This is the SAME defect class as id 72's test-matrix rows — a stated absence outliving the feature that ended it — so enumerate it as one: the retired wording ('uncalled until', 'no live caller', 'ships uncalled', 'seam', 'delivered uncalled', 'has no caller until') swept across src/ and tests/ with bin/obj excluded BY PATH, one classification line per hit, before any edit. id 72's own enumeration found the test-matrix half of this class; the src/ half was never swept"
      ],
      "notes": "RE-OPENED 2026-09-13 by maintainer ruling: the 2026-09-13 dispositioning below is SUPERSEDED. The maintainer rejected accepting these and directed that phase 14 be finished and correct, not closed by disposition. Status is therefore `pending` again and the ACCEPTED, NOT FIXED note that follows is retained as history, not as the current disposition. || SUPERSEDED DISPOSITION FOLLOWS || DISPOSITIONED 2026-09-13 — ACCEPTED, NOT FIXED, at the human gate. Phase 14 was closed to scope after the audit filed more entries than it closed; the maintainer ruled that entries which do not change shipped behaviour are closed with documented evidence rather than worked. Status is `done` because the backlog has no `accepted` status — it means DISPOSITIONED, not implemented. Reason: unresolved <see cref> targets are a documentation-quality defect with no runtime effect. GenerateDocumentationFile=false means the compiler never checks them, and turning it on repository-wide is a build-policy change disproportionate to the benefit. The three specific stale remarks this entry named are corrected in place by the features that touched those files. || ORIGINAL ENTRY FOLLOWS || Found by id 71's review round 3 (progress/review_operator_note_survives_the_compensation_branches.md, R3-F1, filed per its own recommendation). It corrects a verification claim the LEADER wrote into id 71's round-2 text-fix brief: that `dotnet build --no-incremental` under TreatWarningsAsErrors 'catches broken <see cref> in rewritten doc comments'. It cannot: Directory.Build.props:22 sets <GenerateDocumentationFile>false</GenerateDocumentationFile>, and .editorconfig assigns no severity to CS1574/CS1584 — so the compiler never emits the diagnostic, and the prescribed verification was a guard that could not fire. The reviewer's doc-enabled build surfaced the class. Filed as its own entry rather than fixed inside id 71 because it is a repository-wide CLASS (CLAUDE.md: enumerate the class before fixing any instance) and fixing id 71's two instances would have closed two lines and left the class live."
    },
    {
      "id": 79,
      "name": "sa4_operator_cancel_release_order_in_the_nestjs_assessment",
      "phase": 14,
      "title": "SA-4 changed the shared spec in both repositories, but #7's code still releases the credit hold before the stock on a credit_approved/confirmed cancellation and still ignores a credit.approved.v1 that arrives after an operator cancellation",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "enumerate first, by content under order-to-cash-nestjs/apps/orders/src with node_modules and dist excluded by path: every site that plans, enqueues or asserts credit_release before stock_release for an operator cancellation, and every credit.approved.v1 handling of an operator-cancelled order — the leader's enumeration when SA-4 was applied found cancel-order.handler.ts:249-253 (compensationPlanned ['credit_release', 'stock_release']), saga-steps.ts:154 (credit.approved.v1), :188 (stock.released.v1 variants), :244 (credit.released.v1 variants) and :82-93 (the synthesised credit_released step), plus four specs asserting the old order: orders-cancel.integration.spec.ts, application/cancel-order.handler.spec.ts, application/saga-steps.spec.ts and presentation/orders-cancel.controller.spec.ts",
        "from credit_approved/confirmed the operator cancellation enqueues stock.release first and plans ['stock_release', 'credit_release']; stock.released.v1 at those statuses issues credit.release; credit.released.v1 at those statuses cancels with operator_cancelled",
        "a credit.approved.v1 for an order whose operator cancellation was already accepted (still stock_reserved with its stock release under way, or cancelled with operator_cancelled) issues credit.release and nothing else — no transition, no order.confirmed.v1, no despatch.create",
        "a despatch.create refused with PRECONDITION_FAILED after a winning release is a terminal rejection with no dead-letter and no order.saga_failed.v1; a despatch that won leaves the order despatching with no credit.release issued",
        "Fulfillment's one-lock requirement is ALREADY met in #7 and needs a guard, not code: stock.release (apps/fulfillment/src/application/stock-reservation.handler.ts:103,109) and despatch.create (despatch-creation.handler.ts:64,70) both lock the order's stock rows through stockIdsOfOrder + lockByIdsForOrder — name the existing test, or write one, that fails if either path stops taking that lock",
        "each behaviour is armed in #7's own suite: restoring the old release order, deleting the late-approval release, and routing the refused despatch to a park each fail a named spec, recorded with its verbatim message"
      ],
      "notes": "Filed by the leader on 2026-09-11 when SA-4 was applied to both repositories. Id 75 is the precedent: a shared-spec amendment raised here whose #7 code alignment is tracked in #8's backlog, because #7 is a completed assessment with no open backlog of its own. #8's own conformance is id 62's rework. The amendment is prose only — #7's contract types were regenerated, contracts:check reported OK, 22 contracts tests and the workspace typecheck passed — so nothing in #7 fails today; its code implements the superseded order."
    },
    {
      "id": 80,
      "name": "saga_command_fast_path_is_head_of_line_blocked",
      "phase": 14,
      "title": "The saga command fast path drains its channel with ONE sequential worker, so one slow or absent responder stalls every other order's saga commands for up to ~16.5 s per command — #7's @nestjs/cqrs saga ran each dispatch concurrently",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "enumerate first, by content under src/ with bin and obj excluded by path: every consumer of ChannelSagaCommandSignal.Reader and every other sequential drain of an in-process dispatch queue, one classification line per hit — the leader found src/Orders/Infrastructure/Saga/SagaCommandDispatchWorker.cs:22-29 (await foreach, awaiting each dispatcher.DispatchAsync in turn) over ChannelSagaCommandSignal.cs:19-20 (SingleReader = true)",
        "reproduced FIRST by a test that fails on today's code: order A's responder never answers (or answers only after a controlled delay longer than the test's bound), and an ordinary command for order B is signalled after A's — B's command must reach 'sent' within a bound shorter than A's worst-case dispatch time. The sweeper is DISABLED in that test so it cannot mask the blocking, which is exactly the masking Confirmed_ReleaseWins silently relies on (recorded in id 62's fix round 2)",
        "the chosen concurrency model is stated with its cost — bounded parallelism across orders, with commands for the SAME order kept serialised if any invariant needs that — and the record names which invariants, if any, rely on per-order dispatch ordering and shows them unchanged",
        "a ported-idiom ledger row in the implementation record: #7 relied on @nestjs/cqrs 11.0.3's registerSaga running each command as mergeMap(command => defer(() => commandBus.execute(command))) (node_modules/@nestjs/cqrs/dist/event-bus.js:196), which subscribes every command's execution concurrently, so one Issue…Command handler awaiting a slow NATS dispatch (apps/orders/src/application/commands/saga-dispatch.handlers.ts:22-23) never delays another; in #8 that property is supplied by the fix, guarded by the reproducing test",
        "armed: restoring the single sequential drain makes the reproducing test fail with a message naming order B's command and the bound it missed",
        "specs/order_saga_orchestrator/design.md §5.5 is corrected in place: its composition (:333-334) ported #7's off-the-consume-loop property but not its concurrency, and its own argument against inline dispatch (:327, 'every subsequent fact would queue behind a dead responder') describes the defect the single worker moved one stage later"
      ],
      "notes": "Found by the leader on 2026-09-11 while verifying id 62's fix round 1: F3's second arming attempt disabled the sweeper and stock.release never reached 'sent', because the one fast-path worker was blocked by the test's gated despatch.create RPC. Confirmed by reading SagaCommandDispatchWorker.cs:22-29 and ChannelSagaCommandSignal.cs:19-20. With the defaults the worst-case stall per command is 3 × TimeoutMs 5 000 + backoff 500 + 1 000 = 16.5 s (OrdersSagaOptions.cs:26-32, the same figure LeaseMs's own remark names), during which the fast path dispatches nothing for any other order; the sweeper (IntervalMs 30 000, PendingGraceMs 10 000) is the only other route. Consequence under SA-4: a cancellation's stock.release for a confirmed order is dispatched after the despatch.create issued on confirmation, so 'release wins' is practically reachable only through the sweeper or a failed despatch — cancellations of confirmed orders will usually be overtaken. A ported-idiom miss of exactly the class CLAUDE.md's ledger exists for: #7's concurrency came from rxjs mergeMap inside its framework, and design §5.5 argued the consume-loop half and never asked what supplied the other. Not fixed inside id 62: it is a cross-cutting dispatch property with its own reproduction and arming."
    },
    {
      "id": 81,
      "name": "gateway_concurrent_trace_test_times_out_on_a_fixed_two_second_budget",
      "phase": 14,
      "title": "The Gateway's OR4 concurrent-trace integration test times out on a fixed 2 000 ms RPC budget under a loaded machine — a red full run that was absorbed by a green rerun, which is not evidence about a red run",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "enumerate first, by content and with bin/obj excluded by path: every fixed RPC or wait budget in tests/Gateway.IntegrationTests plus NatsRpcClient's own default timeout, one classification line per hit — the leader's sighting is NatsRpcClientIntegrationTests.OR4_TwoConcurrentCalls_EachCarriesItsOwnActiveTraceId failing with RpcTimeoutError, RPC call to gateway.it.trace-concurrent timed out after 2000ms, in /tmp/claude-1000/quality_feature73_1.log (2026-09-12 07:41)",
        "the cause is identified BEFORE any change, with evidence, and named explicitly: the responder's own start-up inside the 2 000 ms window, contention with a full suite's containers, the single-reader dispatch path filed as id 80, or none of these — a guess recorded as a cause is itself a finding",
        "proved by a change of KIND, never of probability: delay the responder (or load the machine) by a controlled interval and show the current budget loses every time and the chosen fix wins every time. 'The flake stopped' is not evidence, for the same reason a green run is not evidence about a red one",
        "armed: restoring the old budget or pacing makes the named test fail with a message naming the budget it missed",
        "if the test is correct and the budget is simply too tight for a machine running the whole suite, say so and state the number chosen WITH its margin and how the margin was measured — never raise the number until it goes green"
      ],
      "notes": "RE-OPENED 2026-09-13 by maintainer ruling: the 2026-09-13 dispositioning below is SUPERSEDED. The maintainer rejected accepting these and directed that phase 14 be finished and correct, not closed by disposition. Status is therefore `pending` again and the ACCEPTED, NOT FIXED note that follows is retained as history, not as the current disposition. || SUPERSEDED DISPOSITION FOLLOWS || DISPOSITIONED 2026-09-13 — ACCEPTED, NOT FIXED, at the human gate. Phase 14 was closed to scope after the audit filed more entries than it closed; the maintainer ruled that entries which do not change shipped behaviour are closed with documented evidence rather than worked. Status is `done` because the backlog has no `accepted` status — it means DISPOSITIONED, not implemented. Reason: a fixed 2000 ms RPC budget in ONE Gateway integration test, under load, on a machine running the whole suite. Test-only, and the class it belongs to (unpaced or fixed budgets) was already swept and fixed at the six sites id 63 named. || ORIGINAL ENTRY FOLLOWS || Found by the leader on 2026-09-12 while verifying id 73, whose own changes touch only src/Notifications and the Notifications tests — nothing in Gateway. The first full quality.sh run of that feature went red on this one test; the implementer reran the single test (green in 362 ms, gateway_rerun_1.log) and then ran a second full suite (green, quality_feature73_2.log, 1872) and recorded it as 'the one flake'. That is exactly what CLAUDE.md forbids absorbing: a red run needs its own diagnosis, and a green rerun says nothing about it. No existing entry covers this — content searches for OR4, trace-concurrent, RpcTimeout and TwoConcurrentCalls across the backlog return nothing; id 69 is the Gateway READINESS-loop pacing gap (StandInResponder and FulfillmentStockEndToEndTests), a different mechanism, and id 63's class is attempt-counted budgets whose retried error returns instantly. This test is one of the three OR4_TwoConcurrentCalls cases feature 27's design section 11 (row L21) names, so the trace-concurrency claim it guards is one the benchmark already relies on. Filed rather than left in an implementation record, per the rule that an unexplained intermittent becomes a numbered entry."
    },
    {
      "id": 82,
      "name": "arming_failure_messages_that_name_nothing",
      "phase": 14,
      "title": "Some arming mutations are proved by a failure message that names nothing — Assert.False(string.IsNullOrEmpty(x)) prints Expected: False / Actual: True, so the arm is evidence only to a reader who opens the stack line",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "the class is enumerated FIRST, as a search result across tests/ with bin and obj excluded by path: every assertion used as the failing assertion of a recorded arm whose message cannot name the claim — Assert.False(string.IsNullOrEmpty(...)), Assert.True(x != null), Assert.NotNull on a bare bool-like check, Assert.True(collection.Any()) and the like — one classification line per hit, naming which arming row in which progress/impl_*.md record depends on it",
        "each hit is either replaced by an assertion that prints the offending value (Assert.Matches, Assert.Equal, Assert.Contains with the actual collection) or kept with a stated reason why no self-describing form exists; the id 73 sighting is the console-line trace guard, where Assert.Matches(\\\"^[0-9a-f]{32}$\\\", ...) one line later already subsumes the IsNullOrEmpty check for null and empty",
        "the arming rows in the affected records are re-run and their verbatim messages updated — an arming table quoting a message that names nothing is not evidence for the claim above it, and the record is the artefact #9 inherits",
        "armed: re-introducing one replaced assertion makes its named test fail with the old, nameless message, and the record shows both messages side by side so the difference is legible rather than asserted",
        "CLAUDE.md's arming rule gains one sentence: the failure message must name the claim, and an assertion whose message cannot do so is not an acceptable failing assertion for an arm — with the id 73 example cited"
      ],
      "notes": "RE-OPENED 2026-09-13 by maintainer ruling: the 2026-09-13 dispositioning below is SUPERSEDED. The maintainer rejected accepting these and directed that phase 14 be finished and correct, not closed by disposition. Status is therefore `pending` again and the ACCEPTED, NOT FIXED note that follows is retained as history, not as the current disposition. || SUPERSEDED DISPOSITION FOLLOWS || DISPOSITIONED 2026-09-13 — ACCEPTED, NOT FIXED, at the human gate. Phase 14 was closed to scope after the audit filed more entries than it closed; the maintainer ruled that entries which do not change shipped behaviour are closed with documented evidence rather than worked. Status is `done` because the backlog has no `accepted` status — it means DISPOSITIONED, not implemented. Reason: an arming message that names nothing degrades the QUALITY OF FUTURE EVIDENCE, not the behaviour of any code. The rule it asks for is now stated in CLAUDE.md (a failure message must name the claim), which is where it does the work — every feature since has been briefed with it. || ORIGINAL ENTRY FOLLOWS || Raised as advisory A6 by id 73's review round 2 (progress/review_notification_send_degrades_on_permanent_failure.md, 'New findings this round'): the reviewer's own M7 and the implementer's arm 3 both ended in 'Assert.False() Failure / Expected: False / Actual: True', identifiable only by the stack-trace line. The repository's standing rule is that a failure counts as evidence when its message names what was broken — a rule written for mutations, and this entry extends it to the assertion that catches them. Not blocking for id 73: the assertions work and the evidence is unambiguous once the line is read, which is why it was routed rather than held. Filed as its own entry after reading the candidates rather than by proximity: ids 67-70 and 74 own different defects (design-time env reads, composition-root delegation, Gateway readiness pacing, retyped key lists, dead-letter selection by content) and id 72 is about test-matrix rows that outlived their named closer, so folding this into id 72 would be the exact misrouting id 72 exists to correct."
    },
    {
      "id": 83,
      "name": "architecture_rule_cannot_see_references_inside_async_lambdas",
      "phase": 14,
      "title": "ApplicationInfrastructureLayeringTests cannot see an Infrastructure reference confined to an async lambda — the dominant shape of Billing and Fulfillment Application code, so a future violation there ships unguarded on a green suite",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "enumerate the covered population FIRST, as a search result with bin and obj excluded by path: every Application-layer method in src/*/Application whose work happens inside an async lambda passed to another method (unitOfWork.ExecuteAsync(async ct => ...) and any sibling shape), one classification line per hit — this is the code the guard currently cannot see into, and its size is the entry's own justification",
        "the boundary is re-established by probe BEFORE any fix and recorded as observed shapes, never as a theory: a reference inside an async lambda is missed; inside a sync lambda in the same position it is caught; in an async METHOD on the outer type it is caught; in a method signature, interface member or generic type argument it is caught (id 76's review round 1, probes P1 through P8, which disproved the mechanism the implementation record first stated)",
        "the gap is closed by whatever mechanism bullet 2's probe establishes — and the entry deliberately does NOT prescribe one. CORRECTED BY THE LEADER 2026-09-12: this bullet previously read 'the rule is closed over compiler-generated nested types — walking Cecil's nested types for each declaring type', which names a cause that id 76's own review round 1 MEASURED FALSE. Its words: 'That mechanism is disproved.' Sync lambdas are compiler-generated nested closures too, and probes P6 (non-capturing sync lambda) and P8 (capturing sync lambda) in the same unitOfWork.ExecuteAsync position were both CAUGHT, while P7 — a capturing ASYNC lambda in a local Func<Task<string>> with no unitOfWork and no port call — was MISSED. So nested-closure-invisibility is not the boundary; every probe that survived sat inside an ASYNC lambda specifically, whose state machine the compiler emits as its own type. Derive the real mechanism from the probe, then close it; the cost is stated with a MEASURED figure for whatever the fix adds to the Architecture.Tests runtime, never an estimate",
        "armed with the async-lambda shape specifically: a real Infrastructure construction inside unitOfWork.ExecuteAsync(async ct => ...) in TWO services makes ApplicationMustNotDependOnInfrastructure fail, and the failure message names both offending types (id 82's rule — a failing assertion whose message names nothing is not evidence); restoring both makes it green, cmp-identical, on a forced rebuild",
        "progress/impl_application_layer_depends_on_infrastructure_unguarded.md section 5's corrected boundary table is updated to record the gap as closed, and this entry states how many real violations existed in the newly visible population at the moment of closing — a count read off bullet 1's enumeration, not an assurance"
      ],
      "notes": "Raised as advisory A2 by id 76's review round 1 (progress/review_application_layer_depends_on_infrastructure_unguarded.md), in the reviewer's own words: the guard is blind precisely where that business logic lives, so a future RpcJson.Serialize added inside one of those lambdas ships unguarded on a green suite. The reviewer could not file it itself (it must not write feature_list.json), so the leader filed it. The bound id 76's record claims — that none of its 26 real violations was shaped as an async-lambda-only reference — is INDEPENDENTLY VERIFIED by that review: Billing's and Fulfillment's services exposed their payload type in a public signature, and the three Orders files used async methods on the outer type, which probe P5 proves is caught. So id 76 is sound and this is a gap in reach, not a hole under it. Not an SA-n: nothing in specs/shared causes it, and the same review confirmed the shared spec untouched. Belongs to the guard-hardening family with ids 72 and 82 — a guard whose assertion cannot detect the defect it names, here because the scan cannot reach the code rather than because the assertion is weak."
    },
    {
      "id": 84,
      "name": "gateway_keeps_a_third_copy_of_the_now_canonical_rpc_payloads",
      "phase": 14,
      "title": "src/Gateway/Application/Rpc/GatewayRpcPayloads.cs keeps a third copy of RPC payload records that id 76 made canonical in src/Contracts/Rpc — the same two-copies-cannot-drift argument that justified the unification now applies to it",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "enumerate FIRST, as a search result with bin and obj excluded by path: every record in src/Gateway/Application/Rpc/GatewayRpcPayloads.cs and every sibling Gateway payload file, matched by NAME and by PROPERTY SET against src/Contracts/Rpc, one classification line each — identical (safe to unify), field-different (state the difference and whether it is deliberate), or Gateway-only (no counterpart). The reviewer named StockListRequestPayload, CreditListReplyPayload and InvoiceViewPayload as examples, not as the population",
        "every record classified identical is unified onto the Contracts copy, and the Gateway file keeps only what genuinely has no counterpart; a record that differs is NOT unified silently — the difference is recorded with its reason and left alone",
        "the wire cannot move: the golden-envelope and BC23 parsed-from-the-spec tests, plus the Gateway's own HTTP-level payload tests, are RUN against the unified records and named with their counts — byte-identity asserted, never assumed, exactly as id 76 did for the first unification",
        "armed: one unified record is given an extra property (or one renamed) and a named Gateway HTTP test plus a named BC23 test each fail with a message naming the offending property; restoring makes both green on a forced rebuild",
        "if any Gateway copy is deliberately separate, that separateness is stated in the file itself with its reason, so the next sweep does not re-raise it — and this entry says which records those are"
      ],
      "notes": "RE-OPENED 2026-09-13 by maintainer ruling: the 2026-09-13 dispositioning below is SUPERSEDED. The maintainer rejected accepting these and directed that phase 14 be finished and correct, not closed by disposition. Status is therefore `pending` again and the ACCEPTED, NOT FIXED note that follows is retained as history, not as the current disposition. || SUPERSEDED DISPOSITION FOLLOWS || DISPOSITIONED 2026-09-13 — ACCEPTED, NOT FIXED, at the human gate. Phase 14 was closed to scope after the audit filed more entries than it closed; the maintainer ruled that entries which do not change shipped behaviour are closed with documented evidence rather than worked. Status is `done` because the backlog has no `accepted` status — it means DISPOSITIONED, not implemented. Reason: a third copy of the RPC payload records is a maintenance cost, not a wire risk: the wire is guarded by the golden-envelope parity tests and the AsyncAPI schema theories, which would fail on any real divergence. Overlaps id 70; if either is ever re-opened, do them together. || ORIGINAL ENTRY FOLLOWS || Raised as advisory A4 by id 76's review round 1 (progress/review_application_layer_depends_on_infrastructure_unguarded.md). Correctly OUT of id 76's scope: GatewayRpcPayloads.cs is an Application-layer type, so it violates no layering rule and was never in that feature's population — the reviewer said so explicitly. What makes it worth its own entry is id 76's own justification: the unification there was defended as eliminating a pre-existing three-way duplication so that two copies of one RPC contract cannot drift apart, and that argument does not stop at the Gateway boundary. Gateway's copy is a Presentation-facing convenience today, and CLAUDE.md's own file comments record its separateness as deliberate for the RPC CLIENT direction — so the first bullet's classification may legitimately conclude that some or all of it stays. Filed so the question is answered by an enumeration rather than by silence, per the standing rule that an advisory with a root cause outside the feature that found it becomes a numbered entry."
    },
    {
      "id": 44,
      "name": "money_column_width",
      "phase": 6,
      "title": "Correct the money columns from int to bigint across all write databases, and delete the narrowing",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "all 13 money columns are bigint on the live databases, verified from INFORMATION_SCHEMA",
        "the initial migrations are amended rather than a widening migration added (human gate ruling) — the Phase 6 migrations have never left this machine and have no dependants",
        "every (int) narrowing cast on a money value is DELETED, not made checked — including the unchecked ones already in src/Seed",
        "an architecture or schema test asserts no money column is int, so the regression cannot return silently",
        "the seed re-runs and the 413-row master-data parity against #7 still holds"
      ],
      "note": "Corrects an error of the leader's, carried for three phases: the plan justified int columns as 'spec parity', but specs/shared/ specifies only 'integer minor units' and never a width. int was #7's MySQL choice, and #7 needs no narrowing because JS numbers are doubles. Keeping int forced a long->int narrowing at every money boundary that #8 alone would pay for. Same category of false parity as the JSON key-order finding in Phase 5."
    },
    {
      "id": 85,
      "name": "container_fixtures_self_assign_host_ports_and_race",
      "phase": 14,
      "title": "Eight test fixtures choose their own host port with a TcpListener that is closed before Docker binds it — a time-of-check-to-time-of-use race that has killed whole test collections twice",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "the class is enumerated FIRST, as a search result — the command, its complete output, one classification line per hit — across tests/ with bin and obj excluded BY PATH: every WithPortBinding call passing a self-chosen host port, and every copy of GetFreeTcpPort. The population at filing is 8 fixture files, 12 self-assigned bindings and 8 independent copies of GetFreeTcpPort: KafkaContainerFixture in all six integration projects (Fulfillment :33, Billing :33, Orders :46, Notifications :34, Gateway :28, Projector :32), MailpitContainerFixture (2 ports, :45-46) and MailpitAuthContainerFixture (4 ports, :59-60 and :67-68). The TcpListener at tests/Notifications.IntegrationTests/SendFailureClassifierRealSmtpTests.cs:147 serves a different purpose and must be CLASSIFIED, not assumed to belong",
        "every self-assigned binding moves to the idiom five sibling fixtures in this repository already use — WithPortBinding(containerPort, true), letting Docker assign, then GetMappedPublicPort to read it back — so the port is never unheld between the check and the bind. The five NatsContainerFixture files (Fulfillment, Billing, Orders, Gateway, Projector) are the in-tree control and have never produced this error",
        "the Kafka sites are NOT fixed naively, and the entry says why: KAFKA_ADVERTISED_LISTENERS must carry the host-visible address, which is the reason a port was chosen in advance. Start the container, read GetMappedPublicPort, then set the advertised listener (Testcontainers' documented approach for Kafka), rather than passing true and leaving the broker advertising a port nothing can reach. A change that makes the race disappear by breaking the broker is not a fix",
        "proved by a change of KIND, not of probability, per CLAUDE.md: bind the port the fixture is about to use, in the window between its check and the container start, and show the CURRENT shape fails every time with Docker's own 'port is already allocated', and the FIXED shape succeeds every time. 'The flakes stopped' is not evidence, for the same reason a green run is not evidence about a red one",
        "armed: re-introducing GetFreeTcpPort at one converted site makes a named test fail with a message naming the fixture and the port — an arming failure that names nothing is backlog id 82's class and does not count",
        "GetFreeTcpPort's eight copies are deleted rather than left dead; if any caller genuinely still needs one, exactly one copy survives, in one home, with its callers stating why"
      ],
      "notes": "RE-OPENED 2026-09-13 by maintainer ruling: the 2026-09-13 dispositioning below is SUPERSEDED. The maintainer rejected accepting these and directed that phase 14 be finished and correct, not closed by disposition. Status is therefore `pending` again and the ACCEPTED, NOT FIXED note that follows is retained as history, not as the current disposition. || SUPERSEDED DISPOSITION FOLLOWS || DISPOSITIONED 2026-09-13 — ACCEPTED, NOT FIXED, at the human gate. Phase 14 was closed to scope after the audit filed more entries than it closed; the maintainer ruled that entries which do not change shipped behaviour are closed with documented evidence rather than worked. Status is `done` because the backlog has no `accepted` status — it means DISPOSITIONED, not implemented. Reason: a genuine TOCTOU race in eight test fixtures (GetFreeTcpPort closes its TcpListener before Docker binds the port), but it is test-harness only — it cannot affect shipped behaviour. It has cost two red runs. The in-tree control is already documented: five NatsContainerFixture files use Docker-assigned ports with GetMappedPublicPort and have never produced it, so a future fix is cheap and well-specified. RE-OPEN if it costs a third red run. || ORIGINAL ENTRY FOLLOWS || Filed by the leader on 2026-09-12, measured twice rather than suspected. FIRST SIGHTING, id 71: Projector.IntegrationTests failed 57 of 59, every one at 1 ms out of KafkaContainerFixture.InitializeAsync(), 'Bind for 0.0.0.0:35127 failed: port is already allocated' (progress/impl_operator_note_survives_the_compensation_branches.md:402-405, count confirmed independently by its reviewer at review_...:309, recorded at current.md:693). SECOND SIGHTING, today: the post-stash re-run of quality.sh returned 1879 passed / 1 failed / 1880 total, the single failure being Gateway.IntegrationTests' StreamProjectorEndToEndTests.AFactPublishedOnTheRealOrdersFactsTopic_ConsumedByTheRealProjector_ArrivesAtAConnectedSseClient, again at 1 ms in KafkaContainerFixture.InitializeAsync, 'Bind for 0.0.0.0:33761 failed: port is already allocated'. MECHANISM, read from source rather than inferred: GetFreeTcpPort (e.g. Gateway's at :62-64) opens new TcpListener(IPAddress.Loopback, 0), reads the port the OS assigned, and CLOSES the listener; the port is handed to Docker later at WithPortBinding (:28), so nothing holds it in between and anything may take it. Because the race is lost at container START, every test in the collection dies at 1 ms — which is why one sighting cost 57 tests and the other cost exactly 1, and why it reads as environmental. DISTINCT from id 81 (a fixed 2000 ms RPC budget in the Gateway's OR4 concurrent-trace test) and from id 69 (readiness-loop pacing): same family of integration fragility, different mechanism, and this one is a genuine TOCTOU in the harness rather than a budget or a wait. Neither sighting was a product defect, but both cost a full red run and the diagnosis time to prove it environmental, which is the cost this entry exists to end."
    },
    {
      "id": 86,
      "name": "composition_root_delegation_guard_anchors_a_call_shape_not_the_live_host_call",
      "phase": 14,
      "title": "The delegation guard anchors an argument to a call SHAPE rather than to the one live host call, so a same-shaped decoy leaves a service's health probes unwired with the suite green — plus a provenance guard that checks a declaration and not the last write",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "anchor to THE invocation, not to its shape: collect the file's <HostType>.<HostMethod> invocations first, require EXACTLY ONE, and read the argument list of that node. Deriving the population from the same node also closes the unit mismatch with ThePopulationTableMatchesEveryDelegatingArgumentFoundOnDisk, whose unit is the per-file multiset of configure* names and which the decoy preserves exactly",
        "match the host expression by rendered-suffix on a member boundary — the rule TargetMatches already uses for targets — so a fully-qualified or aliased host call stops being a FALSE RED (measured: a fully-qualified host call today fails, and the message misdiagnoses it as a missing argument)",
        "delete the two false absolutes rather than weakening them: tests/Architecture.Tests/CompositionRootDelegationWiringTests.cs:474-476 and Directory.Packages.props:26-28 both claim an interpolation hole's contents can never be reached as an ArgumentSyntax node. That is false and was measured false. The manifest copy is the more expensive half — a false absolute in a dependency manifest that no reader of the test will re-check, and that #9 inherits as settled. This is the FIFTH consecutive round in which a false absolute about this mechanism shipped",
        "reword ParseFile's message to name what it actually rejects — ANY directive trivia, #pragma and #region included — rather than calling them conditional-compilation directives (measured false red with a misnamed category)",
        "arm each against the CLASS, not the reported literal: for the anchor, at minimum a same-shaped decoy, a positional-argument decoy, and a decoy inside an interpolation hole. This entry exists because five consecutive rounds each armed the exploit they were handed rather than its class",
        "SAME ENTRY — AssertConnectionStringProvenance (tests/Architecture.Tests/CompositionRootDelegationWiringTests.cs:437-461) checks the DECLARATION'S INITIALIZER and never the variable's last write. Leaving the declaration correct and adding 'ordersConnectionString = BillingSeedWriter.ConnectionString();' one line below leaves Architecture.Tests 9/9 AND Seed.UnitTests 44/44 green, with Orders fixtures written into the Billing database. Close it by following assignments, or by checking the USE rather than the name"
      ],
      "notes": "Filed verbatim from the final review of Slice A (progress/review_composition_root_delegation_and_design_time_factories.md, 'The residual, in filable form'), 2026-09-12, under a leader-imposed cap: id 68 closed after five fix rounds and five defeats, with this residue disclosed and filed rather than funding a sixth. MECHANISM, measured: IsArgumentOfInvocation (:516-524) requires an argument's enclosing invocation to RENDER AS <HostType>.<HostMethod>; it does not require that invocation to be the one live host call, and ExtractNamedArgument (:490-507) still searches the whole tree. So the 'satisfy the count elsewhere' family that D16 closed for UNRELATED call sites remains open for a SAME-SHAPED decoy: drop configureHealth: from the real BillingHost.CreateBuilder(...) and add a second, discardable BillingHost.CreateBuilder(args, BillingProgramConfiguration.Configure, configureHealth: BillingProgramConfiguration.ConfigureHealth) with configure passed POSITIONALLY so the required argument still matches exactly once. Compiles with 0 warnings; Architecture.Tests 35/35 and the class 9/9 green; Billing's health probes unwired. The decoy was placed inside an interpolation hole, which additionally falsifies the :474-476 and Directory.Packages.props:26-28 absolutes — after the mutation the file's only configureHealth: node IS the one in the hole, and the guard read it and passed. THE PROVENANCE HALF IS ID 56's D1 FOR THE THIRD TIME, AT THE THIRD SUCCESSIVE LINE — first the env key (id 56 D1), then the declaration's initializer (id 68 D18), now the variable's last write. Each fix closed the line it was shown and not the property, and each time both suites stayed green with Orders fixtures in the Billing database. DELIBERATELY NOT CLOSED IN ID 68: the cap was set because five rounds on one architecture test is disproportionate while id 80 (a ~16.5 s head-of-line stall of unrelated orders) waits. D17 — a shadowing type making byte-identical call text mean a no-op — is NOT part of this entry: it is a genuine bound on any syntax-only instrument, correctly disclosed in the guard itself, and not fixable without symbol binding."
    },
    {
      "id": 87,
      "name": "outbox_relay_deadlock_victim_escapes_run_once",
      "phase": 14,
      "title": "A SQL Server deadlock-victim error propagates out of OutboxRelay.RunOnceAsync instead of being retried — the execution strategy in force is the non-retrying default, and it fails a named test under full-suite load",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "reproduce it deterministically FIRST, before any fix, and record the verbatim failure: today it surfaces only under full-suite load (twice, both runs dying at about 13 minutes), while an isolated Orders.IntegrationTests run passes 146/146 — a reproduction that depends on running the whole solution is not a reproduction, so drive the contention directly (two relays, overlapping claim ranges, a controlled lock held by a third connection, or an equivalent) until the deadlock is a change of KIND rather than of probability, per CLAUDE.md",
        "the class is enumerated FIRST as a search result with bin and obj excluded BY PATH: every Database.CreateExecutionStrategy() call and every UseSqlServer(...) registration across src/, one classification line per hit, stating for each whether a retrying strategy is configured and what happens to a deadlock victim there. The relay is where it was NOTICED, not where the class ends — the same shape may sit in every service's unit-of-work and in the saga command store's own claim UPDATE",
        "the MECHANISM IS NOT PRESCRIBED BY THIS ENTRY and must be derived from the enumeration and the reproduction. EnableRetryOnFailure is the hint SQL Server's own message offers, but it is not automatically correct here: an execution strategy that retries wraps a transaction the relay opens itself (BeginTransactionAsync with a pinned IsolationLevel at OutboxRelay.cs:75), and EF forbids a user-initiated transaction under a retrying strategy unless the whole unit is wrapped in strategy.ExecuteAsync — which this code already does. Establish what actually happens before choosing between retrying, catching deadlock error 1205 specifically, reordering the claim to avoid the lock cycle, or changing the test's own contention design",
        "state explicitly whether the claim's own ordering is the root cause: two relays taking UPDLOCK on overlapping row ranges in opposite orders is a lock-cycle by construction, and if so a retry only hides it. Say which of the two it is, with evidence",
        "armed: the fix is proved by re-running the deterministic reproduction from bullet 1 and showing it now succeeds, AND by re-introducing the pre-fix behaviour and showing the named test fails again with a message naming the deadlock — an arming failure whose message names nothing is backlog id 82's class",
        "if any part of this is judged acceptable-as-is (for example, that a deadlock victim in the relay is harmless because the next poll re-claims the rows), that judgement is stated with the evidence for it and the consequence for the relay's own loop, never left implicit"
      ],
      "notes": "CLOSED 2026-09-13 WITHOUT A REVIEW ROUND, at the human gate, to conserve budget. The fix is armed both ways, its class was enumerated across every CreateExecutionStrategy and UseSqlServer site, and all affected suites are green; it was committed in b453931 labelled as implemented-but-not-reviewed. || ORIGINAL ENTRY FOLLOWS || Found by the leader on 2026-09-12 while verifying id 80. VERBATIM: 'System.InvalidOperationException : An exception has been raised that is likely due to a transient failure. Consider enabling transient error resiliency by adding EnableRetryOnFailure to the UseSqlServer call. ---- Microsoft.Data.SqlClient.SqlException : Transaction (Process ID 130) was deadlocked on lock resources with another process and has been chosen as the deadlock victim. Rerun the transaction.' Thrown from OutboxRelay.RunOnceAsync (src/Orders/Infrastructure/Outbox/OutboxRelay.cs:69, via :152 and :158), failing tests/Orders.IntegrationTests/OutboxRelayConcurrencyTests.cs:51 OI4_TwoConcurrentRelays_GrantDisjointBatchesAndPublishEveryRecordExactlyOnce. NOT A REGRESSION FROM ID 80, established by evidence rather than assumed: OutboxRelayConcurrencyTests boots NO host (it is [Collection(MsSqlCollection.Name)] over MsSqlContainerFixture and constructs two OutboxRelay instances directly via BuildRelay, calling RunOnceAsync on both), so SagaCommandDispatchWorker, ChannelSagaCommandSignal and OrdersSagaOptions — the only three production files id 80 touched — are never constructed; and OutboxRelay.cs has mtime 2026-09-10 21:48 with no diff, untouched this session. NOT A FLAKE TO BE WAVED THROUGH: the failing path is PRODUCTION code, and in production a deadlock victim escaping RunOnceAsync surfaces as an unhandled exception in the relay's own loop. It appeared in two separate full-suite runs, both ending at about 13 minutes, while the same project passes 146/146 in isolation. CORRECTED BY THE LEADER 2026-09-12, SAME DAY: I first characterised that as 'load-dependent, not random' on the strength of those two observations. A THIRD full-suite run (quality_feature83.log, 19:43, 1908/1908 green across 18 projects) contains ZERO OI4 mentions and ZERO deadlock mentions, so the failure did NOT recur under the same conditions. It is INTERMITTENT, not near-deterministic, and my earlier wording overstated what two samples can support. The acceptance is unaffected — bullet 1 already requires a deterministic reproduction to be built rather than waited for, and that requirement is now the whole point: a defect seen twice in three full runs cannot be closed by observing a green run. The test itself documents the contention as deliberate: 'Genuinely concurrent — both instances race for the SAME unpublished rows.'"
    },
    {
      "id": 88,
      "name": "saga_dispatch_parallelism_bounds_the_stall_rather_than_removing_it",
      "phase": 14,
      "title": "Id 80 BOUNDS the head-of-line stall rather than eliminating it — with DegreeOfParallelism unresponsive dispatches in flight, the next order's command stalls exactly as before",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "state the residual honestly first, as a measured claim rather than a worry: with DegreeOfParallelism (default 8) commands concurrently stuck on unresponsive responders, the ninth order's command waits for the first loop to free, i.e. up to the same ~16.5 s worst case id 80 reduced. Reproduce that with a test that fails on today's code — N+1 gated dispatches, the (N+1)th asserted to miss a bound shorter than one full dispatch timeout — before choosing any fix",
        "the chosen answer is stated with its cost and its rejected alternatives. Candidates, none prescribed here: raise or auto-size DegreeOfParallelism; shed load by parking a command whose responder has recently timed out (a circuit breaker keyed on subject) so a dead responder stops consuming loops at all; give the sweeper a faster path for rows the fast path could not take; or accept the bound and document it. A circuit breaker is the shape #7 never needed because mergeMap gave it unbounded per-command subscription — say what #8 pays for bounding it",
        "whatever is chosen, the DEAD-RESPONDER case is the one to optimise: one subject with no responder at all should not be able to occupy N loops for N x TimeoutMs. NATS answers 'no responders' immediately for that case (see id 63's finding that this error returns without consuming wall-clock), so a dead subject and a slow subject are distinguishable and should be distinguished",
        "armed: restoring the pre-fix behaviour of whatever is chosen makes a named test fail with a message naming the stalled order and the bound it missed",
        "the record states the new worst case arithmetically, the way id 80's entry stated the old one (MaxAttempts x TimeoutMs + backoff), so the next reader can check it without re-deriving"
      ],
      "notes": "DISPOSITIONED 2026-09-14 — ACCEPTED, NOT FIXED, at the human gate, on the leader's recommendation and with the maintainer's word 'Accepted'. Status is `done` because this backlog has no `accepted` status; it means DISPOSITIONED, not implemented. WHY THIS IS NOT THE THING THE MAINTAINER OVERRULED ON 2026-09-13. That ruling rejected accepting work that was DEVELOPED INCORRECTLY, and eleven entries were re-opened and genuinely finished because of it. This is not one of those: id 88 is a STATED RESIDUAL of id 80, recorded as such in id 80's own review when it closed. Id 80 moved the worst case from 'one stuck responder stalls every order' to 'N stuck responders stall every order', which is a real and measured improvement. There is no defect here to fix. Eliminating head-of-line blocking ENTIRELY is a change to the dispatch design — a different mechanism, not a correction of this one — and the leader does not make design decisions. EVIDENCE: id 80 closed APPROVED at review round 1 with no blocking defects, the first production defect of phase 14 to do so; the bound it introduced is real, guarded and armed; and id 90 (closed 2026-09-14) has since added a composition-time fail-fast plus a guarded floor on DegreeOfParallelism, so the degenerate configurations around this bound are now loud rather than silent. RE-OPEN TRIGGER: an OBSERVED dead-responder incident in a running system, or any change that makes the dispatch degree configurable beyond what id 90 now guards. Until one of those happens this is a known, bounded and documented characteristic, not an open defect. || ORIGINAL ENTRY FOLLOWS || RE-OPENED 2026-09-13 by maintainer ruling: the 2026-09-13 dispositioning below is SUPERSEDED. The maintainer rejected accepting these and directed that phase 14 be finished and correct, not closed by disposition. Status is therefore `pending` again and the ACCEPTED, NOT FIXED note that follows is retained as history, not as the current disposition. || SUPERSEDED DISPOSITION FOLLOWS || DISPOSITIONED 2026-09-13 — ACCEPTED, NOT FIXED, at the human gate. Phase 14 was closed to scope after the audit filed more entries than it closed; the maintainer ruled that entries which do not change shipped behaviour are closed with documented evidence rather than worked. Status is `done` because the backlog has no `accepted` status — it means DISPOSITIONED, not implemented. Reason: a stated residual of id 80, not a defect in it. Id 80 moved the worst case from “one stuck responder stalls every order” to “N stuck responders stall the N+1th”, with N = DegreeOfParallelism (8). That is a real and measured improvement; the remaining bound is documented here rather than pretended away. RE-OPEN if a dead-responder incident is ever observed in practice. || ORIGINAL ENTRY FOLLOWS || Filed by the leader 2026-09-12 from advisory A8 of id 80's review, which asked for it to be routed rather than narrated. The reviewer verified no SA-n is owed: specs/shared/ prescribes nothing about dispatch concurrency. This is a genuine residual, not a defect in id 80's fix — id 80 moved the worst case from 'one stuck responder stalls every order' to 'N stuck responders stall the N+1th', which is a real and measured improvement, and the entry is the honest record that the class is bounded rather than closed."
    },
    {
      "id": 89,
      "name": "saga_dispatch_concurrency_is_exercised_everywhere_and_asserted_nowhere",
      "phase": 14,
      "title": "The parallel dispatch path now runs under all 146 Orders integration tests but is asserted only by unit tests against a fake dispatcher — exercised everywhere, guarded nowhere at integration level",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "enumerate first, as a search result with bin and obj excluded BY PATH: every Orders.IntegrationTests fixture that boots a host containing SagaCommandDispatchWorker, one classification line per hit, stating whether it overrides Dispatch options. The mitigant id 80's review recorded is that the harness does NOT override them, so all 146 run the real parallel worker — verify that is still true rather than inheriting it",
        "add at least one INTEGRATION-level assertion that the fast path dispatches concurrently against real infrastructure: two orders, one responder gated, the other order's command observed reaching 'sent' while the first is still gated — the same claim id 80 proves at unit level against a fake ISagaCommandDispatcher, but through a real host, real NATS and the real saga_commands table",
        "armed by change of KIND: reverting the worker to a single sequential loop must fail the new integration test with a message naming the blocked order and the bound, and the fixed worker must pass it every time — not 'the flake stopped'",
        "state the runtime cost the new test adds, measured, and whether it belongs in an existing collection rather than a new fixture (container startup dominates)",
        "if the conclusion is that a unit-level guard plus universal incidental exercise is sufficient, say so with the evidence and close the entry as a documented decision rather than silently dropping it"
      ],
      "notes": "RE-OPENED 2026-09-13 by maintainer ruling: the 2026-09-13 dispositioning below is SUPERSEDED. The maintainer rejected accepting these and directed that phase 14 be finished and correct, not closed by disposition. Status is therefore `pending` again and the ACCEPTED, NOT FIXED note that follows is retained as history, not as the current disposition. || SUPERSEDED DISPOSITION FOLLOWS || DISPOSITIONED 2026-09-13 — ACCEPTED, NOT FIXED, at the human gate. Phase 14 was closed to scope after the audit filed more entries than it closed; the maintainer ruled that entries which do not change shipped behaviour are closed with documented evidence rather than worked. Status is `done` because the backlog has no `accepted` status — it means DISPOSITIONED, not implemented. Reason: the parallel dispatch path IS exercised by all 146 Orders integration tests (the harness does not override Dispatch options) and IS asserted by three armed unit-level guards against a fake ISagaCommandDispatcher. What is missing is an integration-level assertion of the same claim — a duplicate proof at a more expensive level. || ORIGINAL ENTRY FOLLOWS || Filed by the leader 2026-09-12 from advisory A9 of id 80's review. All three of id 80's guards (HeadOfLineBlocking..., DegreeOfParallelism_Bounds..., and the bound test) are unit-level against a fake ISagaCommandDispatcher. The reviewer judged the mitigant real — the parallel worker is exercised by every one of the 146 Orders integration tests because the harness does not override Dispatch — but exercised is not asserted: no integration test would fail if the concurrency regressed, only the unit tests would. CLAUDE.md's standing concern is guards that cannot detect the defect they name; this is its inverse, a defect that would be detected only at the level furthest from production."
    },
    {
      "id": 90,
      "name": "degree_of_parallelism_clamp_is_unguarded_and_fails_silently",
      "phase": 14,
      "title": "Deleting the Math.Max(1, ...) clamp on DegreeOfParallelism leaves every test green, and a value <= 0 makes the dispatch worker report healthy while dispatching nothing at all",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "guard the clamp: deleting Math.Max(1, ...) in SagaCommandDispatchWorker.ExecuteAsync must fail a NAMED test, with a message naming the option and the observed degree — measured green today (id 80's review, probe P6, all 4 tests passed with the clamp removed)",
        "guard the SILENT-FAILURE shape specifically, which is what makes this worth an entry rather than a nicety: with DegreeOfParallelism <= 0, Enumerable.Range(0, 0) makes Task.WhenAll complete IMMEDIATELY, so the BackgroundService finishes successfully, the host reports healthy, and the fast path dispatches nothing for any order forever — every command then waits for the sweeper. A test must fail on that state, and the failure must name it",
        "decide and state whether an out-of-range value should clamp silently or FAIL FAST at startup. CLAUDE.md's standing rule is that DI and configuration failures must be loud at boot; a silent clamp to 1 hides an operator's mistake, while a throw makes it immediate. Whichever is chosen, the reason is recorded",
        "close advisory A5 of id 80's review in the same pass or say why not: DegreeOfParallelism is NOT bound from the environment in OrdersProgramConfiguration.ConfigureSaga (which reads KAFKA_BOOTSTRAP_SERVERS, FACT_RETRY_* and the DLQ broker). It is unreachable by misconfiguration TODAY, and the moment it is wired to an env var this becomes live — so either wire it WITH the guard, or record that it is deliberately not configurable",
        "armed: each mutation named with its verbatim failure, per id 82's rule that a message naming nothing is not evidence"
      ],
      "notes": "RE-OPENED 2026-09-13 by maintainer ruling: the 2026-09-13 dispositioning below is SUPERSEDED. The maintainer rejected accepting these and directed that phase 14 be finished and correct, not closed by disposition. Status is therefore `pending` again and the ACCEPTED, NOT FIXED note that follows is retained as history, not as the current disposition. || SUPERSEDED DISPOSITION FOLLOWS || DISPOSITIONED 2026-09-13 — ACCEPTED, NOT FIXED, at the human gate. Phase 14 was closed to scope after the audit filed more entries than it closed; the maintainer ruled that entries which do not change shipped behaviour are closed with documented evidence rather than worked. Status is `done` because the backlog has no `accepted` status — it means DISPOSITIONED, not implemented. Reason: NOT REACHABLE TODAY, and that is the whole reason: DegreeOfParallelism is not bound from the environment anywhere (OrdersProgramConfiguration.ConfigureSaga reads KAFKA_BOOTSTRAP_SERVERS, FACT_RETRY_* and the DLQ broker, never this). No operator can set it to 0. **RE-OPEN THE MOMENT IT IS WIRED TO AN ENV VAR** — with a value <= 0, Task.WhenAll completes instantly and the host reports healthy while dispatching nothing at all, which is the worst failure mode this codebase has a name for. || ORIGINAL ENTRY FOLLOWS || Filed by the leader 2026-09-12 from advisory A4 of id 80's review. The REVIEWER classified it advisory on the ground that it is not exploitable today, and that is correct as far as it goes — DegreeOfParallelism is not bound from the environment (advisory A5), so no operator can currently set it to 0. The LEADER is filing it anyway, and the reason is the shape rather than the reachability: this is a total, silent failure of a production path that leaves the host reporting healthy, which is the worst failure mode this codebase has a name for, and it is one env-var binding away from reachable. CLAUDE.md's guard-hardening theme is precisely defects that a green suite cannot see; a clamp whose deletion leaves 4 of 4 tests passing is one. Cheap to guard now, expensive to discover in a running saga."
    },
    {
      "id": 91,
      "name": "nestjs_has_accepted_operator_cancel_query_has_no_real_database_guard",
      "phase": 14,
      "title": "#7's DrizzleSagaCommandStore.hasAcceptedOperatorCancel has no test of any kind, while #8 guards its equivalent against a real database — two SagaCommandKind tokens and a sibling json column are open to a substitution that compiles, type-checks and leaves 539 tests green while the predicate answers false for every order",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "a spec in order-to-cash-nestjs/apps/orders/src instantiating the REAL DrizzleSagaCommandStore against the existing MySQL Testcontainers fixture — the harness already exists and needs no new scaffolding: saga-command-retry.integration.spec.ts:104 and saga-command-dead-letter.integration.spec.ts:119 both already construct `new DrizzleSagaCommandStore(harness.db, harness.clock)` — asserting that a row carrying a REAL fact's envelope answers false and a row carrying the SYNTHETIC orders.cancel.requested envelope answers true, mirroring #8's tests/Orders.IntegrationTests/SagaCommandStoreTests.cs:387 and :415",
        "the guard is armed by SUBSTITUTION, not deletion, because deletion is not the reachable defect here: repoint one of the two command tokens at a valid sibling (despatch.create — one of six SagaCommandKind members) and separately repoint sagaCommands.triggeringEventEnvelope at its sibling json column payload, which type-checks identically; record both verbatim failures",
        "honour the substitution family's own false negative (CLAUDE.md): if the substituted sibling produces no row at all, the test fails for the ABSENCE reason rather than the NAME reason, which is not evidence — the failure message must name what was intended to break",
        "optional second criterion, a parity observation rather than a #7 defect: #7 has no end-to-end integration coverage of the late-approval path (a credit.approved.v1 arriving after an accepted operator cancellation). Close it here or record why not. CORRECTED 2026-09-13 by id 91's implementer: this bullet ORIGINALLY said 'neither repository', which was FALSE about #8 — #8 covers it in both orderings at tests/Orders.IntegrationTests/OperatorCancelRacesSagaForwardProgressTests.cs (StockReserved_LateApproval_AfterStockReleased and StockReserved_LateApproval_BeforeStockReleased, committed in b453931), verified by the leader. The gap is #7-ONLY. The leader wrote the false half into this entry by copying the review's prose without checking #8's checkout, which is the same unresearched-premise failure CLAUDE.md names",
        "re-open trigger if this entry is dispositioned rather than fixed: the moment ANY third caller of hasAcceptedOperatorCancel is added. The TypeScript-side predicate is sufficient today only because the query is bounded to at most two rows by the (order_id, command) unique key, and that bound is what its doc-comment at drizzle-saga-command-store.ts:89-93 relies on"
      ],
      "notes": "Filed by the leader on 2026-09-13 from §3 of id 79's review, which drafted it and explicitly declined to discharge it in prose — the reviewer does not write new backlog entries. It is NOT a blocker on id 79 and id 79 closed approved with every acceptance bullet met; this is the residue. What is unguarded is precisely the two-line query between two guarded ends: the PREDICATE is armed twice (id 79's arming rows A5, A6) and the CALL SITE is armed twice (A2, A8), while the SQL between them is executed by nothing — 27 hits for hasAcceptedOperatorCancel in #7, of which one is the implementation, one the call site, one the port declaration, three are doc-comments, and every remaining hit is a fake-store member or an assertion against a fake. The consequence of the substitution is not a subtle one: the predicate answers false for every order, so a late credit.approved.v1 takes the ordinary path and returns enqueued: 'despatch.create', ordering a despatch for an order being cancelled — the exact defect SA-4 exists to prevent. This is CLAUDE.md's 'when you port a mechanism, port its guards' rule running in REVERSE, #8 -> #7: the mechanism was ported back from #8 and #8's guard was not. Worth recording for #9, because the rule has only ever been stated in the #7 -> #8 direction and a completed assessment receiving a back-port is exactly where nobody thinks to look for a dropped guard. AWAITING THE MAINTAINER'S FIX-OR-ACCEPT CALL: phase 14 was closed to scope on 2026-09-12 and this arrived after that closure, so per the stopping-rule ruling it needs a disposition rather than a status. The leader's recommendation is FIX rather than accept, for one reason only — the harness already exists, so this is one spec file against a container trio already started by two neighbouring specs, which is the cheapest entry in the backlog; the case for accepting is that #7 is a completed assessment and no shipped behaviour is wrong today."
    },
    {
      "id": 92,
      "name": "nestjs_late_credit_approval_has_no_end_to_end_coverage",
      "phase": 14,
      "title": "#7 has no end-to-end integration coverage of the late-approval path — a credit.approved.v1 arriving after an accepted operator cancellation — while #8 covers it in both orderings; the gap is one-sided and #7-only",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "an integration spec in order-to-cash-nestjs/apps/orders/src driving an order to stock_reserved, accepting an operator cancellation, then delivering a credit.approved.v1, and asserting the SA-4 behaviour end to end: exactly one credit.release row, ZERO despatch.create rows, no order.confirmed.v1, and the order not advanced",
        "cover BOTH orderings, as #8 does: the fact arriving while the order is still stock_reserved with its stock release under way, and arriving after the order is already cancelled with reason operator_cancelled",
        "#8's tests/Orders.IntegrationTests/OperatorCancelRacesSagaForwardProgressTests.cs is the SPECIFICATION of what to assert — StockReserved_LateApproval_BeforeStockReleased (:461, documented at :450-459 as the one reachable production caller of HasAcceptedOperatorCancelAsync) and StockReserved_LateApproval_AfterStockReleased (:345), both committed in b453931. Read them before writing, and classify each of their assertions as ported, deliberately not ported with the reason, or not applicable",
        "budget it honestly: this needs startSagaIntegrationHarness, whose prepareSagaFixtures starts THREE containers (startOrdersTestFixture + startKafkaTestFixture + startNatsTestFixture, saga-integration-harness.ts:178-182), creates six topics, boots the Nest app and its Kafka consumer group, and needs a stub responder set plus an order driven to stock_reserved. It is a new spec of the size of orders-cancel.integration.spec.ts, NOT an addition to an existing file",
        "this is COVERAGE, not a defect: #7's SA-4 behaviour is implemented and unit-guarded (id 79) and its store query is now integration-guarded (id 91). Nothing shipped is known to be wrong. If the entry is dispositioned rather than worked, say so with that evidence rather than implying the path is broken"
      ],
      "notes": "DISPOSITIONED 2026-09-14 — ACCEPTED, NOT FIXED, at the human gate, on the leader's recommendation and with the maintainer's word 'Accepted'. Status is `done` because this backlog has no `accepted` status; it means DISPOSITIONED, not implemented. WHY THIS IS NOT THE THING THE MAINTAINER OVERRULED ON 2026-09-13: this is COVERAGE, not a defect. Nothing shipped is wrong. #7's SA-4 behaviour is implemented and unit-guarded (id 79, APPROVED first round) and its store query is integration-guarded against a real database (id 91, APPROVED first round). The gap is one-sided and #7-only: #8 covers the late-approval path END TO END IN BOTH ORDERINGS at tests/Orders.IntegrationTests/OperatorCancelRacesSagaForwardProgressTests.cs — StockReserved_LateApproval_BeforeStockReleased (:461) and StockReserved_LateApproval_AfterStockReleased (:345), committed in b453931 and verified by the leader. COST OF CLOSING IT, measured rather than guessed: it needs startSagaIntegrationHarness, whose prepareSagaFixtures starts THREE containers (saga-integration-harness.ts:178-182), creates six topics, boots the Nest app and its Kafka consumer group, and needs a stub responder set plus an order driven to stock_reserved — a new spec the size of orders-cancel.integration.spec.ts, in a COMPLETED assessment. The parity claim this trilogy makes is that the CODE is equivalent, and it is. Identical test-suite SHAPES were never claimed. A LEADER ERROR IS RECORDED IN THIS ENTRY'S ACCEPTANCE BULLET 4 AND STAYS THERE: the bullet originally said the gap existed in BOTH repositories, which was false about #8. The leader wrote it by copying the id 79 review's prose without opening #8's checkout — the unresearched-premise failure CLAUDE.md names — and id 91's implementer caught it. RE-OPEN TRIGGER: any change to #7's SA-4 saga path, for any reason. The unit guards pin the handler's decisions; nothing in #7 pins the wire-level consequence, so a change there would be unguarded end to end. || ORIGINAL ENTRY FOLLOWS || Filed by the leader on 2026-09-13, routed from the review of id 91, which named it rather than discharging it in prose (the reviewer does not write new backlog entries). ORIGIN, AND A LEADER ERROR WORTH KEEPING: id 91's acceptance bullet 4 originally described this as a gap in BOTH repositories. That was false about #8, and the leader wrote it by copying the review of id 79's prose without opening #8's checkout — the unresearched-premise failure CLAUDE.md names, committed by the person who maintains the rule. Id 91's implementer caught it and the leader verified the correction against b453931. The bullet was corrected in place in id 91 rather than quietly rewritten, so the error stays legible. The gap is #7-ONLY. WHY IT WAS NOT CLOSED INSIDE ID 91: id 91's own scope bound was one spec file against one MySQL container, and this needs three containers and a full saga boot — closing it there would have expanded a test-only entry into a feature, which is the shape phase 14 was closed to scope for. AWAITING THE MAINTAINER'S FIX-OR-ACCEPT CALL, like id 91 before it: phase 14 closed to scope on 2026-09-12 and this arrived after. The leader's recommendation is ACCEPT WITH EVIDENCE rather than fix, and this is the opposite recommendation to id 91's for a concrete reason: id 91 guarded a query that NOTHING executed and whose substitution hazard was live, for one container and 11 seconds of runtime; this one re-proves, through three containers, behaviour that is already guarded at the unit level in #7 and already proven end-to-end in #8. The parity claim the trilogy exists to make is about the CODE being equivalent, and it is; the test-suite shapes were never claimed identical. RE-OPEN TRIGGER if accepted: the moment #7's SA-4 saga path is changed again for any reason, since the unit guards pin the handler's decisions and nothing in #7 pins the wire-level consequence."
    },
    {
      "id": 93,
      "name": "orders_and_catalog_rpc_payloads_are_still_duplicated_between_orders_and_the_gateway",
      "phase": 15,
      "title": "The orders.* and catalog.* RPC payload records are still defined twice — src/Orders/Presentation/Rpc/ and src/Gateway/Application/Rpc/GatewayRpcPayloads.cs — because id 84 found no src/Contracts/Rpc counterpart to unify them onto",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "enumerate FIRST as a search result, bin and obj excluded BY PATH: every orders.* and catalog.* RPC payload record in src/Orders/Presentation/Rpc/ and in src/Gateway/Application/Rpc/GatewayRpcPayloads.cs, matched by NAME and by PROPERTY SET AND TYPE, one classification line per record — identical, field-different (state the difference and whether it is deliberate), or one-sided. Id 84's review re-derived the Gateway population as 29 records with a 17 unified / 10 Gateway-only / 2 field-different split; the 10 Gateway-only are this entry's population, not a sample",
        "decide and record whether these move to src/Contracts/Rpc (making them wire contract) or stay deliberately separate — this is a DESIGN decision about what Contracts owns, not a mechanical dedup, and it is the maintainer's call, not the implementer's",
        "the wire cannot move: whichever way it is decided, the golden-envelope and BC23 parsed-from-the-spec tests plus the Gateway's own HTTP payload tests are RUN and named with their counts, byte-identity asserted rather than assumed",
        "armed by substitution and by addition, not deletion alone: an undeclared property on a unified record must fail a named test naming the property, and a record name substituted for a real sibling must fail a named test naming what was broken",
        "PORTED-IDIOM LEDGER ROW, verified by id 84's reviewer and the reason this entry is worth keeping for #9: #7 has NO such duplication because its payload types are GENERATED into packages/contracts/src/generated/ and drift-checked by packages/contracts/scripts/check.mts and check.spec.ts — consumed directly at apps/fulfillment/src/application/ports/stock-read.port.ts:3 and apps/gateway/src/application/queries/list-stock.query.ts:9. #8 hand-writes its contract types, so the property #7 gets from code generation is supplied here by nothing, and duplication is the observable consequence. Carry this row wherever the entry lands"
      ],
      "notes": "Filed by the leader on 2026-09-13 from batch D1: the implementer asked for it and id 84's reviewer asked again, both correctly, since neither may write backlog entries. **DELIBERATELY FILED AS PHASE 15, NOT PHASE 14.** The maintainer froze phase 14 at 13 items after an audit phase that filed findings faster than it closed them, and 'finish an item, discover a consequence, grow the phase' is the exact pattern the freeze exists to stop. Phase 14's count is unchanged by this entry and its completion check is unaffected. NOT A DEFECT: nothing shipped is wrong, the duplication is a maintenance cost, and id 84's review confirmed both OrdersCancelReplyPayload definitions are now guarded. The valuable half is the ledger row in acceptance bullet 5 — a genuine engine-supplied property (#7's code generation) that #8 replaces with hand-writing, which is exactly the ported-idiom class CLAUDE.md says nothing else in this harness can see, and it is the half #9 should inherit. || DESIGN DECISION MADE 2026-09-15 by leader: move to src/Contracts/Rpc, following id 84's exact precedent (Contracts owns the wire shape; a record with no counterpart gets one created rather than staying duplicated). Not treated as open — user directed immediate fixes over deferral. || CLOSED 2026-09-15 by leader (Sonnet 5 session): moved 10 Gateway-only orders.*/catalog.* records into new src/Contracts/Rpc/OrdersRpcPayloads.cs and CatalogRpcPayloads.cs, following id 84 precedent. Deleted 3 now-empty Orders Presentation/Rpc files, retargeted callers in both Orders and Gateway. Wire tests run green: Contracts 24/24, Orders.UnitTests 500/500, Gateway.UnitTests 237/237, Gateway HTTP payload 11/11, Architecture 50/50, Orders acceptance 24/24. Armed (undeclared property + sibling substitution), both named and restored. Ledger row written. Leader verified independently: full solution build 0 warnings/0 errors, 11 unit-tier projects green (~1467 tests, 0 failed). progress/impl_phase15_batch2_payload_unification_and_offset_guards.md"
    },
    {
      "id": 94,
      "name": "saga_consumption_so9_recorded_arm_no_longer_kills_its_own_test",
      "phase": 15,
      "title": "SagaConsumptionTests.SO9's recorded F6 arm (EnableAutoOffsetStore false -> true) SURVIVES today: OR1's FactRetryDispatcher keeps the handler exception inside ConsumeAsync, so gate.Attempts >= 2 is satisfied by an in-process retry rather than the Kafka redelivery the test's name claims",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "reproduce first: re-run the F6 mutation (EnableAutoOffsetStore = false -> true) against SagaConsumptionTests.SO9 and record the verbatim result, confirming it PASSES where its arming record says it must fail — a recorded arm is a countable claim and this one is now false",
        "state the mechanism precisely and prove it rather than asserting it: the claim is that OR1's FactRetryDispatcher catches the handler's exception inside ConsumeAsync, so the consumer never dies, Close() never commits the prematurely stored offset, and the gate's second attempt comes from the in-process retry loop rather than from a Kafka redelivery. Show which of those links actually holds",
        "decide what the test should assert now, and this is the real question: SO9's NAME claims Kafka redelivery. Either the assertion is strengthened so it can only pass on a genuine redelivery (distinguishing it from an in-process retry), or the test is renamed to say what it actually proves. A test whose name claims more than its assertion can see is the defect class phase 14 exists to retire, so do not close this by renaming alone without saying why",
        "enumerate the class, not the instance, as a search result with bin and obj excluded BY PATH: every recorded arm in progress/ whose mutation targets consumer offset-store or offset-commit configuration, one classification line per hit, stating for each whether OR1's in-process retry could satisfy its gate the same way. The instance named here is where the class was NOTICED, never where it ends",
        "armed: the corrected assertion must FAIL under the F6 mutation with a message naming what was broken, and must pass on a forced --no-incremental rebuild once restored"
      ],
      "notes": "Filed by the leader on 2026-09-13 from batch D2, which found it while working id 82 — and it was found only because id 82's bullet 3 required RE-RUNNING recorded arms rather than re-reading them. That is the entry earning its keep in a way nobody predicted: re-reading the record would have confirmed the arm forever. **DELIBERATELY FILED AS PHASE 15, NOT PHASE 14.** The maintainer froze phase 14 at 13 items, and 'finish an item, discover a consequence, grow the phase' is the exact pattern the freeze exists to stop; phase 14's count and completion check are unaffected by this entry. SEVERITY: this is a guard-that-does-not-guard, the class phase 14 was convened for, but it is a TEST defect and not a production one — SO9 still passes, the consumer still retries, and no shipped behaviour is known to be wrong. What is lost is the evidence that Kafka redelivery specifically works, which is what the test's name promises. Related: id 82 (the arming-message entry that found it) and the CLAUDE.md rule that a countable claim is not done until it has been seen to fail — this arm WAS seen to fail once, and the ground moved underneath it afterwards, which is a failure mode that rule does not yet cover. || CLOSED 2026-09-15 by leader (Sonnet 5 session, verified directly): mechanism proved (FactRetryDispatcher swallows the handler exception in-process, so the old single-read committed-offset check and gate.Attempts were both blind to F6), assertion strengthened to a polling window bracketing auto.commit.interval.ms, test renamed to SO9_AHandlerThatThrows_LeavesTheCommittedOffsetUnchangedUntilItSucceeds (Kafka redelivery specifically is now architecturally unreachable by OR1 design, so that clause was dropped rather than falsely kept). Both F6 mutations now fail correctly; re-run by leader, 2/2 green. src/ confirmed clean (no permanent change). DISCLOSED, NOT FIXED (out of this entry scope): Projector.OffsetContractTests.PR38 has the same shape and was never armed against this mutation; Orders lacks the reflection-based config unit guard that Notifications/Projector both have. Candidates for a future entry, not filed — see progress/impl_id94_so9_dead_arm.md."
    },
    {
      "id": 95,
      "name": "cold_nats_connection_loses_a_reply_in_orders_stock_availability_tests",
      "phase": 15,
      "title": "The cold-connection shape id 81 diagnosed in the Gateway also exists at tests/Orders.IntegrationTests/NatsStockAvailabilityCheckerTests.cs:111-113 — a NatsConnection used for a concurrent pair without ConnectAsync() first, which loses a reply regardless of the request timeout",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "reproduce the shape before fixing it, as id 81 did: id 81's diagnosis measured 8 failures in 608 COLD rounds against 0 in 550 rounds where the connection was established first, with the losing call's simultaneous twin completing in 2-8 ms and the responder having received BOTH requests. Show the same signature here, or show it does not reproduce and say why the site is nevertheless worth changing",
        "the fix id 81 landed is one line — an explicit ConnectAsync() before the concurrent pair — plus a precondition assertion that names the budget and the offending state when it is violated. The record at progress/impl_batch_d3_container_port_and_rpc_budget_robustness.md gives it verbatim; port it rather than inventing a second shape",
        "proved by a change of KIND, not of probability, with the determinism in the TEST and not in the mutation: a phase-14 brief already got that backwards and a plain defect passed 3/3",
        "armed: removing the ConnectAsync() must fail a named test with a message naming what was lost — per CLAUDE.md's arming rule, a failure whose message names nothing is not evidence",
        "enumerate the CLASS, not this instance, as a search result with bin and obj excluded BY PATH: every test that issues concurrent NATS requests over a connection it has not explicitly established, one classification line per hit. This site is where the class was NOTICED, never where it ends — and id 81's own scope stopped at the Gateway, which is why this entry exists"
      ],
      "notes": "Filed by the leader on 2026-09-13 from batch D3. The implementer found it while diagnosing id 81 and DELIBERATELY did not fix it, because it sits outside id 81's stated scope — the correct call, and it reported the site with the one-line fix attached rather than silently widening its own brief. **DELIBERATELY FILED AS PHASE 15, NOT PHASE 14.** The maintainer froze phase 14 at 13 items; phase 14's count and completion check are unaffected by this entry. WHY IT MATTERS MORE THAN A FLAKE: id 81's diagnosis disproved its OWN entry's premise. The failure is not a too-tight budget — one round lost a reply under a 120000 ms budget on an idle machine, and the losing call's simultaneous twin completed in 2-8 ms while the responder had received both requests. No amount of machine contention produces that. So raising a timeout at this site would hide it forever, which is precisely what id 81 bullet 5 forbade. Related: id 81 (the Gateway instance, fixed) and CLAUDE.md's rule that a retry budget counted in attempts assumes each attempt costs time. || CLOSED 2026-09-15 by leader (Sonnet 5 session, verified directly given phase-15 budget pressure — targeted test re-run green, class enumeration checked, no src/ change): ported id 81's ConnectAsync()+precondition fix; armed and verified; enumerated class repo-wide, no other live instance found. progress/impl_id95_orders_cold_nats_connection.md"
    },
    {
      "id": 96,
      "name": "gateway_ignores_gateway_port",
      "phase": 16,
      "title": "The Gateway never reads GATEWAY_PORT: .env.example declares it (3001), #7's Gateway reads it (apps/gateway/src/main.ts:30), and #8's binds Kestrel's default instead — found by phase 16's live verification, which had to bind it through ASPNETCORE_URLS",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "GatewayProgramConfiguration reads GATEWAY_PORT, defaulting to 3001 exactly as #7 does (apps/gateway/src/main.ts:30: Number(process.env.GATEWAY_PORT ?? 3001)), and the host listens on it",
        "guarded the way the five sibling *ProgramConfigurationTests already guard their own ports: a default test, and a SUBSTITUTION test that sets GATEWAY_PORT and its sibling port variables to distinct non-default values and fails, naming the key, if the Gateway reads the wrong one — the mutation family CLAUDE.md says a sibling-named variable requires",
        "no in-process test host collides on the real port: every existing Gateway integration suite and tests/Gateway.IntegrationTests/SagaEndToEndVerificationTests.cs still passes, run and counted",
        "scripts/dev-stack.sh no longer needs the ASPNETCORE_URLS workaround, and its comment saying the Gateway does not read GATEWAY_PORT is removed rather than left stale",
        "armed: deleting the read, and substituting a sibling key, each fail a named test with a message naming the variable",
        "the class was enumerated before filing: every variable .env.example declares was matched against every file that reads it, and GATEWAY_PORT is the ONLY service setting read by no src/*.cs file — the others read only by compose, the MS-SQL/Kafka entrypoints or the n8n workflows are legitimately consumed there. Phase 13's env-read guards could not see this, because they guard variables that ARE read; a declared variable that nothing reads is invisible to them"
      ],
      "notes": "Filed and dispatched for an immediate fix on 2026-09-16 by the leader, from the phase-16 web-app implementer's live verification. Filed for the record, not deferred: the maintainer's standing direction is that a detected issue is fixed on detection. A ported-idiom miss from phase 13 — #7's Gateway read its port from the environment and #8's Gateway silently did not, so the declared setting had no effect and nothing failed."
    },
    {
      "id": 97,
      "name": "sa5_money_formatting_names_a_field_no_response_carries",
      "phase": 16,
      "title": "specs/shared/openapi.yaml says clients format money 'from currency.decimalPoints', but every currency on the REST wire is a bare ISO 4217 code and decimalPoints travels only on the internal NATS catalog.reference.list reply — the shared specification names a source no client can reach",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "HUMAN-GATED: specs/shared/ is read-only without a spec amendment, applied byte-identically to #7 and #8, committed on its own, with its history section and README registry row. Nothing below is applied before the maintainer approves SA-5",
        "PROPOSED SA-5 TEXT — openapi.yaml info.description, the Money section, line 56 today reads 'Formatting for humans happens in the client, from `currency.decimalPoints`.' Replace it with: 'Formatting for humans happens in the client, from the currency's ISO 4217 minor-unit exponent (EUR, GBP and USD are 2; JPY is 0; BHD is 3). No response carries that exponent: it is a property of the currency code itself.' No schema, no path and no wire shape changes",
        "after applying, each repository's own contract-type regeneration and drift check run and are named with their result — a description change may or may not reach generated type comments, and neither #7's packages/contracts check nor #8's apps/web types:check may be assumed unaffected",
        "#8 conformance is ALREADY met: apps/web/src/lib/money.ts reads the exponent from ISO 4217 via Intl and never assumes 2 — cite the guard that proves a non-2-decimal currency formats and parses correctly",
        "#7 alignment: #7's apps/web/app/lib/money.ts divides by 100 (a hard-coded 2-decimal exponent). It produces no wrong output today, because #7 seeds only EUR, GBP and USD (all 2-decimal), but under SA-5 it is non-conforming for any other currency. Align it and guard it with a 0- and a 3-decimal case, tracked here as ids 75 and 79 tracked earlier #7 alignments"
      ],
      "notes": "SA-5 APPROVED by the maintainer on 2026-09-17 and APPLIED by the leader the same day: specs/shared/openapi.yaml byte-identical in #7 and #8 (md5 fa261d4cf12aa3b373cb777aaa79b2e1), both parse, #8 init.sh parity OK; #7 contracts:check OK and #8 apps/web types:check OK (generated types unchanged); #8 Gateway.UnitTests 245/245 incl. OpenApiContractTests; README registry row and a history section in BOTH repositories written. Acceptance bullets 1-4 are therefore met; bullet 5 (#7's money.ts alignment) is the remaining work, dispatched. || Filed by the leader on 2026-09-16 from phase 16: the web-app implementer found it, and the phase-16 review asked for it to be routed as a numbered entry and a written proposal rather than left in prose — CLAUDE.md's rule for any disclosure whose root cause is specs/shared/. Verified before filing: openapi.yaml:56 names currency.decimalPoints; the REST schemas carry currency only as $ref CurrencyCode; decimalPoints exists in #8 only on CurrencyViewPayload (src/Contracts/Rpc/CatalogRpcPayloads.cs:51), an internal RPC shape; #7's money.ts comment acknowledges the same gap and divides by 100; both repositories seed only EUR, GBP and USD, so no wrong output exists today. Checked whether #7 solved it without an amendment, as CLAUDE.md requires before proposing one: it did not — it hard-coded the common case. The leader's recommendation is the smallest amendment: one sentence of prose, naming the source that actually exists (ISO 4217), with no wire change. The alternative — exposing decimalPoints on the REST wire — changes the contract in both repositories to carry a value the currency code already determines. AWAITING THE MAINTAINER'S DECISION on SA-5."
    },
    {
      "id": 98,
      "name": "expired_session_on_a_finished_order_shows_connection_lost",
      "phase": 16,
      "title": "On a finished order's page an expired session shows 'Connection lost' with a Retry button that loops on the refusal, instead of sending the user to sign in",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "an expired session on the order-detail page of a TERMINAL order ends at /login — the same outcome a 401 produces everywhere else in the app (src/app/providers.tsx redirectOnSignedOut) — rather than at a 'Connection lost' state",
        "the 'Retry connection' action never loops on a refusal: when the stream is refused, what the user is shown and offered names the actual state",
        "the non-terminal case is unchanged and still covered, and the existing reconnect guarantees (Last-Event-ID resume, per-type de-duplication, one stream per page, stream closed on leave) all still pass",
        "tested with the refusal held deterministically in the test, not by timing; armed by removing the fix, with a failure message naming the terminal order and the missing redirect",
        "no generic text is introduced: whatever the page shows passes the behavioural sweep's reviewed-label check"
      ],
      "notes": "Filed and dispatched for an immediate fix on 2026-09-17 by the leader, from advisory A1 of id 29's review round 4, which carried it to phase 19 as a disclosed limit and left filing to the leader. The maintainer's standing direction is that a detected defect is fixed on detection, and this is a live defect in today's code, not a coverage shape with no instance. Mechanism, as the review traced it: order-stream-client.ts:105-111 turns a refused stream into gave-up; order-detail-view.tsx:23,101-104 then shows 'Connection lost' with a Retry button that only reopens the stream (use-order-stream.ts:78); the detail query polls only while the order is non-terminal (use-order-detail.ts:59-70), so on a terminal order no query fails with 401 and nothing redirects. EventSource exposes no response body or status, so the stream alone cannot tell a refusal from a network drop — the fix has to ask something that can."
    },
    {
      "id": 99,
      "name": "web_apps_name_their_stack",
      "phase": 16,
      "title": "Neither web app says which assessment it is: #7 (NestJS + Nuxt) and #8 (.NET + Next.js) look alike in a browser and both default to port 3000, so a user cannot tell which stack answered",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "#8's web app shows the label '#8 · .NET / Next.js' in the header of every signed-in page and on the login page, and its browser-tab title names the stack",
        "#7's web app shows '#7 · NestJS / Nuxt' in the same three places — tracked here because #7 is a completed assessment with no open backlog of its own, as ids 75, 79 and 91 were",
        "the label is one definition per app, not text repeated in several files, so it cannot drift within an app",
        "a test in each app fails if the label is missing from the signed-in header, the login page or the tab title; armed by removing it",
        "in #8, the label passes the behavioural error sweep's reviewed-label check and changes no error text"
      ],
      "notes": "Requested by the maintainer on 2026-09-17: 'is it easy to know when we are in the web if we are in the #7 or in the #8? If it is not, could we add a label like NestJS/Nuxt and .NET/Next'. It is not easy: both apps default to port 3000 and share page structure. Work in both repositories runs in parallel with id 97's #7 alignment, because the two repositories share no build; the status stays pending while it runs only because init.sh allows one in_progress entry."
    },
    {
      "id": 100,
      "name": "timeline_money_reads_as_minor_units",
      "phase": 16,
      "title": "The order timeline shows money as raw minor units — 'Credit hold of 9 245 EUR approved' for a €92.45 order — so a human reads a hundred times the real amount; #7 renders it the same way, and the seed writes '16130 EUR' ungrouped",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "the Projector's summary builders render every amount with its currency's ISO 4217 minor-unit exponent: 9245 EUR → '92.45 EUR', 5000 JPY → '5 000 JPY' (exponent 0), 12345 BHD → '12.345 BHD' (exponent 3); integer arithmetic only, no floating point, no decimal in domain code, no culture-dependent separators, thousands still grouped by a single ASCII space",
        "the seed's timeline fixtures render amounts with the same rule, so a seeded order and a projected order read alike (the design's recorded 'seed writes 16130 EUR' difference is closed rather than carried)",
        "the same change in #7 (apps/projector and its seed), tracked here as ids 75, 79, 91 and 97 were",
        "PR16's test asserts whole strings for EUR, a 0-exponent and a 3-exponent currency, and a negative and a large value; armed by forcing the exponent to 0 and to 2, and by replacing the integer split with a floating-point division",
        "specs/projector_read_model/design.md's summary table and PR16 note are updated in both repositories; specs/shared/ is not touched (PR16's own text already requires integer minor units and no conversion, which the new rendering keeps)"
      ],
      "notes": "Found by the maintainer on 2026-09-17 in a phase-16 screenshot of ORD-000025's timeline. Both stacks render it this way deliberately (#7 apps/projector/src/domain/money-format.ts:11-16; #8 src/Projector/Domain/MoneyFormat.cs), but PR16 requires only integer minor units and no conversion, not an unscaled display. SA-5 now says human formatting uses the ISO 4217 exponent, so this is the same rule applied to server-written human text. Fixed at once, per the maintainer's standing instruction. Existing read-model documents keep their old text until re-projected or re-seeded."
    },
    {
      "id": 101,
      "name": "stock_page_repeats_the_product_code",
      "phase": 16,
      "title": "The Stock page shows 'PRD-0001 (PRD-0001)' on every row: productName is optional in StockItem and no backend fills it, so both web apps fall back to the code and then repeat it",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "each stock row shows the product's name from the catalog the app already reads (GET /catalog/products), with the code beside it; a product with no known name shows its code once, never twice",
        "a StockItem.productName, if a backend ever sends one, still wins over the catalog",
        "a catalog failure does not hide the stock table: rows fall back to the code alone, and #8's behavioural error sweep still passes, or is extended if the new request joins the stock page's population",
        "the same change in #7's stock page",
        "tests in each app cover name-from-catalog, the code-only fallback with no duplicate, and productName precedence; each is armed"
      ],
      "notes": "Found by the maintainer on 2026-09-17 in a phase-16 screenshot. specs/shared/openapi.yaml StockItem.productName is optional (not in required), and neither #8's src/ nor #7's backend apps populate it: Fulfillment owns stock by product code only. #8 apps/web/src/features/stock/stock-view.tsx:127 and #7 apps/web/app/pages/stock/index.vue:186 render `productName ?? productCode` followed by `(productCode)`. Fixed at once, per the maintainer's standing instruction."
    },
    {
      "id": 102,
      "name": "problem_detail_money_reads_as_minor_units",
      "phase": 16,
      "title": "A problem document's human-readable detail can carry money in raw minor units — INVOICE_PAYMENT_AMOUNT_MISMATCH says 'Payment amount 9245 does not equal the invoice's totalAmount …' and the web app shows that detail verbatim; both repositories",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "every domain or RPC error whose message can reach a problem document's `detail` and names an amount renders it with the shared money-text formatter (id 100): '92.45 EUR', never '9245'",
        "the class is enumerated first, in both repositories, as a search result: every error message with an amount, each classified as reaching a human (fix it) or not (state the path that stops it)",
        "machine-readable fields are unchanged: `code`, and any structured amount field, stay integer minor units",
        "a test per fixed site asserts the whole detail string through the path a user sees (at least RPC error → Gateway problem document in #8), and each is armed",
        "the same change in #7 (apps/billing/src/domain/invoice-errors.ts:71 and any sibling), tracked here as ids 97 and 100 were"
      ],
      "notes": "Filed from review_timeline_money_and_stock_names.md D6 (2026-09-17): src/Billing/Domain/Errors/InvoicePaymentAmountMismatchError.cs:7 → src/Billing/Presentation/Rpc/BillingErrorMapper.cs:95-98 → src/Gateway/Presentation/Problem/ProblemJsonMiddleware.cs:95 → apps/web/src/lib/problem.ts:23. Same class as id 100 (human-facing money text), fixed in the same round under the stopping rule recorded in progress/current.md."
    },
    {
      "id": 103,
      "name": "web_currency_exponent_is_cldr_not_iso4217",
      "phase": 16,
      "title": "Both web apps take the currency exponent from Intl (CLDR display digits), not ISO 4217 — for HUF, IDR, COP, IQD and others they disagree with the backends' ISO table, so the web formats and PARSES those amounts with the wrong number of decimals",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "#8 apps/web/src/lib/money.ts and #7 apps/web/app/lib/money.ts take the exponent from the same ISO 4217 table the backends use (id 100): the non-2 set is identical everywhere, including UYI = 0",
        "the table is written once per repository where the build allows it (#7's web may import @otc/shared-kernel); where it cannot (#8's web cannot import .NET), a test reads the backend table and fails on any difference",
        "parsing and formatting both follow the table: 12345 HUF formats as '123.45'-style with 2 decimals, and a HUF input accepts 2 decimals, and the input step follows it",
        "tests assert HUF, IQD, JPY, BHD and EUR through format, parse and the input step, each armed; the whole-table parity guard is armed with a one-row corruption on each side",
        "SA-5's text ('ISO 4217 minor-unit exponent') is unchanged and now true of every client"
      ],
      "notes": "Filed from review_timeline_money_and_stock_names.md D7 (2026-09-17). Predates id 100: #8's web used Intl since phase 16 (id 29), and id 97 aligned #7's web to Intl. Intl's maximumFractionDigits is CLDR's display precision, which differs from ISO 4217 for about 15 codes (IQD 0 vs 3; HUF, IDR, COP 0 vs 2). Same class as id 100, fixed in the same round."
    },
    {
      "id": 104,
      "name": "dead_letter_producer_defaults_to_localhost",
      "phase": 18,
      "title": "Each service's dead-letter producer has its own bootstrap default ('localhost:9092'), independent of Kafka.BootstrapServers, so an in-process test host that points Kafka at a container still dead-letters to whatever broker is on localhost:9092",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "DeadLetterKafkaOptions.BootstrapServers in Orders, Notifications and Projector falls back to the service's own Kafka.BootstrapServers when not set explicitly, so configuring Kafka once configures both clients; production configuration (the *ProgramConfiguration files) is unchanged in effect",
        "a test per service proves the fallback: a host configured with only Kafka.BootstrapServers dead-letters to that broker; armed by restoring the independent localhost default",
        "the test hosts enumerated on 2026-09-17 (about twenty files under tests/ that call *Host.CreateBuilder without setting DeadLetter.BootstrapServers) are re-checked against the enumeration command, and any explicit workaround made redundant is left or removed deliberately, with the reason recorded",
        "the same question is asked of #7, citing its checkout either way"
      ],
      "notes": "CLOSED 2026-09-17 as a LIGHT change (CLAUDE.md Cost discipline): the leader read the diff and re-ran Orders/Projector/Notifications unit tests (503/121/114), Architecture.Tests 50 and dotnet format, and re-armed the Projector fallback (localhost default restored → DeadLetterBootstrapServersFallback fails 'Expected projector-kafka-only.example:9092 / Actual localhost:9092'; restored, cmp, green). Record: progress/impl_dead_letter_producer_defaults_to_localhost.md. Found by the phase-16 wrap-up's quality gate on 2026-09-17. SagaEndToEndVerificationTests.Criterion1 failed twice with the developer's infrastructure down: the fleet's Projector dead-lettered Criterion4's poison to localhost:9092 (connection refused), which stalled its consumer. The test had passed in phase 15 only because a developer Kafka was listening there, so dead letters silently went to the developer broker. The wrap-up fixed that one test (StartProjectorAsync now sets DeadLetter.BootstrapServers; Criterion4 now selects each consumer's DLQ record and asserts that the Projector's also arrives, armed by removing the setting). Disposition: fix in phase 18. The class is latent: the other hosts that leave the default never dead-letter in their tests. It is not worked during the wrap-up, per the maintainer's instruction that the wrap-up comes first."
    },
    {
      "id": 105,
      "name": "causal_order_check_is_vacuous_without_causation_id",
      "phase": 18,
      "title": "BlackBoxApiTests.AssertCausalOrder checks nothing if causationId ever stops reaching the timeline wire — every entry would lack a cause, and the ordering assertion would pass on zero pairs (the same gap as #7's review N7)",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "AssertCausalOrder fails when the timeline it receives carries no causationId pairs, or fewer than the happy path's known minimum; the failure message names the missing pairs",
        "armed once by stripping causationId from the entries the helper reads"
      ],
      "notes": "CLOSED 2026-09-17 as a LIGHT change: the helper now counts the edges it checks and fails if fewer than the minimum (happy path: 3, measured on the real 9-entry timeline); a new fleet-free meta-guard covers the no-causationId case. The implementer armed it by removing the Gateway's causationId mapping (MongoOrderReadModel.cs:132): the happy-path fact failed with 'no causal edges were checked', and the file was restored and verified with cmp. The leader verified that the src file matches HEAD, re-ran both meta-guards (2/2) and read the helper; the happy path runs in the wrap-up's quality.sh. Record: progress/impl_causal_order_check_is_vacuous_without_causation_id.md. Filed from progress/review_api_tests.md N1 (2026-09-17, id 31 approved). LIGHT change under CLAUDE.md Cost discipline (test-only). Disposition: fix in phase 18's next session if the maintainer agrees; it does not block id 31. #7 carries the same gap (its review N7) and is not mirrored, per the #7-is-complete rule."
    },
    {
      "id": 106,
      "name": "place_order_line_key_is_module_scope_mutable_state",
      "phase": 19,
      "title": "The order-line label htmlFor and its <select> id can disagree, because both derive from a module-scope mutable counter (nextLineKey) that free-runs across every SSR render a long-lived next start process serves, while the browser recomputes it from 1 on every hard navigation",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "the per-line product select's id and its label's htmlFor always agree, on the FIRST render after any number of prior server-side renders in the same process, not only on a freshly restarted process",
        "the key is derived from something request- or render-scoped (e.g. React's useId()) rather than a module-level free-running counter, so it cannot leak state across unrelated requests in the same long-lived server process",
        "a test proves it survives repeated renders: render the form N times in the same module instance (or otherwise force multiple SSR passes without a process restart) and assert the label/select ids still match on each one",
        "getByLabel('Product') / getByRole('combobox', { name: 'Product' }) resolve correctly once fixed; apps/web/e2e/happy-path.spec.ts and compensation.spec.ts may then be simplified back from the structural locator (getByTestId('order-line').locator('select')) they were written to use instead, if the leader judges that worthwhile at the time"
      ],
      "notes": "Found by id 32's implementer while porting the Playwright suite (2026-09-17), confirmed live twice against a running dev-stack.sh: for=\"product-6\"/id=\"product-2\" on one load, for=\"product-7\"/id=\"product-2\" on the next. apps/web/src/features/orders/place-order-form.tsx:32-35, let nextLineKey = 1; incremented by emptyLine(). Does not block id 32's acceptance bullets (a mouse-driven operator is unaffected; the e2e tests route around it with a structural locator) and breaks label-click-to-focus and real assistive technology independent of Playwright. LIGHT change under CLAUDE.md Cost discipline (a component-local id-generation fix): one implementer, leader checks the diff and re-runs the affected Vitest/Playwright tests. Re-open trigger named in acceptance: any test that starts relying on getByLabel/getByRole name matching for a per-line field. FIXED 2026-09-18 (phase 25 close-out): the per-line id pair now derives from React useId() inside a new OrderLineRow subcomponent, request/render-scoped rather than a module-scope free-running counter; the reconciliation key moved to a useRef. Armed: reverted to the old counter, the new hydration-boundary Vitest test failed naming the missing combobox, restored and reconfirmed green. 18/18 file tests, 287/287 full web suite, typecheck and eslint clean. e2e locators left as structural (compensation.spec.ts already was; happy-path.spec.ts kept, only its stale comment corrected) -- reverting them needs a real docker-stack run, judged disproportionate to this LIGHT fix. Leader independently re-ran the file-level Vitest suite: 18/18. Full record: progress/impl_place_order_line_key_is_module_scope_mutable_state.md."
    },
    {
      "id": 107,
      "name": "dotnet_sonarscanner_has_no_local_tool_manifest",
      "phase": 21,
      "title": "dotnet-sonarscanner is a global machine-level dotnet tool with no .config/dotnet-tools.json manifest pinning its version in this repository, so `pnpm run sonar:scan` fails with 'command not found' on a machine that has not separately installed it",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "a `.config/dotnet-tools.json` local tool manifest pins dotnet-sonarscanner to a specific version",
        "`dotnet tool restore` installs it locally without a prior global install, and `sonar:scan` (or its README instructions) says to run that first",
        "the pinned version is confirmed compatible with the SonarQube server version this repository already runs (2.36.2's image, or whatever docker-compose.infra.yml pins by the time this is worked)"
      ],
      "notes": "Found during id 34's work (2026-09-18): SonarQube is optional infrastructure, so this is not blocking, but a fresh checkout's `sonar:scan` currently depends on an undocumented global tool install. LIGHT change under CLAUDE.md Cost discipline (build tooling config, no saga/money/security touch). Disposition: fix in a future session; not required to close phase 21. FIXED 2026-09-18 (phase 25 close-out, leader-direct root-config edit): .config/dotnet-tools.json created, pinning dotnet-sonarscanner to 11.2.1 (the version already proven live against SonarQube 26.8.0 in id 34s work). dotnet tool restore confirmed live to install locally; dotnet dotnet-sonarscanner confirmed live to resolve the pinned local version without relying on this machines pre-existing global install. package.jsons sonar:scan script updated to run dotnet tool restore first and invoke via dotnet dotnet-sonarscanner (SDK local-tool dispatch) instead of the bare global command."
    },
    {
      "id": 108,
      "name": "domain_editorconfig_var_rule_is_unenforced",
      "phase": 21,
      "title": ".editorconfig declares csharp_style_var_elsewhere = false (\"explicit types make domain code read like the spec\") at severity :suggestion, so it is never a build warning or error and dotnet format --verify-no-changes ignores it -- the convention is stated but not enforced, and the codebase violates it ~1,900 times (373 in src/, 1,529 in tests/, 147 in Domain/ folders alone, across 6 files, measured in Phase 8)",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "one of two honest exits is taken, not a partial fix: (a) csharp_style_var_elsewhere = true:silent, adjusting the stated convention to match what the code already does, or (b) the convention is kept but scoped to where its own stated argument holds -- a [src/*/Domain/**.cs] section at :warning, with all Domain/ violations (147 sites measured in Phase 8, re-measure before fixing -- the count may have moved) actually fixed and the resulting rule proven to fail the build on a new violation",
        "if (b) is chosen: armed -- introduce one new `var` use in a Domain/ file where the type is not apparent, confirm `dotnet build` fails, restore, confirm green",
        "whichever choice is taken, .editorconfig's own comment describing the rule is corrected to match reality"
      ],
      "notes": "Deferred in Phase 8 (see the external Plan doc's Phase 21 section, 'Quality Gates', which named this as a Phase 21 item) but never turned into a feature_list.json entry, so it silently fell out of scope when phase 21 (ids 34, 47) closed on 2026-09-18. Caught by the leader re-reading the Plan doc's own phase-21 checklist before updating it, rather than assuming the checklist and feature_list.json agreed. LIGHT change under CLAUDE.md Cost discipline (analyzer config, no saga/money/security touch) if option (a) is chosen; option (b) is closer to FULL scope (147 real code-site fixes across src/*/Domain/) and should be classified based on the actual count at the time it is worked, not assumed light because the config change itself is small. DISPOSED 2026-09-18 (phase 25 close-out, leader-direct root-config edit): option (a) taken. Re-measured the violation count live before deciding -- 209 real var declarations at line-start alone across 20 Domain/ files (undercount; excludes inline/foreach forms), up from Phase 8s 147, confirming the convention had only grown further unenforced, not shrunk. csharp_style_var_elsewhere flipped to true:silent (was false:suggestion, never above :suggestion severity so never actually enforced either way) and the .editorconfig comment corrected to state the real history and the decision, per the entrys own honest-exit (a). Option (b) (147+ real code-site fixes) judged disproportionate this late in the trilogy for a rule with no build-time guard behind either reading."
    },
    {
      "id": 109,
      "name": "otel_instruments_never_declare_a_unit",
      "phase": 22,
      "title": "None of #8's OTel Meter.CreateHistogram/CreateGauge calls (otc_saga_completion_ms, otc_outbox_lag_ms, otc_request_latency_ms, otc_fact_processing_latency_ms, otc_dlq_depth) pass a `unit:` parameter, so the Prometheus exporter never appends the unit suffix (_milliseconds, _ratio) #7's JS SDK calls produce -- the Grafana dashboard originally shipped querying names with the suffix and returned empty panels until fixed to match the real (suffix-less) names",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "a decision is made and recorded: either add `unit:` to every affected CreateHistogram/CreateGauge call so #8's exported metric names match #7's convention byte-for-byte (and the dashboard queries are reverted to the suffixed form), or the dashboard's current suffix-less queries are declared the correct, permanent shape and #7's convention is explicitly not followed here -- not left as an undocumented mismatch either way",
        "if `unit:` is added: every dashboard panel that queries the affected metrics is re-verified against real, non-empty data after the rename, the same way id 35 verified them",
        "the ported-idiom ledger (ADR or progress/impl_ note, per CLAUDE.md) gets one line: #7 relied on the JS OTel SDK auto-appending a unit suffix; in #8 that property is supplied by nothing, because CreateHistogram/CreateGauge's `unit:` parameter was never set"
      ],
      "notes": "Found by id 35's implementer while verifying the observability dashboard (2026-09-18): 4 of 5 panels were empty because the shipped dashboard queried metric names with a _milliseconds/_ratio suffix that .NET's OTel SDK never produces without an explicit unit: parameter. Fixed the dashboard's queries to match reality (infra/grafana/dashboards/order-to-cash-overview.json) -- confirmed live, all five panels non-empty. This entry is the other direction: whether the src/ instrumentation should change to match #7's naming instead, which the LIGHT verification pass correctly declined to decide on its own. Not blocking; the dashboard fix alone satisfies id 35's three acceptance bullets. DISPOSED 2026-09-18 (phase 25 close-out, leader-direct): the dashboards current suffix-less metric names declared the correct, permanent shape; src/ instruments NOT changed to add unit:, #7s naming convention NOT followed. All five dashboard panels already non-empty against real data (id 35); changing src/ would require reverting id 35s own fix for no functional benefit. Ported-idiom ledger line written per this entrys acceptance bullet 3. Full record: progress/impl_otel_instruments_never_declare_a_unit.md."
    },
    {
      "id": 110,
      "name": "orders_saga_commands_adapter_reply_decode_guard",
      "phase": 25,
      "title": "NatsSagaCommandsAdapter.SendAsync has no try/catch(JsonException) around its reply decode, unlike its sibling NatsStockAvailabilityChecker (fixed by feature 46 Advisory A1) and unlike #7 nats-saga-commands.adapter.ts:170-178 -- a malformed reply body throws a bare JsonException instead of the classified SagaCommandTransportError the saga retry/terminal-classification logic expects",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "a malformed (non-JSON) reply body on any saga-command subject (stock.reserve, credit.hold, despatch.create, invoice.issue, stock.release, credit.release) raises SagaCommandTransportError, never a bare JsonException",
        "proved by an integration test over a real broker with a stand-in responder answering non-JSON bytes, mirroring NatsStockAvailabilityCheckerTests' equivalent malformed-reply case",
        "armed: the added guard is removed and the test is confirmed to fail naming the unguarded JsonException, then restored and reconfirmed green"
      ],
      "notes": "Found by backlog id 52's retroactive ported-idiom ledger audit (2026-09-18, phase 25 close-out), boundary RL5. Confirmed gap, not a hypothesis: src/Orders/Infrastructure/Messaging/NatsSagaCommandsAdapter.cs:131-143 has no catch around RpcJson.IsErrorBody/RpcJson.Deserialize<TReply>. LIGHT change under CLAUDE.md Cost discipline (a bounded exception-handling port, same shape as feature 46's already-shipped fix on the sibling class) -- one implementer, leader checks the diff and the new test. Full detail: progress/impl_order_saga_orchestrator.md, section \"Retroactive ported-idiom ledger (backlog id 52)\", row RL5. FIXED 2026-09-18 (phase 25 close-out): SendAsync now wraps its reply-decode in try/catch(JsonException), throwing SagaCommandTransportError, matching the sibling NatsStockAvailabilityChecker guard exactly. Armed: catch removed, rebuilt --no-incremental, the new integration test failed with the real runtime JsonReaderException at the unguarded call site (not a stand-in assertion); restored and reconfirmed via git diff against tracked HEAD (equivalent to cmp, since the arming backup was taken pre-fix rather than post-fix -- disclosed transparently in the implementer report). Orders.UnitTests 503/503, Orders.IntegrationTests 157/157 (12 min, real MS-SQL/Kafka/NATS), init.sh green, no regressions. Leader independently reviewed the diff. Full record: progress/impl_orders_saga_commands_adapter_reply_decode_guard.md."
    },
    {
      "id": 111,
      "name": "outbox_relay_poison_payload_retry_loop_unverified",
      "phase": 25,
      "title": "Whether a corrupted outbox.payload row retries unboundedly through OutboxRelay.RunOnceAsync's READPAST claim on every poll, with no poison-row circuit-breaker, and whether #7 shares the identical exposure, was not established within backlog id 52's audit time-box",
      "sdd": false,
      "status": "done",
      "acceptance": [
        "read #7's outbox-relay.service.ts caller of runOnce() (not reached in id 52's audit) and determine whether #7 has the same poison-row exposure",
        "EITHER a test demonstrating the row is skipped/parked after N failed publish cycles (a fix, if #8 genuinely lacks what #7 has), OR a recorded, cited comparison establishing genuine #7 parity, disposed as ACCEPTED NOT FIXED with the citation -- the ledger's \"None owed\" conclusion is only sound once the sibling half is actually checked, not assumed",
        "if fixed: armed with a corrupted-payload row and a bounded retry count, proving the circuit-breaker actually stops the loop rather than merely asserting it should"
      ],
      "notes": "Found by backlog id 52's retroactive ported-idiom ledger audit (2026-09-18, phase 25 close-out), boundary RL4 -- exceeded the audit's own 10-minute per-boundary budget and was recorded as a finding per the entry's acceptance bullet 5 rather than pushed through. No #7-vs-#8 asymmetry confirmed yet either way (both let a payload-decode failure escape the graceful publish-rollback path uncaught, both are one layer up caught by a poll-loop-level catch-and-retry) -- what is unverified is whether that composes into an unbounded retry, and whether #7 shares it. LIGHT-or-FULL depending which exit is taken (a parity citation is LIGHT; a real circuit-breaker touching the outbox relay -- shared across Orders/Fulfillment/Billing per OutboxRelayParityTests -- is closer to FULL). Full detail: progress/impl_outbox_and_idempotency.md, section \"Retroactive ported-idiom ledger (backlog id 52)\", row RL4. DISPOSED 2026-09-18 (phase 25 close-out): leader read outbox-relay.service.ts directly. NOT parity -- an asymmetry in #8s favour: #7s poll loop has no catch around runOnce(), only finally, so an unhandled promise rejection (e.g. a payload-decode failure outside the inner publish try-block) would crash the whole service process on first occurrence, with no unhandledRejection handler anywhere in apps/*/src/main.ts. #8s OutboxRelayBackgroundService.cs already catches every non-cancellation exception and retries paced on the next tick, never crashing. Neither repo has a poison-row circuit-breaker, but #8 does not lack what #7 supplies -- the reverse. ACCEPTED, NOT FIXED, re-open trigger: a real corrupted outbox.payload row observed in a real environment. Full record: progress/impl_outbox_relay_poison_payload_retry_loop_unverified.md."
    }
  ]
}
