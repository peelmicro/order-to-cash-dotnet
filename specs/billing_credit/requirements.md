# `billing_credit` — Requirements (assessment #8, .NET)

> **The normative requirements for this feature are `R37` – `R41` in [`specs/shared/requirements.md`](../shared/requirements.md) §5**, elaborated by [`specs/shared/domain-model.md`](../shared/domain-model.md) §5.1 (the `BuyerCredit` aggregate, the `CreditLedgerEntry` child entity, the derived quantities, invariants **B1** – **B5**), [`specs/shared/saga.md`](../shared/saga.md) §2 (the command vocabulary and its idempotency keys), §3.1 steps 2/3, §4.2 – §4.3 (the credit-rejection compensation and why no credit is released on that path), §5 (the consumption map) and §6 (the redelivery table), and [`specs/shared/asyncapi.yaml`](../shared/asyncapi.yaml) (the `billing.credit.hold` / `billing.credit.release` / `billing.credit.list` request and reply schemas and the `credit.approved.v1` / `credit.rejected.v1` / `credit.released.v1` payloads). They are **not restated here** — this file adds only the local requirements (`BC<n>`) for gaps the shared spec leaves to each assessment, and the traceability for them.
>
> **Id reuse is a claim.** `BC1` – `BC17` and `BC20` reuse #7's local ids (`order-to-cash-nestjs/specs/billing_credit/requirements.md`) as an explicit assertion that the .NET realisation carries **the same obligation**, checked one by one against #7's committed code and its review before the id was written down. `BC18` and `BC19` are **deliberately not reused** — see §1.8. `BC21` – `BC32` are **new to #8**, and each names why #7 could not have faced it. **`BC29` – `BC32` were added by the post-gate revision of 2026-09-05** — `BC29` and `BC30` because the gate overruled this spec's two recommendations, `BC31` and `BC32` because the standing instruction attached to that ruling (*"stop leaving issues to the next phase — fix them"*) overturned two of the four deferrals `design.md` §10.3 had recorded.
>
> **`R42` – `R44` are NOT this feature's.** They are the credit-check simulator (feature 20, `billing_credit_simulator`) and stay `TODO` in the shared matrix. This feature owns the **port** they sit behind and the structural guarantee `R44` demands (`BC13` – `BC15`), so that feature 20 is a *binding* change and not a redesign.
>
> **`R40` is delivered but not driven.** The `consume` ledger entry belongs to invoice issue (feature 21). This feature ships `BuyerCredit.Consume` with its domain unit tests and **no caller**, exactly as feature 17 shipped `StockItem.Consume` for feature 18. The `R40` matrix row is flipped here because its named test is a domain-unit test this feature writes and greens; nothing about invoicing is claimed.
>
> **`R41` is fully delivered in #8, and that is a difference from #7 worth stating at the top.** #7 shipped `releaseHold` with no caller and no responder, because **no `billing.credit.release` subject existed in its `asyncapi.yaml` at the time** (#7's `progress/spec_billing_credit.md` row 10, and its review's non-blocking finding `N3`). #8 inherits #7's **final** shared contract, in which `channels.creditRelease` (`specs/shared/asyncapi.yaml:440`) and both its payload schemas exist with their full idempotency semantics. This feature therefore builds the responder (`BC25`), and `R41`'s `order_cancelled` half gets a live caller path rather than a caller-less branch. The `invoice_paid` half remains Billing-internal and arrives with feature 22.
>
> Related shared requirements this feature composes with but does not own: `R13`/`R14`/`R15` (the outbox, the relay, the partition key — Billing's copies are proven against them here, the rows stay owned by feature 14), `R17`/`R18` (the idempotent-consumer pattern — **not** copied into this service, `design.md` §9), `R29` (the orchestrator's retry and its `(orderReference, operation)` responder contract — the Billing side of it is `BC7`), `R12` (`correlationId` / `causationId` chains — `BC1`), `R26`/`R27`/`R28` (the compensation this feature's `credit.rejected.v1` triggers, owned by feature 16 and already green).

## 1. Local requirements — `BC<n>`

### 1.1 Wire contract of the responders

**BC1.** WHEN Billing emits a fact in response to a `billing.credit.hold` or `billing.credit.release` command, THE SYSTEM SHALL set the fact's `correlationId` from the request's `x-correlation-id` header and its `causationId` from the request's `x-request-id` header (`R12`); IF either header is absent or is not a well-formed `UniqueId`, THEN THE SYSTEM SHALL reply `RpcError` `VALIDATION_FAILED`, SHALL append no ledger entry and SHALL emit no fact. `billing.credit.list` SHALL require neither header.

**BC2.** THE SYSTEM SHALL accept `billing.credit.hold`, `billing.credit.release` and `billing.credit.list` as **bare JSON** payloads matching their AsyncAPI request schemas — no framework packet envelope around them — and SHALL reply with a bare JSON success payload or a bare JSON `RpcError`, serialised through the one shared `JsonWire.Options`; a responder SHALL never reply with anything else and SHALL never leave a request unanswered on any path it can reach.

### 1.2 Resolving the credit line — the two contract violations

**BC3.** IF a `billing.credit.hold` or `billing.credit.release` command names a `(retailerCode, companyCode)` pair for which **no** credit line exists, THEN THE SYSTEM SHALL reply `RpcError` `NOT_FOUND` naming the pair, SHALL append no ledger entry and SHALL emit **no** fact, and SHALL NOT create a credit line on demand — a credit line is master data, and a fact with no aggregate has no `aggregateId` to carry (`domain-model.md` §7.1).

**BC4.** IF a `billing.credit.hold` command names a currency other than the resolved credit line's currency, THEN THE SYSTEM SHALL reply `RpcError` `VALIDATION_FAILED` carrying the expected and received codes, SHALL append no ledger entry and SHALL emit **no** fact — an order is single-currency by **M2**/**O1** and a credit line's currency is its retailer's, so a mismatch is a defective message, not a statement about the buyer's credit, and the three `reason` values of `credit.rejected.v1` stay closed (`R44`). *(This is the clause the boxed amendment under shared `R39` already fixes; the amendment is inherited, not made here.)*

### 1.3 Deriving `availableCredit` from an append-only ledger — the crux

**BC5.** THE SYSTEM SHALL derive a credit line's available credit **only** from its `credits` row and its append-only ledger, as

```
committedExposure(line) = Σ amount WHERE type = 'hold'  −  Σ amount WHERE type = 'release'
availableCredit(line)   = creditLimit − committedExposure(line)
```

and SHALL persist **no** materialised, cached or derived available-credit, active-hold or open-exposure column anywhere. A `consume` entry appears in neither term, which is *why* invoice issue is numerically neutral (`R40`) — a structural consequence of the formula, not a rule applied on top of it.

**BC6.** THE SYSTEM SHALL derive the per-order split reported by `billing.credit.list` as

```
exposure(order)     = Σ hold(order) − Σ release(order)          -- ≥ 0 by B5
openExposure(order) = min( Σ consume(order), exposure(order) )
activeHold(order)   = exposure(order) − openExposure(order)
```

and SHALL satisfy, for every credit line and at every instant, `Σ activeHold(order) + Σ openExposure(order) = creditLimit − availableCredit`, so the `CreditView`'s three amounts can never tell a different story than `BC5`'s two-term sum.

**BC7.** WHILE a `hold` ledger entry already exists for the request's `orderReference` on the resolved credit line — **whatever its net exposure is now**, including an order whose hold has since been released — THE SYSTEM SHALL reply to `billing.credit.hold` with `outcome: already_held` carrying the amount of that recorded `hold` entry as `heldAmount` and the line's **current** `availableCredit`, SHALL append no ledger entry, SHALL consult the credit-decision port not at all, and SHALL emit no fact (invariant **B4**, `saga.md` §6 layer 3, `R29`'s responder half). The idempotency key is `(orderReference, hold)`; `x-request-id` is **not** the key.

**BC8.** IF a `billing.credit.hold` command is re-issued for an order whose previous hold was **rejected**, THEN THE SYSTEM SHALL re-evaluate the request from scratch — a rejection records nothing (**B1**), so there is nothing for `BC7` to recognise — and MAY therefore emit a second `credit.rejected.v1` with a distinct `eventId`; THE SYSTEM SHALL rely for safety on the orchestrator's three idempotency layers and on `EfCoreSagaCommandStore.EnqueueAsync`'s `(order_id, command)`-idempotent enqueue, and SHALL NOT invent a fourth ledger entry type to record refusals.

**BC9.** WHEN executing `billing.credit.hold` or `billing.credit.release`, THE SYSTEM SHALL acquire an exclusive row lock on the resolved `credits` row **before** reading any `credit_items` row and before evaluating anything, SHALL make every `credit_items` read of that transaction a locking read, and SHALL perform every read and every write of that line's ledger inside that same transaction, so that IF two commands for different orders compete against one credit line concurrently, THEN their combined recorded holds never exceed `creditLimit` and neither transaction deadlocks (exactly one row is ever locked, so no lock-ordering cycle can form).

### 1.4 The three ledger operations

**BC10.** WHEN a `credit.hold` is approved, THE SYSTEM SHALL append exactly one `hold` entry and exactly one `credit.approved.v1` **in the same transaction** (`R38`, `R13`), and the fact's `availableCreditAfter` SHALL equal `availableCredit` recomputed from the ledger *including* the entry just appended — never the pre-hold value, never a value carried from the request.

**BC11.** WHEN `Release` is invoked for an order, THE SYSTEM SHALL append exactly one `release` entry for that order's **outstanding exposure** and emit exactly one `credit.released.v1` carrying that amount and the caller-supplied reason (`invoice_paid` | `order_cancelled`); WHILE the order has no outstanding exposure — never held, or already released — THE SYSTEM SHALL append nothing, emit nothing and report the no-op; and THE SYSTEM SHALL never append a `release` that would drive `exposure(order)` below zero (**B5**), raising a domain error instead.

**BC12.** WHEN `Consume` is invoked for an order holding an active hold, THE SYSTEM SHALL append exactly one `consume` entry of the same amount, SHALL emit **no** fact, and SHALL leave `availableCredit` numerically unchanged (`R40`); IF it is invoked for an order with no active hold, THEN THE SYSTEM SHALL raise a domain error and append nothing.

### 1.5 The credit-decision port — feature 20's seam, fixed now

**BC13.** THE SYSTEM SHALL consult the credit-decision port **only after** the aggregate has determined, from `BC5`, that the requested amount fits within the available credit, so that a bound adapter can turn an otherwise-approvable hold into a refusal but can **never** turn an over-limit hold into an approval — `R44`'s *"SHALL NOT allow the simulator to bypass `R37`"*, enforced by evaluation order rather than by discipline.

**BC14.** THE SYSTEM SHALL forbid the credit-decision port from returning the reason `over_limit` — `over_limit` is the aggregate's word and only the aggregate may say it — and SHALL build `credit.rejected.v1`, the outbox record and the RPC reply through **one** code path for every refusal, so that a genuine over-limit refusal and an adapter refusal differ in exactly one field (`reason`) and in nothing else (`R44`). WHEN the port refuses a hold the aggregate found fitting, THE SYSTEM SHALL record exactly one `credit.rejected.v1` on the aggregate it persists, carrying the port's reason, the requested amount and the unchanged available credit.

**BC15.** WHERE no credit-check simulator is bound, THE SYSTEM SHALL bind an adapter that approves every request it is asked about, and THE SYSTEM SHALL make substituting feature 20's simulator a change to **DI registration only** — no change to any file under `Domain/`, `Application/` or `Presentation/`.

### 1.6 The facts leave through Billing's own outbox

**BC16.** THE SYSTEM SHALL write `credit.approved.v1`, `credit.rejected.v1` and `credit.released.v1` as outbox records in the **same transaction** as the ledger entries they describe (`R13`), and SHALL publish them to `otc.billing.facts.v1` **only** through Billing's own outbox relay, keyed by `correlationId` (`R14`, `R15`) — no responder, handler or aggregate publishes directly.

### 1.7 The structural work this feature owns

**BC17.** THE SYSTEM SHALL hold every write model's copy of the outbox-relay family byte-identical to the canonical copy after banner and namespace normalisation, SHALL keep the canonical copy free of any service name outside those normalised regions and free of any `using` outside a declared portable whitelist, and SHALL require the family from every service that owns a relational `outbox` configuration — the same instrument, and the same three cases, `IdempotentConsumerParityTests` already applies to the idempotent-consumer pair.

> **The #8 family is #7's, at full scope: seven files.** `OutboxRelay.cs`, `OutboxRelayOptions.cs`, `OutboxRelayBackgroundService.cs`, `OutboxEnvelopeMapper.cs`, `KafkaFactPublisher.cs`, **`OutboxWriter.cs`** and **`KafkaOptions.cs`**. The spec pass proposed narrowing the set to five — excluding the last two because making them identical needs a per-service payload-mapper port and a neutral domain-event base name, *"a redesign of two shipped services in service of a guard"* — and **the human gate overruled it on 2026-09-05**: reusing `BC17`'s id claimed #7's obligation at full scope, so the redesign is the work rather than a reason to avoid it. `design.md` §8.2 specifies the two structural enablers (the neutral `FactEvent` base and the per-service `IFactPayloadMapper` port), §8.2.4 specifies the `KafkaOptions` unification, and `tasks.md` A1 – A12 name every file in `src/Orders/` and `src/Fulfillment/` the change touches. The guard is armed **file by file**: a divergence in any one of the seven must fail case 1 by name (`tasks.md` F6).

### 1.8 What #7's `BC18` and `BC19` asked for, and why #8 does not claim them

**`BC18` is not reused.** #7's `BC18` obliged its seed's `outbox-parity.spec.ts` to compare only executable SQL, because that instrument matched its regex against **migration file text**, so a `--` comment mentioning `outbox` became a compared "statement". #8's equivalent instrument, `tests/Billing.IntegrationTests/ReliabilityTableParityTests.cs`, migrates four fresh real databases and compares `INFORMATION_SCHEMA.COLUMNS` and `sys.indexes` — *"Asserted from the DATABASE, never from EF metadata"*, its own class summary. A comment cannot enter that comparison at all, so there is no property to require and no defect to fix. Claiming `BC18` would be claiming a fix for a bug this stack cannot have.

**`BC19` is not reused.** #7's `BC19` split its `apps/seed/src/verify.ts` into exact-count and lower-bound assertions, because that verifier `exit 1`s on a long-lived development database and this feature's own live-boot procedure re-runs the seed. **#8 has no such verifier**: `src/Seed/Presentation/SeedRunner` writes and then *prints* a row-count summary (`SeedSummary`), and asserts no count against a fixture; a `grep` for `assertEqual`/`assertAtLeast`/`Verify` across `src/Seed` returns nothing. #8's live-boot procedure therefore does not re-run the seed at all (`design.md` §11), and there is no exit code to fix.

### 1.9 New to #8 — properties #7 got for free, or contract surface #7 did not have

**BC21.** THE SYSTEM SHALL handle `billing.credit.*` requests **concurrently** with a bounded degree, resolving a **distinct DI scope per request** and never one per responder, so that a request blocked on a `credits` row lock does not delay an unrelated request on another line. *(#7 got per-request concurrency and per-request injection scopes from `@nestjs/microservices`' NATS server. #8's responder is a hand-written `BackgroundService`; the property must be built and guarded, as `FS18` already established for Fulfillment.)*

**BC22.** WHEN the host shuts down while a `billing.credit.*` request is in flight, THE SYSTEM SHALL wait for **every** in-flight request to complete and SHALL NOT propagate the failure of any one of them out of `StopAsync`, so a single faulted reply cannot abort the host's shutdown sequence or mask the completion of its healthy siblings; the failure SHALL be logged with its subject and `correlationId`. *(Backlog id 50. `StockRpcResponder.StopAsync`'s `await Task.WhenAll(pending)` rethrows the first faulted task's exception out of host shutdown; feature 19 writes the third copy of that shape, so the correct behaviour is specified once here and Fulfillment's copy is brought into line.)*

**BC23.** THE SYSTEM SHALL derive the expected property names of every `billing.credit.*` request and reply record **by parsing `specs/shared/asyncapi.yaml`**, never from a hand-retyped key list, and SHALL assert that each existing hand-retyped key list in `StockRpcPayloadTests` and `SagaCommandPayloadTests` agrees with the parsed set — so a schema and its test cannot be changed together, wrongly, and stay green. *(Backlog id 51. Billing's payload tests would otherwise be the **fourth** hand-retyped copy while three documents claim the instrument reads the contract.)*

**BC24.** WHEN a `credit_items` row's `credit_date` is read back into the domain, THE SYSTEM SHALL yield the same instant that was written, independently of the host's local time zone. *(#7 got this from a `mysql2` pool created with `timezone: 'Z'` — its `design.md` §7.1 says so in one clause. MS-SQL's `datetime2(3)` carries **no** offset and EF Core returns `DateTimeKind.Unspecified`, so a `DateTimeOffset` built from it without an explicit `TimeSpan.Zero` silently applies the machine's local offset. `CreditLedgerEntry` is the first #8 child entity that carries a date across this boundary — `ReservationSnapshot` carries none.)*

**BC25.** THE SYSTEM SHALL answer `billing.credit.release` by appending exactly one `release` ledger entry for the order's outstanding exposure with reason `order_cancelled` and emitting exactly one `credit.released.v1`, replying `released: true` with the released amount and the resulting available credit; WHILE the order has no outstanding exposure, THE SYSTEM SHALL append nothing, emit nothing, and reply `released: false` — success, not an error (`asyncapi.yaml` `requestCreditRelease`). `reason` is not a caller-supplied field on this subject.

**BC26.** WHEN a `billing.credit.hold` request would satisfy more than one of `BC7`'s `already_held`, `BC4`'s currency mismatch and `R39`'s over-limit, THE SYSTEM SHALL apply them in the fixed order **`already_held` → `currency_mismatch` → `over_limit`**, so a re-issued command naming the wrong currency for an order that already holds is answered `already_held` rather than `VALIDATION_FAILED`. *(#7's review recorded this precedence as real, defensible and pinned by nothing — its non-blocking finding `N1`: "neither `BC4` nor `BC7` states the precedence and no test pins it … worth a line in the shared spec so #8/#9 do not order the two checks differently". #8 pins it here rather than rediscovering it.)*

**BC27.** THE SYSTEM SHALL map every transient store failure of a `billing.credit.*` command to an `RpcError` code the orchestrator treats as **retryable**, and SHALL never produce `CONFLICT` from this service. *(#8's `NatsSagaCommandsAdapter.IsTerminalRpcErrorCode` classifies `CONFLICT` as a terminal business rejection — feature 42 — so a deadlock victim answered `CONFLICT` would permanently end the order's saga. #7's orchestrator retried every code, so the same mapping was safe there. The Fulfillment analogue is `FS21`.)*

**BC28.** THE SYSTEM SHALL group a credit line's ledger entries by `orderReference` using a comparison that agrees with the database's own matching of that column, and SHALL derive the `retailerCode`, `companyCode`, `creditCode` and `currency` of every emitted fact and every reply from the **resolved credit line row**, never from the request's echoed strings. *(#7's `summariseLedger` grouped with JavaScript `===` — ordinal — against MySQL's case-insensitive `utf8mb4_0900_ai_ci`; the same latent disagreement exists between .NET's ordinal `string` equality and MS-SQL's `SQL_Latin1_General_CP1_CI_AS`, and it is closed the same way `FS19`/ledger `L3` closed it for product codes: at the edge, by the validator's alphabet, plus an explicit comparer.)*

**BC29.** THE SYSTEM SHALL give every service's outbox-relay Kafka producer its **own** client id and SHALL make a service that names none fail to **compile**, never to boot with an empty or shared one. *(New to #8, and created by `BC17`'s widened scope. #7's per-service `kafka.config.ts` was **excluded** from its parity family precisely because it held this per-service value, so the property came free from the file's existence. Unifying `KafkaOptions.cs` across three services removes that home, so the property must be re-supplied: the canonical `ClientId` carries **no default** and is declared `required`, which moves the obligation onto each service's own options class and makes omission a `CS9035` build error rather than a producer silently registering as librdkafka's default `rdkafka`.)*

**BC30.** WHEN any summation over a credit line's ledger — the per-order exposure, the committed exposure, the open exposure, or the available credit derived from them — would exceed the range of a 64-bit signed integer, THE SYSTEM SHALL **raise** a domain error carrying a stable code and SHALL NOT return a wrapped value; and THE SYSTEM SHALL make that behaviour reachable from a unit test that drives the summation function directly, with no aggregate state and no persisted ledger. *(New to #8. JavaScript numbers do not wrap — they lose precision above 2⁵³, which no amount here approaches — so #7 faced nothing at this boundary. C# `long` wraps **silently**: `Directory.Build.props` sets no `CheckForOverflowUnderflow`, so every `+` and `-` in the domain is an unchecked operation by default. The spec pass argued the sum is bounded by construction and recommended "stated, not guarded"; **the human gate overruled it on 2026-09-05** on the ground that #8 has already shipped one money-width defect whose requirement text was satisfied exactly, and that `Reconstitute`'s refusal blocks one **route** to an overflowing ledger, not the arithmetic. `Reconstitute`'s refusal (`BC30`'s outer bound, armed by `tasks.md` B10) is now the second line, not the only one.)*

**BC31.** THE SYSTEM SHALL pass a **freshly constructed** header collection to every outbound NATS saga-command request, never a collection reused or cleared between calls. *(New to #8. `NATS.Net`'s `NatsHeaders` is documented as not thread-safe and is mutable; #7's `nats` client took a plain object literal per publish, so a reused instance was not constructible. `NatsSagaCommandsAdapter`'s class remark already asserts this property and no test holds it — backlog id 48. Closed here, test-only, because `BC27`'s mapper test already reads that class's own classification and five more services will copy the shape.)*

**BC32.** THE SYSTEM SHALL make every integration test that deserialises an RPC reply assert that reply's own discriminating field **before** it touches any collection, so a wrong-shaped or error-shaped reply fails on a named assertion rather than on a null dereference — or, worse, not at all. *(New to #8. #7's tests destructured a JavaScript object and read `undefined`, which its assertions caught; C# deserialises an `RpcError` body into an all-defaults reply record **without throwing**, so the same test shape silently passes. Backlog id 53; the three named sites are listed in `design.md` §10.5.)*

### 1.10 First boot against the live compose stack

**BC20.** WHEN the Billing responders first start against a compose stack whose `otc_orders.saga_commands` holds `parked` `credit.hold` rows, THE SYSTEM SHALL answer the sweeper's next re-issue of each row so that, **without operator action**, each such order gains exactly one `hold` ledger entry, exactly one `credit.approved.v1` in `otc_billing.outbox` stamped published, and advances in Orders through `credit_approved` → `confirmed` → `despatched`, parking at `invoice.issue` for which this feature registers no responder. *(Worked out precisely in `design.md` §11 from the running stack; verified live, not by an automated test.)*

## 2. Traceability — local rows

The shared rows `R37` – `R41` already carry named tests in [`specs/shared/test-matrix.md`](../shared/test-matrix.md) §5; this feature's tests use exactly those case names in PascalCase and flip those five rows at close-out (`R42` – `R44` stay `TODO` for feature 20). Local rows:

| Id | Level | Test file › case | Status |
|---|---|---|---|
| **BC1** | unit | `tests/Billing.UnitTests/CreditResponderHeaderTests.cs` › `BC1_RepliesValidationFailedAndDispatchesNothing_WhenACorrelationOrRequestHeaderIsMissingOrMalformed` — theory now also covers `HeaderCase.MalformedRequestIdOnly` (a present-but-not-a-`UniqueId` `x-request-id`, distinct from the pre-existing missing case) | DONE |
| **BC1** | integration | `tests/Billing.IntegrationTests/CreditHoldTests.cs` › `BC1_StampsCorrelationIdFromTheHeaderAndCausationIdFromTheRequestId_OnTheEmittedCreditApprovedFact` | DONE |
| **BC2** | integration | `tests/Billing.IntegrationTests/CreditWireTests.cs` › `BC2_AnswersABareJsonRequestWithABareJsonReply_OnAllThreeSubjects` and `BC2_AnswersABareJsonRpcError_OnAValidationFailure` | DONE |
| **BC3** | integration | `tests/Billing.IntegrationTests/CreditHoldTests.cs` › `BC3_RepliesNotFoundNamingThePair_WritingNoLedgerEntryAndEmittingNoFact_WhenNoCreditLineExists` | DONE |
| **BC4** | integration | `tests/Billing.IntegrationTests/CreditHoldTests.cs` › `BC4_RepliesValidationFailedWritingNoLedgerEntryAndEmittingNoFact_WhenTheRequestedCurrencyDiffersFromTheLines` | DONE |
| **BC5** | domain unit | `tests/Billing.UnitTests/BuyerCreditTests.cs` › `BC5_DerivesAvailableCreditAsTheLimitMinusHoldsPlusReleases_SoAConsumeEntryMovesItByNothing` | DONE |
| **BC6** | domain unit + integration | `tests/Billing.UnitTests/CreditExposureTests.cs` › `BC6_SplitsOutstandingExposureIntoActiveHoldsAndOpenInvoiceExposureWhoseSumAlwaysEqualsTheLimitMinusAvailableCredit`; `tests/Billing.IntegrationTests/CreditListTests.cs` › `BC6_ReportsActiveHoldsOpenExposureAndAvailableCreditThatReconcileToTheCreditLimitForEveryListedLine` | DONE |
| **BC7** | unit + integration | `tests/Billing.UnitTests/CreditHoldServiceTests.cs` › `BC7_ShortCircuitsToAlreadyHeld_OnAHoldEntryWhoseExposureHasSinceBeenReleased_CallingNoPortAndWritingNothing`; `tests/Billing.IntegrationTests/CreditHoldTests.cs` › `BC7_AnswersAlreadyHeldWithTheRecordedHeldAmountAndTheCurrentAvailableCredit_WritingNoEntryAndEmittingNoSecondFact_WhenReIssued` | DONE |
| **BC8** | integration | `tests/Billing.IntegrationTests/CreditHoldTests.cs` › `BC8_ReEvaluatesAPreviouslyRejectedHoldFromScratch_AndWritesNoLedgerEntryEitherTime` | DONE |
| **BC9** | integration | `tests/Billing.IntegrationTests/CreditHoldRaceTests.cs` › `BC9_TwoConcurrentHoldsAgainstOneNearlyExhaustedCreditLine_YieldExactlyOneCreditApprovedAndOneCreditRejected_WithCommittedHoldsNeverExceedingTheLimitAndNoDeadlock` | DONE |
| **BC10** | domain unit + integration | `tests/Billing.UnitTests/CreditHoldTests.cs` › `BC10_ReportsAvailableCreditAfterRecomputedWithTheAppendedHold_NeverThePreHoldValue`; `tests/Billing.IntegrationTests/BuyerCreditRepositoryTests.cs` › `BC10_WritesTheHoldEntryAndTheCreditApprovedOutboxRecordInOneTransaction_AndAForcedRollbackLeavesNeither` | DONE |
| **BC11** | domain unit | `tests/Billing.UnitTests/CreditLedgerTests.cs` › `BC11_ReleasesTheOrdersOutstandingExposureOnce_ReportsANoOpOnASecondRelease_AndRefusesAReleaseThatWouldDriveExposureBelowZero` | DONE |
| **BC12** | domain unit | `tests/Billing.UnitTests/CreditLedgerTests.cs` › `BC12_AppendsAConsumeEntryThatLeavesAvailableCreditUnchangedAndEmitsNoFact_AndRefusesToConsumeAnOrderWithNoActiveHold` | DONE |
| **BC13** | unit | `tests/Billing.UnitTests/CreditHoldServiceTests.cs` › `BC13_ConsultsTheCreditDecisionPortOnlyAfterTheAggregateHasFoundTheAmountFits_AndNeverForAnOverLimitRequest` | DONE |
| **BC14** | domain unit + unit | `tests/Billing.UnitTests/CreditHoldTests.cs` › `BC14_EmitsACreditRejectedFactForAnAdapterRefusalThatDiffersFromTheOverLimitRefusalInTheReasonFieldAndInNothingElse`; `tests/Billing.UnitTests/CreditDecisionPortTests.cs` › `BC14_TypesThePortSoThatOverLimitIsNotAReasonAnAdapterCanReturn`; `tests/Billing.UnitTests/CreditHoldServiceTests.cs` › `BC14_ReturnsRejectedWithThePortsReasonAndRecordsExactlyOneCreditRejectedFactCarryingThatReasonTheRequestedAmountAndTheUnchangedAvailableCredit_WhenThePortRefusesAFittingHold` | DONE |
| **BC15** | unit | `tests/Billing.UnitTests/AlwaysApproveCreditDecisionTests.cs` › `BC15_ApprovesEveryRequest_AndIsTheOnlyRegistrationFeature20Replaces` | DONE |
| **BC16** | integration | `tests/Billing.IntegrationTests/BillingOutboxRelayTests.cs` › `BC16_PublishesTheFactsOfACreditHoldTransactionToTheBillingTopicKeyedByCorrelationId_AndStampsPublishedAtOnlyAfterAcknowledgement` | DONE |
| **BC17** | unit | `tests/Orders.UnitTests/OutboxRelayParityTests.cs` › `HoldsEveryWriteModelsCopyOfTheOutboxRelayFamilyByteIdenticalToTheCanonicalAfterTheBannerAndTheNamespaceLine`, › `KeepsTheCanonicalFamilyAdoptableVerbatimNamingNoServiceAndImportingNothingServiceSpecific`, › `RequiresTheOutboxRelayFamilyFromEveryServiceThatOwnsARelationalOutboxConfiguration` | DONE |
| **BC20** | live verification | `progress/impl_billing_credit.md` § Live boot — the recorded `SELECT`s and log lines of `design.md` §11 | DONE — `progress/impl_billing_credit.md` § Live boot |
| **BC21** | unit + integration | `tests/Billing.UnitTests/CreditResponderConcurrencyTests.cs` › `BC21_ResolvesADistinctDependencyInjectionScopePerRequest_NeverOnePerResponder`; `tests/Billing.IntegrationTests/CreditResponderConcurrencyTests.cs` › `BC21_AnswersASecondRequestWhileAnEarlierOneIsBlockedOnACreditRowLockHeldByAnotherTransaction` | DONE |
| **BC22** | unit | `tests/Billing.UnitTests/CreditResponderShutdownTests.cs` › `BC22_CompletesShutdownWaitingForEveryInFlightRequest_WhenOneFaultsAndOneSucceeds`; `tests/Fulfillment.UnitTests/StockResponderShutdownTests.cs` › `BC22_CompletesShutdownWaitingForEveryInFlightRequest_WhenOneFaultsAndOneSucceeds` | DONE |
| **BC23** | unit | `tests/Billing.UnitTests/CreditRpcPayloadTests.cs` › `BC23_EveryCreditRequestAndReplyRecordCarriesExactlyThePropertyNamesAsyncApiDeclares_ParsedFromTheSpecNeverRetyped`; `tests/Fulfillment.UnitTests/StockRpcPayloadTests.cs` › `BC23_TheRetypedKeyListsAgreeWithTheKeySetsParsedFromAsyncApi`; `tests/Orders.UnitTests/SagaCommandPayloadTests.cs` › `BC23_TheRetypedKeyListsAgreeWithTheKeySetsParsedFromAsyncApi`; `tests/Orders.UnitTests/OrdersCreateErrorMapperTests.cs` › `BC23_TheThreeRetypedRpcErrorCodeSetsAgreeWithTheEnumParsedFromAsyncApi` | DONE |
| **BC24** | integration | `tests/Billing.IntegrationTests/BuyerCreditRepositoryTests.cs` › `BC24_RoundTripsALedgerEntrysInstantUnchanged_UnderANonUtcHostTimeZone` | DONE |
| **BC25** | integration | `tests/Billing.IntegrationTests/CreditReleaseTests.cs` › `BC25_AppendsExactlyOneReleaseEntryAndEmitsExactlyOneCreditReleasedFactWithReasonOrderCancelled_ThenReportsReleasedFalseWritingNothingOnARepeat` | DONE |
| **BC26** | unit | `tests/Billing.UnitTests/CreditHoldTests.cs` › `BC26_RanksAlreadyHeldAboveCurrencyMismatchAndCurrencyMismatchAboveOverLimit_WhenMoreThanOneApplies` | DONE |
| **BC27** | unit | `tests/Billing.UnitTests/CreditErrorMapperTests.cs` › `BC27_MapsEveryTransientStoreFailureToACodeTheSagaAdapterTreatsAsRetryable_NeverToATerminalBusinessCode` | DONE |
| **BC28** | unit + integration | `tests/Billing.UnitTests/CreditExposureTests.cs` › `BC28_GroupsLedgerEntriesByOrderReferenceCaseInsensitively_MatchingTheDatabasesOwnComparison`; `tests/Billing.IntegrationTests/CreditHoldTests.cs` › `BC28_EmitsTheResolvedCreditLinesCodesOnTheFact_NotTheRequestsEchoedStrings` (approved fact) and `R39_OverLimit_RejectedReplyWithNoLedgerEntryAndOneCreditRejectedV1NamingReasonRequestedAmountAndAvailableCredit` (rejected fact, `orderReference`/`retailerCode`/`companyCode`/`creditCode`/`currency`); `tests/Billing.IntegrationTests/CreditReleaseTests.cs` › `BC25_AppendsExactlyOneReleaseEntryAndEmitsExactlyOneCreditReleasedFactWithReasonOrderCancelled_ThenReportsReleasedFalseWritingNothingOnARepeat` (released fact, same five fields) | DONE |
| **BC29** | build-time guard | the canonical `KafkaOptions.ClientId` is `required`; the guard is the **compiler**, armed in `tasks.md` A8 by deleting `ClientId = "otc-orders"` from `OrdersOutboxOptions` and recording the `CS9035` error verbatim. Runtime witness: `tests/Orders.UnitTests/OrdersOutboxRegistrationTests.cs` › the existing registration case, which supplies a client id explicitly | DONE |
| **BC30** | domain unit | `tests/Billing.UnitTests/CreditExposureTests.cs` › `BC30_RaisesCreditLedgerOverflowRatherThanWrapping_WhenTheHoldTotalExceedsALong` and `BC30_RaisesCreditLedgerOverflowRatherThanWrapping_WhenTheCommittedExposureAcrossOrdersExceedsALong`; `tests/SharedKernel.UnitTests/MoneyTests.cs` › `BC30_RaisesRatherThanWrapping_WhenAddSubtractOrMultiplyOverflowsMinorUnits`; `tests/Billing.UnitTests/BuyerCreditTests.cs` › `Reconstitute_RefusesASnapshotWhoseCommittedExposureAlreadyExceedsTheCreditLimit_AndASnapshotWhoseEntryCurrencyDiffersFromTheLines` (the outer bound) | DONE |
| **BC31** | unit | `tests/Orders.UnitTests/NatsSagaCommandsAdapterTests.cs` › `BC31_PassesAFreshlyConstructedHeaderCollectionOnEveryRequest_NeverOneReusedBetweenCalls` | DONE |
| **BC32** | integration | `tests/Orders.IntegrationTests/OrdersCreateAcceptanceTests.cs`, `tests/Fulfillment.IntegrationTests/StockReplenishTests.cs`, `tests/Fulfillment.IntegrationTests/StockListTests.cs` › `BC32_FailsOnItsOwnAssertion_WhenTheResponderAnswersAnRpcError` (one case per file) AND, at backlog id 53's three originally-named sites, `AcceptanceItem1_OrdersCreate_CallsFulfillmentStockCheckWithTheRequestsOwnCompanyAndLines`, `StockReplenishTests.HappyPath_UnitsUp_ReservedUnitsAndReservationsUntouched_OutboxEmpty`, `StockListTests.FS15_ListsStockViewsWithDerivedAvailableUnits_...` — all three now assert the reply's own discriminating field before touching a collection | DONE |

**Shared rows this feature flips** (`specs/shared/test-matrix.md` §5): `R37`, `R38`, `R39`, `R40`, `R41`. The matrix's stack-neutral paths map onto #8 as `design.md` §13 lists. `R42` – `R44` stay `TODO` (feature 20).

## 3. Shared-spec changes

**None.** #7's pass amended shared `R39` and its matrix row; **#8 inherited that amendment already** — the boxed clause is present at `specs/shared/requirements.md` §5 under `R39`, and `test-matrix.md`'s `R39` row already carries the amended case name. Nothing in `specs/shared/` is edited by this feature.

**Two shared-contract observations, recorded with owners, not fixed here:**

1. `saga.md` §2's command table lists `credit.hold` and `invoice.issue` for Billing but **not** `credit.release`, while `asyncapi.yaml`'s `requestCreditRelease` names *"Caller — orders.saga"* and `saga.md` §4.3 requires the credit hold to be released first on the operator-cancellation path. The subject and both its payload schemas exist; only the §2 table row is missing. **Owner: feature 41 `orders_cancel_responder`**, which is the caller. Raised here because this feature builds the responder that row would point at, and *not* edited: `specs/shared/` is read-only and a table row is a spec amendment like any other.
2. No shared requirement obliges the `x-correlation-id` / `x-request-id` request headers, yet `R12` is unsatisfiable for Fulfillment- and Billing-emitted facts without them. Restated from `FS2`/`FS3`; `BC1` depends on it identically. Already recorded as #7's promotion candidate 2.
