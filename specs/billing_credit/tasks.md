# `billing_credit` — Tasks

> Work top to bottom. Tests are written **inside** each group, never at the end. Every task is small enough to verify before ticking. References: `requirements.md` (`BC1` – `BC32`, shared `R37` – `R41`), `design.md` (the § numbers below).
>
> **Revised 2026-09-05, after the human gate overruled both of this spec's GATE recommendations.** Two rulings, one standing instruction. Ruling 1 widened `OB1` back to #7's **seven** files and made the refactor that requires — the neutral `FactEvent` base and the `IFactPayloadMapper` port — this feature's work: **group A is rewritten and roughly doubled**, and `F6` arms the widened guard file by file. Ruling 2 replaced *"stated, not guarded"* on the ledger `Σ` with a real guard: **`B12` and `B13`** are new. The standing instruction (*"stop leaving issues to the next phase — fix them"*) overturned three of the four deferrals in `design.md` §10.3: **group K** is new and closes backlog ids **48**, **53** and **54**. Id **52** stands, for the reason in §10.3.
>
> **Group A goes first and crosses into two shipped services on purpose.** It brings `src/Orders/` and `src/Fulfillment/` onto the service-neutral shape, then writes the guard. **Every file it touches is named in a task below** — that is deliberate, so the implementer's brief is *derived from this list* rather than guessed at, and so no bound written elsewhere can contradict what this list mandates. **Their existing tests must stay green throughout**: `design.md` §13's paragraph *"Which existing tests cover the refactored path"* names the twelve suites that watch it, and every task in group A ends with the ones to run.
>
> **The complete set of files outside `src/Billing` and `tests/Billing.*` this feature may touch**: everything group A names; `src/SharedKernel/Money.cs` and `tests/SharedKernel.UnitTests/MoneyTests.cs` (B13); `src/Fulfillment/Presentation/StockRpcResponder.cs` and `tests/Fulfillment.UnitTests/StockResponderShutdownTests.cs` (E7); `tests/Fulfillment.UnitTests/StockRpcPayloadTests.cs` (G3); `tests/Orders.UnitTests/{SagaCommandPayloadTests,OrdersCreateErrorMapperTests,NatsSagaCommandsAdapterTests}.cs` (G2, G4, K1); `tests/Orders.IntegrationTests/OrdersCreateAcceptanceTests.cs` and `tests/Fulfillment.IntegrationTests/{StockReplenishTests,StockListTests}.cs` (K2); `.env.example` (F2); `specs/fulfillment_stock/design.md` and `specs/fulfillment_stock/tasks.md` (G3, K3); `specs/shared/test-matrix.md`'s Status column only (J1); `feature_list.json`'s one status line (J6). **Nothing else.**
>
> **No migration.** Every table this feature writes shipped in phase 6 (`design.md` §7.1), and both money columns are already `bigint`. If a column looks absent, **stop and report** — do not add one.
>
> **Integration tests obey the synchronisation rule** of `design.md` §13: terminal or monotonic evidence only, never a transient or derived state. **Never poll `availableCredit`.**
>
> **⚑ ARM rows are not optional.** Each names the mutation, the test that must FAIL, and the obligation to record the failure message **verbatim** in `progress/impl_billing_credit.md`. Restore from a backup copy you took — **never `git checkout --`** — then `touch` the restored file (or `dotnet build --no-incremental`) before the confirming green run, and confirm the restore by re-reading the changed line. An arming table produced without a forced rebuild proves nothing (`CLAUDE.md`, arming protocol).
>
> **A task whose own prose asserts a count, an identity, an ordering or an absence is a guard whether or not it is flagged — and in this list every such task IS flagged.** Two features in a row shipped an unguarded countable claim because the claim was in an unflagged task (`CLAUDE.md`, *"A task that makes a countable claim must be armed"*). If you find a countable claim below with no ⚑, arm it anyway and say so in the report.
>
> **Deleting the emission is one mutation family, not the whole of arming.** Every fact-emitting branch here gets **both** questions asked of it: does the guard fail when the row is absent, and does it fail when a field is wrong? #7 shipped this feature with a branch that survived the first question and was rejected for it, and its reviewer's later probe `W3` — a `credit.rejected.v1` with a nonsense `requestedAmount` — **survived the fix**. C8 exists so it does not survive here.

## A — The relay family at #7's full scope: seven files, service-neutral (`design.md` §8.2 – §8.4; `BC17`, `BC29`)

> Ruling 1 of the 2026-09-05 gate. Order matters: the two structural enablers first (A1 – A4), then the three files they unblock (A5 – A8), then the alias edits that were the whole of the first pass (A9), then the guard (A11). `OB1` is **red** from A11 until `F1` creates Billing's copies, and is **armed** in `F6`. Do not reorder.

- [x] **A1.** **Orders: rename `OrderDomainEvent` → `FactEvent`** (`design.md` §8.2.2). Files, all of them: `src/Orders/Domain/Events/OrderDomainEvent.cs` → **renamed to `FactEvent.cs`** (`.editorconfig` requires the file to match the type), `src/Orders/Domain/Events/{OrderPlaced,OrderConfirmed,OrderCompleted,OrderCancelled}.cs`, `src/Orders/Domain/Order.cs:389` (`Func<OrderDomainEvent>?` → `Func<FactEvent>?`), `src/Orders/Infrastructure/Outbox/{OutboxWriter,OrderFactPayloadMapper}.cs`, `src/Orders/Application/Ports/UnknownConsumerNameError.cs:13` (**doc comment only** — reword, do not rename anything), `tests/Orders.IntegrationTests/OutboxAtomicityTests.cs:31,140` (two casts), `tests/Orders.IntegrationTests/OutboxEnvelopeTests.cs:173` (a test-local subtype's base). A rename and nothing else: **no signature gains or loses a parameter in this task.** Then `dotnet test tests/Orders.UnitTests tests/Orders.IntegrationTests tests/Architecture.Tests tests/Contracts.UnitTests` — green before moving on.
- [x] **A2.** **Fulfillment: rename `StockDomainEvent` → `FactEvent`.** Files: `src/Fulfillment/Domain/Events/StockDomainEvent.cs` → **renamed to `FactEvent.cs`**, `src/Fulfillment/Domain/Events/{StockReserved,StockRejected,StockReleased,OrderDespatched}.cs`, `src/Fulfillment/Domain/StockItem.cs:174` (`RecordOrderFact(StockDomainEvent)` → `RecordOrderFact(FactEvent)`), `src/Fulfillment/Infrastructure/Outbox/{OutboxWriter,StockFactPayloadMapper}.cs`. The renamed base file's remark currently says *"the `OrderDomainEvent` shape (`src/Orders/Domain/Events/OrderDomainEvent.cs`) mirrored exactly"* — reword it to name `FactEvent` and §8.2.2. Then `dotnet test tests/Fulfillment.UnitTests tests/Fulfillment.IntegrationTests tests/Architecture.Tests` — green.
- [x] **A3.** **Orders: the `IFactPayloadMapper` port** (`design.md` §8.2.3). New file `src/Orders/Application/Ports/IFactPayloadMapper.cs` with the interface and the doc comment §8.2.3 gives. `src/Orders/Infrastructure/Outbox/OrderFactPayloadMapper.cs`: `static class` → `public sealed class OrderFactPayloadMapper : IFactPayloadMapper`, `ToPayload` an instance method, **the `switch` body and every private mapping method unchanged** — this is a shape change, not a mapping change. Register `services.AddSingleton<IFactPayloadMapper, OrderFactPayloadMapper>();` in `src/Orders/Infrastructure/OrdersOutboxServiceCollectionExtensions.cs`, beside the other explicit registrations. `tests/Orders.UnitTests/OrdersOutboxRegistrationTests.cs`: add `typeof(IFactPayloadMapper)` to `_expectedSingleRegistrations`. Then `dotnet test tests/Orders.UnitTests tests/Orders.IntegrationTests tests/Contracts.UnitTests` — **`OutboxWireParityTests` and `GoldenEnvelopeParityTests` are the ones that matter here**: they assert the exact bytes this mapper produces against twelve captured #7 envelopes, so if the refactor changed one payload value they fail. Record that they passed.
- [x] **A4.** **Fulfillment: the same port.** New file `src/Fulfillment/Application/Ports/IFactPayloadMapper.cs`; `StockFactPayloadMapper` becomes `sealed class : IFactPayloadMapper` with the `switch` untouched; `services.AddSingleton<IFactPayloadMapper, StockFactPayloadMapper>();` in `src/Fulfillment/Infrastructure/FulfillmentServiceCollectionExtensions.cs`. **No registration-list test edit is needed here** — `FulfillmentDispatcherRegistrationTests` builds the real host and `ValidateOnBuild = true` fails if the port is missing; confirm that by running it. Then `dotnet test tests/Fulfillment.UnitTests tests/Fulfillment.IntegrationTests` — green.
- [x] **A5.** **The canonical `OutboxWriter.cs`** (`design.md` §8.2.3). In `src/Orders/Infrastructure/Outbox/OutboxWriter.cs`: constructor `(IClock clock, IFactPayloadMapper payloadMapper)`; `var factEvent = (FactEvent)domainEvent;`; `var payload = payloadMapper.ToPayload(factEvent);`; every other `orderEvent` reference renamed `factEvent`; and **the XML doc rewritten as neutral prose** — the `<see cref="Persistence.EfCoreOrderRepository.SaveChangesAsync"/>` becomes `<c>SaveChangesAsync</c>` on "the aggregate repository", because a service-specific `cref` will not compile in the other two copies. Then repair the **21 call sites in 7 files** that construct the writer directly, adding `, new OrderFactPayloadMapper()` or `, new StockFactPayloadMapper()`: `tests/Orders.IntegrationTests/{OutboxEnvelopeTests,OutboxAtomicityTests,OutboxWireParityTests,OutboxRelayTests,IdempotentConsumerTests}.cs`, `tests/Fulfillment.IntegrationTests/{StockItemRepositoryTests,FulfillmentOutboxRelayTests}.cs`. ⚑ **Countable claim:** after the edit, `grep -rn 'new OutboxWriter(' src tests` must return **21 lines and zero one-argument calls** — paste the command and its full output.
- [x] **A6.** **Fulfillment's `OutboxWriter.cs` becomes a copy.** Replace its body with A5's canonical file, changing **only** the `// COPY OF —` banner, the `namespace` line and the `using` lines. Then `dotnet test tests/Fulfillment.UnitTests tests/Fulfillment.IntegrationTests` — green.
- [x] **A7.** **`KafkaOptions.cs` unified** (`design.md` §8.2.4). Both `src/Orders/Infrastructure/Outbox/KafkaOptions.cs` and `src/Fulfillment/Infrastructure/Outbox/KafkaOptions.cs` become the canonical file of §8.2.4 — `BootstrapServers` unchanged, `ClientId` declared **`required` with no default**, the doc comments neutral. The per-service value moves outwards, one line each: `src/Orders/Infrastructure/OrdersOutboxOptions.cs` → `new() { ClientId = "otc-orders" }`, `src/Fulfillment/Infrastructure/FulfillmentOptions.cs` → `new() { ClientId = "otc-fulfillment" }`. One existing call site stops compiling and is repaired here: `tests/Orders.IntegrationTests/StandInSagaResponders.cs:226` must name a client id. Then `dotnet test tests/Orders.UnitTests tests/Orders.IntegrationTests tests/Fulfillment.UnitTests tests/Fulfillment.IntegrationTests` — green, `KafkaFactPublisherConfigTests` included.
- [x] **A8.** ⚑ **ARM (`BC29`, ledger `L26`) — the guard is the compiler.** Delete `ClientId = "otc-orders"` from `src/Orders/Infrastructure/OrdersOutboxOptions.cs` and run `dotnet build`. It must **fail** with `CS9035: Required member 'KafkaOptions.ClientId' must be set in the object initializer or attribute constructor.` Record the error **verbatim, with the file and line**, exactly as a failing test's message would be recorded. Restore from your backup copy, confirm by re-reading the line, force the rebuild, and confirm the build is green again. *(The property this arms is not "a field exists" — it is that unifying `KafkaOptions.cs` cannot silently give two services the same producer identity. An empty `ClientId` does not throw at runtime: librdkafka substitutes `rdkafka`.)*
- [x] **A9.** **The alias edits** (`design.md` §8.2.1), both services, six files. `src/Orders/Infrastructure/Outbox/OutboxRelay.cs`: replace the plain `using OrderToCash.Orders.Infrastructure.Persistence;` with `using WriteModelDbContext = OrderToCash.Orders.Infrastructure.Persistence.OrdersDbContext;` and the constructor parameter with `WriteModelDbContext db`. `src/Orders/Infrastructure/Outbox/KafkaFactPublisher.cs`: add `using FactTopic = OrderToCash.Orders.Infrastructure.Outbox.OrdersFactTopic;` and change the single `OrdersFactTopic.Name` to `FactTopic.Name`. `src/Orders/Infrastructure/Outbox/OutboxRelayBackgroundService.cs:33`: reword the comment's `OrdersDbContext` to *"the write model's DbContext"* — **comment text only**. Apply the same three to `src/Fulfillment/Infrastructure/Outbox/{OutboxRelay,KafkaFactPublisher,OutboxRelayBackgroundService}.cs` (aliases naming `FulfillmentDbContext` and `FulfillmentFactTopic`) and re-sync the three bodies to the canonical, banners kept. **Nothing else in any of the six changes** — same body, same SQL, same `ClaimColumnNames`. Then run both services' unit and integration suites; green.
- [x] **A10.** ⚑ **Countable claim — verify, do not assume, over all seven.** Run `grep -niE '\b(orders|fulfillment|billing|projector|notifications)' src/Orders/Infrastructure/Outbox/{OutboxRelay,OutboxRelayOptions,OutboxRelayBackgroundService,OutboxEnvelopeMapper,KafkaFactPublisher,OutboxWriter,KafkaOptions}.cs` and record the complete output. **Every remaining hit must be on a banner line, the `namespace` line, or a `using` line**; there must be **zero** hits anywhere else. A hit outside those three regions is work still owed, not something to normalise away. Paste the command and its full output into the report.
- [x] **A11.** **`tests/Orders.UnitTests/OutboxRelayParityTests.cs`** — `OB1`, the three cases of `design.md` §8.4 over the **seven** guarded files, reusing `IdempotentConsumerParityTests`' helper shapes (`RepositoryPaths.Find`, banner stripping, namespace-line stripping, the service-token scan) and defining its **own** `using` matcher accepting both the plain and the aliased form, with the whitelist of §8.4 case 2 **including the two additions `System.Text.Json` and `.Domain.Events`**. **Do not modify `IdempotentConsumerParityTests`.** Case 1's non-vacuity assertion is that the discovered copy set has **at least three** members and names them; case 1's failure message must also name **the diverging file and the first differing line**, because `F6` mutates each of the seven in turn and a message that says only *"a file differs"* cannot show that all seven are covered. It will FAIL at this point (Billing has no copies) — the correct intermediate state; `F1` makes it green and `F6` arms it.
- [x] **A12.** ⚑ **Countable claim.** Confirm `git status --short src tests` lists **exactly** the files A1 – A11 name and nothing else. Anything else is scope creep and must be reverted — **by re-editing, never by `git checkout --`**. Record the output verbatim.

## B — The domain (`design.md` §3)

- [x] **B1.** `tests/Billing.UnitTests/` — create the project (xunit, `xunit.runner.visualstudio`, `coverlet.collector`, `ProjectReference` to `src/Billing`) and `dotnet sln add` it. Keep `src/Billing/{Domain,Application,Presentation}/README_PLACEHOLDER.cs` — `tests/Architecture.Tests/DomainAssemblies.cs` selects `BillingDomainPlaceholder` **by name**.
- [x] **B2.** `Domain/CreditEntryType.cs` (enum + `CreditEntryTypes` token map, `Parse` refusing anything outside `hold`/`consume`/`release`), `Domain/CreditLedgerEntry.cs` + `BuyerCreditSnapshot.cs` (`CreditLedgerEntrySnapshot`), `Domain/CreditRejectionReason.cs`, `CreditReleaseReason.cs`, `Domain/Errors/` — the six `DomainError` subclasses with the stable codes of §3.5. **`CreditLedgerEntry` exposes no setter and no mutating method** (**B2**).
- [x] **B3.** ⚑ **ARM (`BC12` token half, ledger `L12`).** `tests/Billing.UnitTests/CreditLedgerEntryTests.cs`: `Create` refuses a non-positive amount; and `CreditEntryTypes_Parse_RaisesOnATokenOutsideTheClosedSet_RatherThanIgnoringTheRow`. Then mutate `Parse` to return `CreditEntryType.Hold` for an unknown token and confirm that test FAILS. Record verbatim, restore, force the rebuild, re-run green. *(A silently-skipped row would move `Σ` and therefore `availableCredit` — the failure would present as a wrong credit limit, not as a parse error.)*
- [x] **B4.** ⚑ **ARM (`BC5`, `BC6`, `BC28`, ledger `L14`).** `Domain/CreditExposure.cs` — `Summarise` per §3.3: the two-term identity, `OpenExposure = min(Σconsume, Exposure)`, `HasHoldEntry`, grouping by `orderReference` with **`StringComparer.OrdinalIgnoreCase`**. Pure, no framework reference. `tests/Billing.UnitTests/CreditExposureTests.cs`: the `BC5`/`BC6` identities as properties over at least four fixtures (never held; held; held+consumed; held+released), **the cancelled-before-invoice case** (`hold`, then `release`, no `consume`) yielding `Exposure 0`, `OpenExposure 0`, `ActiveHold 0` and never a negative — the case `domain-model.md` §5.1's literal formula gets wrong — and `BC28_GroupsLedgerEntriesByOrderReferenceCaseInsensitively_MatchingTheDatabasesOwnComparison`. Then switch the comparer to `StringComparer.Ordinal` and confirm the `BC28` case FAILS. Record, restore, force the rebuild, re-run green.
- [x] **B5.** `Domain/BuyerCredit.cs` + `BuyerCreditSnapshot.cs` per §3.1: `Reconstitute` (refuses a snapshot breaking **B1** or **B3**), `AvailableCredit`, `Summary`, `EvaluateHold` (the four cases, **in `BC26`'s fixed order**), `Approve` (throws unless it fits), `Refuse` (throws on a lying `over_limit`), `Release` (returns `null` on nothing outstanding, throws on underflow), `Consume` (no fact), `AppendedEntries`, `ToSnapshot`.
- [x] **B6.** `Domain/Events/` — `CreditDomainEvent` base plus `CreditApproved`, `CreditRejected`, `CreditReleased`, mirroring `src/Fulfillment/Domain/Events/`: `AggregateId` = the credit line, `CorrelationId` = the order id, `CausationId`/`OccurredAt` from `CreditContext`, `creditCode` in all three payloads. ⚑ **Countable claim:** there is **exactly one** `CreditRejected` builder and **exactly one** call site (`Refuse`) — confirm with `grep -rn "new CreditRejected(" src/Billing` and paste the output; more than one line is a defect, not a style choice (§3.4, `R44`).
- [x] **B7.** ⚑ **ARM (`R37`).** `tests/Billing.UnitTests/BuyerCreditTests.cs`: the `R37` matrix case (method name carrying the matrix wording verbatim in PascalCase, prefixed `R37_`) — *keeps active holds plus open exposure within the credit limit and raises on any update or deletion of a ledger entry* — asserting that asking the aggregate to reverse an entry **appends a new `release`** and leaves the loaded entries identical; plus `BC5_DerivesAvailableCreditAsTheLimitMinusHoldsPlusReleases_SoAConsumeEntryMovesItByNothing`. Then make `Release` rewrite the order's existing `hold` entry instead of appending, and confirm `R37_…` FAILS. Record, restore, force the rebuild, re-run green.
- [x] **B8.** ⚑ **ARM (`R38`, `R39`, `BC10`, `BC14` domain half, `BC26`).** `tests/Billing.UnitTests/CreditHoldTests.cs`: the `R38` and `R39` matrix cases verbatim; `BC10_ReportsAvailableCreditAfterRecomputedWithTheAppendedHold_NeverThePreHoldValue`; `BC14_EmitsACreditRejectedFactForAnAdapterRefusalThatDiffersFromTheOverLimitRefusalInTheReasonFieldAndInNothingElse` (build both from **one** fixture and assert envelope **and payload** equality after normalising `EventId`, `OccurredAt`, `Reason` — a positive assertion of sameness); and `BC26_RanksAlreadyHeldAboveCurrencyMismatchAndCurrencyMismatchAboveOverLimit_WhenMoreThanOneApplies`, driven by a request that satisfies all three at once. **Each case asserts the emitted fact count is exactly one and opens its payload**, never only the count. Then reorder `EvaluateHold`'s cases to put `CurrencyMismatch` first and confirm `BC26_…` FAILS. Record, restore, force the rebuild, re-run green.
- [x] **B9.** ⚑ **ARM (`R40`, `R41`, `BC11`, `BC12`).** `tests/Billing.UnitTests/CreditLedgerTests.cs`: the `R40` and `R41` matrix cases verbatim; `BC11_ReleasesTheOrdersOutstandingExposureOnce_ReportsANoOpOnASecondRelease_AndRefusesAReleaseThatWouldDriveExposureBelowZero`; `BC12_AppendsAConsumeEntryThatLeavesAvailableCreditUnchangedAndEmitsNoFact_AndRefusesToConsumeAnOrderWithNoActiveHold`. `R41`'s case asserts **exactly one** `credit.released.v1` per release, with the right `reason` **and** the right `releasedAmount` and `availableCreditAfter`. Then arm **twice**: (a) make `Consume` reduce committed exposure — expect `R40_…` and `BC12_…` to FAIL; (b) make `Release` double-count the released amount — expect `R41_…` and `BC11_…` to FAIL. Record both verbatim, restore, force the rebuild, re-run green.
- [x] **B10.** ⚑ **ARM (ledger `L25`, the OUTER bound).** Add to `BuyerCreditTests.cs`: `Reconstitute_RefusesASnapshotWhoseCommittedExposureAlreadyExceedsTheCreditLimit_AndASnapshotWhoseEntryCurrencyDiffersFromTheLines`. This bounds every `Σ` this service performs **through the aggregate**, which is one route to an overflowing ledger and not the arithmetic — it is the **second** line of `BC30`, not the only one (gate ruling, 2026-09-05). Then delete the `committedExposure > creditLimit` check and confirm it FAILS. Record, restore, force the rebuild, re-run green.
- [x] **B12.** ⚑ **ARM (`BC30`, ledger `L25`) — the summation raises rather than wraps.** Wrap `CreditExposure.Summarise`'s accumulation in an explicit `checked` region and convert `OverflowException` into `CreditLedgerOverflowError` (`CREDIT_LEDGER_OVERFLOW`, §3.3, §3.5), with the `OverflowException` as its inner exception. **Write the accumulation as explicit loops, not `Enumerable.Sum`** — `Sum(IEnumerable<long>)` happens to throw in the current BCL but nothing in its signature says so, and *"the library did it"* is the answer the ledger exists to refuse; `checked` does not propagate into a called method in any case. Then `tests/Billing.UnitTests/CreditExposureTests.cs` gains **two** cases that drive `Summarise` **directly**, with no aggregate, no `Reconstitute` and no database — `CreditLedgerEntrySnapshot` is a plain record, so the data is one line: `BC30_RaisesCreditLedgerOverflowRatherThanWrapping_WhenTheHoldTotalExceedsALong` (two `hold` entries on one order, each `long.MaxValue / 2 + 1`) and `BC30_RaisesCreditLedgerOverflowRatherThanWrapping_WhenTheCommittedExposureAcrossOrdersExceedsALong` (the same total split across two `orderReference`s, so the wrap happens in the cross-order term rather than the per-order one — a `checked` region that covered only the inner loop would pass the first case and fail this one). **Assert the error's `Code`, not just that something threw.** Then arm: change `checked` to `unchecked` and confirm **both** cases FAIL — they must fail on a wrapped **negative** total, which is the defect being guarded, so record what the wrapped value was. Restore, force the rebuild, re-run green.
- [x] **B13.** ⚑ **ARM (`BC30`, ledger `L25`) — `Money`'s own arithmetic.** `src/SharedKernel/Money.cs`: `Add`, `Subtract` and `Multiply` become `checked` expressions. **Three expressions; change nothing else in that file, and add no package** (`SharedKernelHasNoPackagesTests` asserts zero references). This is shared code, so it is justified explicitly: `availableCredit = creditLimit − committedExposure` is a `Money.Subtract`, and guarding the ledger while leaving that unchecked would guard the sum and not the quantity the sum exists to produce (`design.md` §3.3, line 2). The behaviour changes **only** on inputs that previously produced a wrong answer. `tests/SharedKernel.UnitTests/MoneyTests.cs` gains `BC30_RaisesRatherThanWrapping_WhenAddSubtractOrMultiplyOverflowsMinorUnits` — a `[Theory]` with one case per operation. Then arm: revert one operation to unchecked and confirm the matching `InlineData` FAILS. Record, restore, force the rebuild, then **run the whole solution's tests** — a shared-code change is not done until every service's suite is green, and the run itself is the evidence that nothing depended on the wrap.
- [x] **B11.** **Runs last in this group, after B12 and B13** — the numbering is historical, the order is not. Run `dotnet test tests/Billing.UnitTests` with coverage; the **domain layer must already be ≥ 80 %** at this point. Confirm `dotnet test tests/Architecture.Tests` is green — in particular `DomainDecimalTests`, which is non-trivial for the first time (`design.md` §2, ledger `L15`).

## C — Application layer and the credit-decision port (`design.md` §5, §6)

- [x] **C1.** Ports: copy `IClock`, `IUnitOfWork`, `IFactPublisher` + `PublishableFact` from `src/Orders/Application/Ports/` with a `// COPY OF — src/Orders/Application/Ports/<file>.cs` banner; write `IBuyerCreditRepository` and `ICreditReadPort` per §5.2. **No `tx` parameter anywhere.**
- [x] **C2.** `Infrastructure/Messaging/Rpc/CreditRpcPayloads.cs` — the six request/reply records transcribed from `asyncapi.yaml`, with `CreditHoldRequestPayload.Amount` a **nested** `{ amount, currency }` object (`design.md` §4.3) and every optional reply property nullable so an absent value is omitted rather than sent as `null`. `RpcJson.cs` and `RpcErrorPayload.cs` copied from Fulfillment with banners.
- [x] **C3.** `Application/CreditApplicationErrors.cs` (`CreditLineNotFoundError`, `CreditCurrencyMismatchError`); `Application/Commands/HoldCreditCommand.cs`, `ReleaseCreditCommand.cs` (both carrying `CorrelationId` and `RequestId` as `UniqueId`) and their thin `ICommandHandler` classes; `Application/Queries/ListCreditQuery.cs` and its `IQueryHandler`.
- [x] **C4.** ⚑ **ARM (`BC14` type half, ledger `L24`).** `Application/Ports/ICreditDecisionPort.cs` per §6.1 — `CreditDecisionRequest`, the **closed** `AdapterRejectionReason` enum with exactly the two simulator members, `CreditDecision.Approve`/`Refuse`, `ICreditDecisionPort.DecideAsync`, with the doc comment stating both guarantees (consulted only on `Fits`; may not say `over_limit`; must not perform I/O). The total mapping `AdapterRejectionReason → CreditRejectionReason` lives in **one** place and is an exhaustive `switch` the compiler forces to be updated. `tests/Billing.UnitTests/CreditDecisionPortTests.cs`: `BC14_TypesThePortSoThatOverLimitIsNotAReasonAnAdapterCanReturn` — assert the enum's member set is **exactly** the two simulator reasons and that the mapping is total. Then add an `OverLimit` member to `AdapterRejectionReason` and confirm the build or that test FAILS. Record, restore, force the rebuild, re-run green.
- [x] **C5.** `Infrastructure/Credit/AlwaysApproveCreditDecision.cs` + `tests/Billing.UnitTests/AlwaysApproveCreditDecisionTests.cs` (`BC15`), with a header comment naming feature 20 as the **only** thing that replaces its registration.
- [x] **C6.** `Application/CreditHoldService.cs` and `CreditReleaseService.cs` per §5.3/§5.5/§5.6: `LockForOrderAsync → null ⇒ CreditLineNotFoundError → EvaluateHold → short-circuits → OverLimit ⇒ Refuse(over_limit) → Fits ⇒ DecideAsync ⇒ Approve | Refuse(reason) → SaveChangesAsync → map to reply`; the reply is built inside the delegate and **returned only after `ExecuteAsync` resolves**; `ctx = new CreditContext(clock.UtcNow, command.RequestId)`.
- [x] **C7.** ⚑ **ARM (`BC7`, `BC13`, `BC14` handler half — this is #7's blocking defect `D1`, reproduced deliberately).** `tests/Billing.UnitTests/CreditHoldServiceTests.cs` with fake repository / unit of work / clock / port: `BC7_ShortCircuitsToAlreadyHeld_OnAHoldEntryWhoseExposureHasSinceBeenReleased_CallingNoPortAndWritingNothing` (seeded so the requested amount **is** affordable, so a net-exposure-filtering short-circuit would happily re-approve); `BC13_ConsultsTheCreditDecisionPortOnlyAfterTheAggregateHasFoundTheAmountFits_AndNeverForAnOverLimitRequest` (a recording fake asserting **exactly zero** calls on the over-limit path); `BC14_ReturnsRejectedWithThePortsReasonAndRecordsExactlyOneCreditRejectedFactCarryingThatReasonTheRequestedAmountAndTheUnchangedAvailableCredit_WhenThePortRefusesAFittingHold` — asserting on the **domain events of the aggregate handed to `SaveChangesAsync`**, not on the reply and not on a spy; plus reply-after-commit and rollback-produces-no-reply. Then arm: **delete the `Refuse(...)` call from the port-refusal branch, leaving the reply unchanged**, and confirm the `BC14` handler case FAILS on the missing **fact**. Record verbatim, restore, force the rebuild, re-run green. *(In #7 this exact mutation type-checked and left all 56 unit tests green.)*
- [x] **C8.** ⚑ **ARM (payload corruption — the second mutation family).** With C7's cases in place, arm **three** further mutations, one at a time: (a) record the fact with a hard-coded wrong `reason`; (b) record it with a wrong `requestedAmount`; (c) record the correct fact but delete `SaveChangesAsync` from the branch. Each must make a **named** case FAIL. **(b) survived #7's fix and was recorded as its nit `N5`** — if it survives here, the case is not asserting the payload and must be extended **before** restoring. Record all three verbatim, restore, force the rebuild, re-run green.
- [x] **C9.** ⚑ **ARM (`BC11` no-op, `BC25` handler half).** `tests/Billing.UnitTests/CreditReleaseServiceTests.cs`: releasing an order with outstanding exposure records **exactly one** `release` entry and **exactly one** `credit.released.v1` with `reason: order_cancelled`, the right `releasedAmount` and the right `availableCreditAfter`; releasing an order with none writes **nothing**, records **no** fact and replies `released: false`. Then arm both directions — delete the fact emission (expect the first to fail) and make the no-op path emit a fact anyway (expect the second to fail). Record both, restore, force the rebuild, re-run green.

## D — Persistence adapters (`design.md` §7)

- [x] **D1.** `Infrastructure/Persistence/EfCoreUnitOfWork.cs` (copy of Orders'/Fulfillment's over `BillingDbContext`, `IsolationLevel.ReadCommitted` stated explicitly, banner); `Infrastructure/Persistence/BuyerCreditRowMapper.cs` per §7.4 — **`value.UtcDateTime` to write, `new DateTimeOffset(value, TimeSpan.Zero)` to read**, with the `OrderRowMapper` comment reproduced.
- [x] **D2.** `Infrastructure/Persistence/EfCoreBuyerCreditRepository.cs`: `LockForOrderAsync` — §5.5's three statements, each with `WITH (UPDLOCK, HOLDLOCK, ROWLOCK)` (steps 2 and 3 `WITH (UPDLOCK, HOLDLOCK)`), each naming **every mapped column literally**; the committed-exposure scalar executed as raw SQL so the hint survives. `SaveChangesAsync` — add one `CreditItem` row per `AppendedEntries` member, drain `PullDomainEvents()` into outbox rows **one awaited `INSERT` at a time** (copy `EfCoreOrderRepository.InsertOutboxRowAsync` and its comment verbatim), clear domain events **last**.
- [x] **D3.** ⚑ **Countable claim — the forbidden constructs, and the reviewer is told to grep for every one.** In `src/Billing/`, **zero** occurrences of each of: `IF NOT EXISTS`, `MERGE`, `.SumAsync(`, `AddRange(` on outbox rows, `(int)` applied to a money value, `Update(`/`Remove(` against `CreditItems` or `Credits`. Run one `grep -rn` per pattern over `src/Billing` and paste **every** command and its **complete** output into the report — a claim of absence is a search result, not a reading (`CLAUDE.md`). Reasons: ledger `L17` (check-then-act, feature 45's defect), `L7` (LINQ composition drops the table hint), `L16` (`IDENTITY` order), `L5` (money narrowing), `B2` (append-only).
- [x] **D4.** `tests/Billing.UnitTests/CreditClaimProjectionTests.cs`: compare the literal column lists of D2's statements against the mapped properties of `Credit` and `CreditItem` in the EF model — the `OutboxClaimProjectionTests` / `StockClaimProjectionTests` instrument.
- [x] **D5.** ⚑ **ARM (`BC9`, ledger `L6`/`L8`).** Remove `WITH (UPDLOCK, HOLDLOCK)` from **the order-scoped `credit_items` read** in D2. Expect H4's `BC7` re-issue case and/or H5's `BC9` race case to FAIL. Record verbatim, restore, force the rebuild, re-run green. *(Under `READ_COMMITTED_SNAPSHOT ON` the un-hinted read returns a row version, so the idempotency predicate is computed from a snapshot that is already false.)*
- [x] **D6.** `Infrastructure/Persistence/EfCoreCreditReadRepository.cs` per §7.3: the page of `credits` (optional filters, `ORDER BY retailer_code, company_code`, `Skip`/`Take`), the `CountAsync`, and one read of the page's `credit_items` — all `AsNoTracking`, **no hint, no transaction**, folded through the **same** `CreditExposure.Summarise`. Do not add a lock hint to the view: `R31`'s equivalent property for this service is that a read blocks nobody (ledger `L9`).
- [x] **D7.** `tests/Billing.IntegrationTests/BuyerCreditRepositoryTests.cs` (real MS-SQL through the existing `MsSqlContainerFixture`): round-trip reconstitute/save; **`BC10_WritesTheHoldEntryAndTheCreditApprovedOutboxRecordInOneTransaction_AndAForcedRollbackLeavesNeither`**; and *availableCredit recomputed from **every** row of `credit_items` equals the fact's `availableCreditAfter` after each committed operation* (§3.1's honest check).

## E — Presentation, wire, host (`design.md` §4, §14)

- [x] **E1.** `src/Billing/Billing.csproj`: add the `src/Cqrs` `ProjectReference` and the six `PackageReference`s of §14.2, each with the confinement comment its Fulfillment counterpart carries. **No new `PackageVersion` in `Directory.Packages.props`** — if one seems necessary, stop and report.
- [x] **E2.** `Presentation/Rpc/CreditSubjects.cs` + `tests/Billing.UnitTests/CreditSubjectsTests.cs` (read `asyncapi.yaml` as text, assert each of the **three** constants equals its channel `address`).
- [x] **E3.** `Presentation/Rpc/RpcMeta.cs` — copied verbatim from `src/Fulfillment/Presentation/Rpc/RpcMeta.cs` with a `// COPY OF —` banner.
- [x] **E4.** `Presentation/Rpc/CreditRequestValidator.cs` + `tests/Billing.UnitTests/CreditRequestValidatorTests.cs` — **one case per rule** of §4.4, including the `^ORD-[0-9]{6,}$` and `^[A-Z]{3}$` alphabets (ledger `L3`, `L13`, `L14`) and the paging defaults `page = 1` / `pageSize = 25` (ledger `L10`).
- [x] **E5.** ⚑ **ARM (`BC1`, ledger `L4`).** `tests/Billing.UnitTests/CreditResponderHeaderTests.cs`: `BC1_RepliesValidationFailedAndDispatchesNothing_WhenACorrelationOrRequestHeaderIsMissingOrMalformed` — a `[Theory]` covering missing and malformed, on `billing.credit.hold` **and** `billing.credit.release`, asserting the dispatcher was called **exactly zero** times; plus one case asserting `billing.credit.list` succeeds with **no** headers. Then make the responder tolerate a missing `x-request-id` and confirm the `[Theory]` FAILS. Record, restore, force the rebuild, re-run green.
- [x] **E6.** ⚑ **ARM (`BC27`, ledger `L19`).** `Presentation/Rpc/CreditErrorMapper.cs` per §4.5 + `tests/Billing.UnitTests/CreditErrorMapperTests.cs`: `BC27_MapsEveryTransientStoreFailureToACodeTheSagaAdapterTreatsAsRetryable_NeverToATerminalBusinessCode` — a `[Theory]` over the mapped exceptions asserting the produced `code` is **not** in the terminal set, which the test **reads from `NatsSagaCommandsAdapter`'s own classification** rather than retyping the nine codes (the `StockRpcErrorMapperTests` instrument); plus one case asserting `CONFLICT` is produced by **no** input at all. Then map `SqlException` 1205 to `CONFLICT` and confirm it FAILS. Record, restore, force the rebuild, re-run green.
- [x] **E7.** ⚑ **ARM ×2 (`BC22`, backlog id 50, ledger `L22`) — two services, both halves.** Write `src/Billing/Presentation/CreditRpcResponder.cs`'s `StopAsync` per §4.7 (await every in-flight task **individually**, log faults, return normally) and apply the same correction to `src/Fulfillment/Presentation/StockRpcResponder.cs`. Add `BC22_CompletesShutdownWaitingForEveryInFlightRequest_WhenOneFaultsAndOneSucceeds` to **both** `tests/Billing.UnitTests/CreditResponderShutdownTests.cs` and `tests/Fulfillment.UnitTests/StockResponderShutdownTests.cs`, asserting **both** properties: `StopAsync` completes without throwing, **and** the healthy task observably ran to completion before it returned. Then arm twice in each service: (a) revert to `await Task.WhenAll(pending)` — the fault half must FAIL; (b) remove the wait entirely — the drain half must FAIL. Record all four messages verbatim, restore, force the rebuild, re-run green. **Do not touch any other part of `StockRpcResponder.cs`.**
- [x] **E8.** ⚑ **ARM (`BC21` scope half, ledger `L21`).** `Presentation/CreditRpcResponder.cs` per §4.1: three subscription loops on **one** `BackgroundService`; per message a `SemaphoreSlim(MaxConcurrentRequests)` acquired **before** the scope, a tracked task, **one `IServiceScope` per request**, dispatch, reply; never throws, never leaves a request unanswered. `tests/Billing.UnitTests/CreditResponderConcurrencyTests.cs`: `BC21_ResolvesADistinctDependencyInjectionScopePerRequest_NeverOnePerResponder`. Then hoist the scope out of the per-request path and confirm it FAILS. Record, restore, force the rebuild, re-run green.
- [x] **E9.** `Infrastructure/BillingOptions.cs` + `BillingServiceCollectionExtensions.cs` (explicit registration, one port at a time, `ICreditDecisionPort` → `AlwaysApproveCreditDecision`); `BillingHost.cs` and `Program.cs` per §14.3 — `ValidateOnBuild`/`ValidateScopes` forced on in **every** environment, `AddDispatcher` **last**.
- [x] **E10.** ⚑ **ARM.** `tests/Billing.UnitTests/BillingDispatcherRegistrationTests.cs`: `BillingHost.CreateBuilder(...).Build()` succeeds, and removing any one port registration makes it throw. Then flip `ValidateOnBuild = true` to `false` and confirm a named test FAILS; if none does, **add one before restoring** (feature 15's review `D3`/`Q1` precedent). Record, restore, force the rebuild, re-run green.

## F — Outbox relay, Kafka publisher, and the guard turning green (`design.md` §8)

- [x] **F1.** Copy `OutboxWriter`, `OutboxEnvelopeMapper`, `OutboxRelay`, `OutboxRelayOptions`, `OutboxRelayBackgroundService`, `KafkaFactPublisher`, `KafkaOptions`, `SystemClock` from `src/Orders/` with a `// COPY OF — src/Orders/Infrastructure/<path>.cs` banner on each, applying only the edits §8.1 lists (the two aliases, `CreditDomainEvent` in the writer's cast, `ClientId` default `otc-billing`). `Infrastructure/Outbox/CreditFactPayloadMapper.cs` maps the three domain events to their `Contracts` payload records (the `OrderFactPayloadMapper` shape). ⚑ **Countable claim: A5's `OB1` must now be green with a discovered copy set of exactly three — `Orders`, `Fulfillment`, `Billing`.** Run `dotnet test tests/Orders.UnitTests --filter FullyQualifiedName~OutboxRelayParity` and record the output.
- [x] **F2.** `Infrastructure/Outbox/BillingFactTopic.cs` + `tests/Billing.UnitTests/BillingFactTopicTests.cs` (read `asyncapi.yaml` as text, assert the constant equals the `billingFacts` channel's `bindings.kafka.topic`). `.env.example` gains `BILLING_KAFKA_CLIENT_ID` and `BILLING_MAX_CONCURRENT_REQUESTS` with comments, beside the existing `FULFILLMENT_*` entries.
- [x] **F3.** ⚑ **ARM (ledger `L18`).** `tests/Billing.UnitTests/CreditRpcPayloadTests.cs` gains the reply-shape cases: an `approved` reply carries **exactly** `outcome`, `orderReference`, `creditCode`, `currency`, `heldAmount`, `availableCredit` and **no** `reason` key; a `rejected` reply carries `reason` and no `heldAmount`. Then change `JsonWire.Options`' null handling in a scratch copy — or make `Reason` a non-nullable empty string — and confirm the approved case FAILS. Record, restore, force the rebuild, re-run green.
- [x] **F4.** ⚑ **ARM (ledger `L16`).** Replace `InsertOutboxRowAsync`'s per-row awaited `INSERT` with a batched `AddRange` + one `SaveChangesAsync`. Expect a named test asserting `seq` order to FAIL; if none does, **add one before restoring**. Record verbatim, restore, force the rebuild, re-run green.
- [x] **F5.** ⚑ **Countable claim.** `tests/Billing.IntegrationTests/BillingOutboxRelayTests.cs` (MS-SQL + Kafka, topic created with 6 partitions): `BC16_PublishesTheFactsOfACreditHoldTransactionToTheBillingTopicKeyedByCorrelationId_AndStampsPublishedAtOnlyAfterAcknowledgement` — drive `RunOnceAsync()` by hand and read back through a **real** consumer, asserting **exactly one** record on the topic for that `correlationId`, its **key** equal to the order id (read from the broker, not inferred), and `published_at` stamped only after the acknowledgement. Also confirm `dotnet test tests/Orders.UnitTests --filter FullyQualifiedName~IdempotentConsumerParity` stays green: **no** idempotent-consumer copy is added to Billing (`design.md` §9), and case 3's predicate is still not met by this service.

- [x] **F6.** ⚑ **ARM ×9 (`BC17` / `OB1`, ledger `L27`) — the widened guard, file by file.** With `F1` green, arm `OutboxRelayParityTests` **once per guarded file**: introduce a one-character divergence into `src/Billing/Infrastructure/Outbox/<file>` for each of `OutboxRelay.cs`, `OutboxRelayOptions.cs`, `OutboxRelayBackgroundService.cs`, `OutboxEnvelopeMapper.cs`, `KafkaFactPublisher.cs`, **`OutboxWriter.cs`** and **`KafkaOptions.cs`** in turn, and confirm case 1 FAILS **naming that file**. Then arm case 2 (write `OrdersDbContext` into the canonical `OutboxWriter.cs` body and confirm the adoptability case fails) and case 3 (delete one of the seven from `src/Billing/` and confirm the census case fails, naming the missing file). **Nine mutations, nine verbatim messages** — the ruling is that the guard must fail if **any** of the seven diverges, and seven separate armings are the only evidence that says so. Restore each from your backup before the next, force the rebuild, re-run green at the end. *(Seven of these are cheap and mechanical; the point is that the two files the first pass wanted to exclude are among them.)*

## G — Payload and enum parity parsed from `asyncapi.yaml` (`design.md` §10.2; backlog id 51; `BC23`)

- [x] **G1.** Write the helper — `AsyncApiSchema.PropertyNamesOf(schemaName)` and `AsyncApiSchema.EnumValuesOf(schemaPath)` — reading `specs/shared/asyncapi.yaml` as text with the `RpcSubjectsTests` block-extraction technique, resolving a one-level `allOf` (`CreditListRequestPayload` needs it) and returning declared property names in declaration order. Then `tests/Billing.UnitTests/CreditRpcPayloadTests.cs`: `BC23_EveryCreditRequestAndReplyRecordCarriesExactlyThePropertyNamesAsyncApiDeclares_ParsedFromTheSpecNeverRetyped`, covering **all six** `billing.credit.*` schemas plus the nested `Money`. **No literal key list anywhere in this file.**
- [x] **G2.** `tests/Orders.UnitTests/SagaCommandPayloadTests.cs` gains **one** case, `BC23_TheRetypedKeyListsAgreeWithTheKeySetsParsedFromAsyncApi`, asserting each existing hand-retyped list equals the parsed set schema by schema. **Do not rewrite the existing cases** — they catch unilateral drift of the code, which is a different defect.
- [x] **G3.** `tests/Fulfillment.UnitTests/StockRpcPayloadTests.cs` gains the same single case, covering every `fulfillment.stock.*` and `fulfillment.despatch.*` request and reply schema. Correct the false sentences the entry names: the XML doc of this file, `specs/fulfillment_stock/design.md` §6.3 and its `tasks.md` C3 — each currently claims the instrument reads the spec. **Amend the two `specs/fulfillment_stock/` sentences to describe what is now true; change nothing else in those files.**
- [x] **G4.** ⚑ **Countable claim.** `tests/Orders.UnitTests/OrdersCreateErrorMapperTests.cs` gains `BC23_TheThreeRetypedRpcErrorCodeSetsAgreeWithTheEnumParsedFromAsyncApi`, asserting that **all three** retyped sets — `OrdersCreateErrorMapper._contractRpcErrorCodes`, this test file's own copy, and `NatsSagaCommandsAdapter`'s terminal-code set (as a subset) — agree with the **twelve** values parsed from `asyncapi.yaml`'s `RpcError.code.enum`. Enumerate the three sources by name in the failure message; do not assert "the sets match" without saying which three.
- [x] **G5.** ⚑ **ARM (`BC23`, all four instruments).** Copy `specs/shared/asyncapi.yaml` to a **scratch** path, rename one property in `CreditHoldReplyPayload` and delete one value from `RpcError.code.enum`, point the helper at the scratch copy, and confirm G1, G2, G3 and G4 each FAIL. Record all four messages verbatim. **Never edit the real `specs/shared/asyncapi.yaml` — it is read-only.** Delete the scratch copy, force the rebuild, re-run green.

## H — Integration: the responders over real NATS, MS-SQL and Kafka (`design.md` §13)

- [x] **H1.** `tests/Billing.IntegrationTests/`: copy `NatsContainerFixture` and `KafkaContainerFixture` from `tests/Fulfillment.IntegrationTests` with banners; add `BillingHostFixture` booting the **real** `BillingHost.CreateBuilder` graph against the containers **and taking `ICreditDecisionPort` as a construction parameter** defaulting to the always-approve adapter (§13 — #7's harness bound the adapter unconditionally, which is why its port-refusal defect was unreachable). Helpers: `RequestBareAsync(subject, payload, headers?)`, `OutboxRowsForAsync(correlationId)`, `LedgerOfAsync(orderReference)`, `CommittedExposureOfAsync(creditCode)`, `SeedCreditLineAsync(...)`, `SeedLedgerEntryAsync(...)`. **Every reply assertion opens the body and asserts its own discriminating field before touching a collection** (backlog id 53's shape) — a wrong-shaped reply must fail on an assertion, never on a dereference.
- [x] **H2.** ⚑ **Countable claim.** `CreditHoldTests.cs`, approvals: `R38`'s integration half — an `approved` reply, **exactly one** `hold` row in `credit_items`, **exactly one** `credit.approved.v1` outbox row whose `correlation_id`/`causation_id` equal the request headers (`BC1_StampsCorrelationIdFromTheHeaderAndCausationIdFromTheRequestId_OnTheEmittedCreditApprovedFact`) and whose `availableCreditAfter` equals the recomputed value (`BC10`), plus `BC28_EmitsTheResolvedCreditLinesCodesOnTheFact_NotTheRequestsEchoedStrings` — send the request with a differently-cased `retailerCode` and assert the fact carries the **stored** casing.
- [x] **H3.** ⚑ **Countable claim.** `CreditHoldTests.cs`, refusals and violations: `R39`'s integration half (over-limit ⇒ a `rejected` reply, **zero** new `credit_items` rows, **exactly one** `credit.rejected.v1` whose payload carries `reason: over_limit`, the requested amount and the unchanged available credit); `BC3_RepliesNotFoundNamingThePair_WritingNoLedgerEntryAndEmittingNoFact_WhenNoCreditLineExists`; `BC4_RepliesValidationFailedWritingNoLedgerEntryAndEmittingNoFact_WhenTheRequestedCurrencyDiffersFromTheLines`. **Each "no fact" claim is asserted as a count of zero outbox rows for that `correlationId`**, never as an absence of an exception. Add one case that boots the harness with a **refusing** port and asserts the same fact and payload reach the outbox over the real wire — the level #7 could not reach.
- [x] **H4.** ⚑ **Countable claim.** `CreditHoldTests.cs`, idempotency: `BC7_AnswersAlreadyHeldWithTheRecordedHeldAmountAndTheCurrentAvailableCredit_WritingNoEntryAndEmittingNoSecondFact_WhenReIssued`, **including the released-then-re-issued variant** (seed a `hold` and a matching `release`, re-issue, and assert `already_held` with the **recorded** `heldAmount` and the **current** `availableCredit`, one `credit_items` count unchanged, zero new outbox rows); and `BC8_ReEvaluatesAPreviouslyRejectedHoldFromScratch_AndWritesNoLedgerEntryEitherTime`. Wait on the first **reply**, then assert the append-only `credit_items` count.
- [x] **H5.** ⚑ **Countable claim.** `CreditHoldRaceTests.cs`: `BC9_TwoConcurrentHoldsAgainstOneNearlyExhaustedCreditLine_YieldExactlyOneCreditApprovedAndOneCreditRejected_WithCommittedHoldsNeverExceedingTheLimitAndNoDeadlock` — `Task.WhenAll` of two raw NATS requests for **different** orders against a line with room for exactly one, repeated on **10 fresh credit lines**, asserting only on the replies, the final `Σ hold − Σ release ≤ creditLimit`, and the outbox holding exactly one of each fact. No `SqlException` 1205 in any of the ten.
- [x] **H6.** `CreditListTests.cs`: `BC6_ReportsActiveHoldsOpenExposureAndAvailableCreditThatReconcileToTheCreditLimitForEveryListedLine` — a fixture covering all four ledger shapes (never held; held; held + consumed; held + released), plus the filters and paging at SQL level.
- [x] **H7.** ⚑ **ARM (`BC21`, ledger `L21`).** `tests/Billing.IntegrationTests/CreditResponderConcurrencyTests.cs`: `BC21_AnswersASecondRequestWhileAnEarlierOneIsBlockedOnACreditRowLockHeldByAnotherTransaction` — the test opens its own MS-SQL transaction holding `WITH (UPDLOCK, HOLDLOCK)` on credit line **A**'s row, sends a `credit.hold` for **A** (which must block), sends a `credit.hold` for **B** and asserts it is **answered** while the first is still outstanding, then releases the lock and asserts the first completes. Then revert `CreditRpcResponder` to `await HandleAsync(...)` inside the subscription loop and confirm this test FAILS **and H5 still passes** — record both, because "the race test stays green under a mutation that destroys the concurrency it claims to exercise" **is** the finding. Restore, force the rebuild, re-run green.
- [x] **H8.** ⚑ **ARM (`BC24`, ledger `L11`).** Add to `BuyerCreditRepositoryTests.cs`: `BC24_RoundTripsALedgerEntrysInstantUnchanged_UnderANonUtcHostTimeZone` — write an entry at a known instant, read it back through `BuyerCreditRowMapper`, assert equality **with the test process's `TZ` set to a non-UTC zone** (or with the offset injected, so the assertion cannot pass by the runner happening to be UTC). Then drop `TimeSpan.Zero` from the read conversion and confirm it FAILS. Record, restore, force the rebuild, re-run green. *(#7 got this from a `mysql2` pool created with `timezone: 'Z'`; `datetime2(3)` has no offset and EF Core returns `DateTimeKind.Unspecified`.)*
- [x] **H9.** ⚑ **Countable claim.** `CreditWireTests.cs`: `BC2_AnswersABareJsonRequestWithABareJsonReply_OnAllThreeSubjects` — a `[Theory]`, one `InlineData` per subject; decode the reply and assert the top-level object has **no** `response`, `isDisposed` or `id` key and deserialises into the AsyncAPI reply schema — and `BC2_AnswersABareJsonRpcError_OnAValidationFailure`.
- [x] **H10.** ⚑ **Countable claim.** `CreditReleaseTests.cs`: `BC25_AppendsExactlyOneReleaseEntryAndEmitsExactlyOneCreditReleasedFactWithReasonOrderCancelled_ThenReportsReleasedFalseWritingNothingOnARepeat` — first call: `released: true`, exactly one new `credit_items` row of type `release`, exactly one `credit.released.v1` whose payload carries the released amount, the resulting available credit and `reason: order_cancelled`; second call: `released: false`, `credit_items` count unchanged, **zero** new outbox rows. Plus `BC3`'s `NOT_FOUND` on this subject.
- [x] **H11.** `dotnet test` over the whole solution green; `./quality.sh` exit 0; coverage recorded (**≥ 80 % domain, ≥ 60 % overall**); `./init.sh` exit 0.

## I — Live walkthrough against the compose stack (`design.md` §11; `BC20`)

- [x] **I1.** **Pre-state read, before anything is started.** Record: `SELECT o.order_reference, r.code, c.code, o.status, o.total_amount FROM otc_orders.dbo.orders o JOIN … ;` `SELECT order_reference, command, status, attempts, next_attempt_at, LEFT(last_error,60) FROM otc_orders.dbo.saga_commands;` `SELECT COUNT(*) FROM otc_billing.dbo.credits;` `SELECT order_reference, type, amount FROM otc_billing.dbo.credit_items;` `SELECT order_reference, product_code, units, status FROM otc_fulfillment.dbo.reservations;`. **Expected**: five orders `stock_reserved` with `credit.hold` `parked`, **154** credit lines, **15** ledger rows netting to zero on every line, ten `reserved` reservations. If the live state has moved on, record what it actually is and adjust §11's arithmetic before proceeding — do not report a mismatch as a failure.
- [x] **I2.** Start Orders, Fulfillment and Billing against the compose stack (Orders and Fulfillment rebuilt with group A and E7, unchanged in behaviour). Within one sweeper interval, record **unattended**, with timestamps: Billing's structured `credit.hold` and relay log lines; `SELECT order_reference, type, amount FROM otc_billing.dbo.credit_items` showing **exactly one new `hold` row per order**; `SELECT event_type, correlation_id, causation_id, published_at FROM otc_billing.dbo.outbox`; the `saga_commands` rows showing `credit.hold` `sent` and `invoice.issue` `parked`; the reservations `consumed`; `DES-000006` … `DES-000010`; and every order at `despatched`. Assert the chain **fact.correlationId = the order id** and **fact.causationId = the `saga_commands` row id** on real cross-service data. Write all of it into `progress/impl_billing_credit.md` § Live boot.
- [x] **I3.** ⚑ **Countable claim — the genuine over-limit demo, no simulator bound.** Place one fresh order against `(CarrefourEs, IBERFOODS)` for **16 × `PRD-0001`** (399 984 minor units > the 399 004 available after I2's three holds; `mod 100 = 84`, so it is unmistakably not the `.99` affordance — use **21 ×** = 524 979 if the holds have not landed). Record: the `credit.rejected.v1` with `reason: over_limit`, the orchestrator's `stock.release`, the `stock.released.v1`, and the order reaching `cancelled` / `credit_rejected` with its `compensationSteps[]`. **Confirm `credit_items` gained exactly zero rows for that order** — a `SELECT COUNT(*)`, recorded. This is the first end-to-end compensation this repository has run, and it proves `R44`'s last clause **before** feature 20 exists.
- [x] **I4.** Send one `billing.credit.release` over raw NATS for one of I2's held orders and record the `released: true` reply, the new `release` row, the `credit.released.v1` in the outbox, and the `released: false` reply of an immediate repeat with **no** second row and **no** second fact. *(This subject has no production caller until feature 41 — this is the walkthrough that exercises it.)* **No seed re-run is part of this procedure** (`design.md` §11).
- [x] **I5.** Record in the report which shared-contract observations remain open with their owners: `saga.md` §2's missing `credit.release` row (feature 41) and the correlation-header promotion candidate (`requirements.md` §3).

## K — The three deferrals the 2026-09-05 standing instruction overturned (`design.md` §10.3 – §10.6)

> *"Stop leaving issues to the next phase — fix them."* Backlog ids **48**, **53** and **54**, re-decided per entry in `design.md` §10.3. Id **52** stands — its own acceptance defines it as standalone work that fixes nothing in place, so folding it into a feature would violate its terms. All three tasks below are **test-only or documentation-only**: no production file changes in this group.

- [x] **K1.** ⚑ **ARM (`BC31`, backlog id 48).** `tests/Orders.UnitTests/NatsSagaCommandsAdapterTests.cs` gains `BC31_PassesAFreshlyConstructedHeaderCollectionOnEveryRequest_NeverOneReusedBetweenCalls`: capture the `NatsHeaders` instance the adapter passes to the `RawRequester` fake on **two consecutive** `SendAsync` calls and assert they are **reference-distinct** (`Assert.NotSame`) — **not** equal by value, because value equality passes against one instance cleared and re-populated, which is the exact shape the entry names. **No change to `src/Orders/Infrastructure/Messaging/NatsSagaCommandsAdapter.cs`** — the property already holds; only the guard was missing. Then arm: hoist the instance to a field and clear/re-add per call, and confirm the new case FAILS **while every existing header-value assertion in that file stays green**. Record **both** halves verbatim — a mutation that also broke the value assertions would not have shown what this guard adds. Restore, force the rebuild, re-run green.
- [x] **K2.** ⚑ **ARM ×3 (`BC32`, backlog id 53).** Give each of the three sites the shape `FS22` adopted after feature 46's `D1` — read the body, assert **this reply's own discriminating field**, only then touch a collection: `tests/Orders.IntegrationTests/OrdersCreateAcceptanceTests.cs:159` (assert the reply's `orderReference` matches `^ORD-[0-9]{6,}$`; today the comment *"// the request succeeded"* is the whole claim and an `RpcError` body deserialises into an all-defaults object without throwing), `tests/Fulfillment.IntegrationTests/StockReplenishTests.cs:52` and `StockListTests.cs:27` (assert the reply's own discriminating field before `Items`). Name the added case `BC32_FailsOnItsOwnAssertion_WhenTheResponderAnswersAnRpcError` in each file. Then arm each: make the responder answer a real `RpcError` for the request that test sends, and confirm the named case FAILS **on its own assertion** — not on a `NullReferenceException`, and not by passing. Record all three verbatim. **Test files only; the responders are restored unchanged.** *(The containers are already up: tasks A1 – A9 run both integration suites.)*
- [x] **K3.** **Backlog id 54 — the Fulfillment ledger row nobody wrote.** `specs/fulfillment_stock/design.md` §15's table gains one row, `L13`, for the un-hinted in-transaction re-read at `src/Fulfillment/Application/DespatchCreationService.cs:67`. The text is drafted verbatim in `progress/review_fulfillment_despatch.md` §5, row *"L1, second face"* — transcribe it into the table's five columns and make sure all three of the entry's acceptance clauses survive the transcription: that the read is issued only **after** `LockForOrderAsync` is granted; that `EfCoreUnitOfWork.cs:30` opens `IsolationLevel.ReadCommitted` under RCSI, making the snapshot **statement-scoped** rather than transaction-scoped; and the consequence for #9 stated explicitly — under a transaction-scoped snapshot (PostgreSQL `REPEATABLE READ`) the same code returns a stale read and turns a correct idempotent repeat into a permanent `UNAVAILABLE`. **Documentation only**: no code, no tests, and nothing else in that file changes beyond the two sentences `G3` already corrects. This feature already has that file open, which is why the first pass's reason for deferring it (*"a document this feature only reads"*) was wrong when it was written.

## J — Close-out

- [x] **J1.** Flip `specs/shared/test-matrix.md` §5 **column 5 only** for `R37`, `R38`, `R39`, `R40`, `R41` to `DONE — <path> › <method>` (matrix rule 4: names verbatim); leave `R42` – `R44` `TODO`; update the §1 summary counts. **Change nothing else in `specs/shared/` — no requirement text, no columns 1–4, and never `asyncapi.yaml`.** If any shared text seems wrong, stop and report it as a spec amendment for the human gate; do not edit it.
- [x] **J2.** Flip the `BC1` – `BC32` rows in `specs/billing_credit/requirements.md` §2 from `TODO` to `DONE`, citing the real method names. **A row whose cited test does not exercise the requirement's distinguishing branch stays `TODO`.**
- [x] **J3.** Write `progress/impl_billing_credit.md`: what was built; **every deviation from this design argued**; the **full arming table** (mutation, test, verbatim failure message, restore evidence, forced-rebuild evidence) for A8 (the `CS9035` build error), B3, B4, B7, B8, B9, B10, B12, B13, C4, C7, C8, C9, D5, E5, E6, E7 (×4), E8, E10, F3, F4, F6 (×9), G5 (×4), H7, H8, K1 (×2 halves), K2 (×3); the complete `grep` outputs of A5, A10, A12, B6, D3 and F1; the live-boot record (I1–I5); the manual verification script for the human; the package section for the commit message; and the hand-over list — **feature 20** (the port and its single registration line, §6.3), **feature 21** (`Consume` ships ready and uncalled), **feature 22** (§8.5's same-transaction ordering note), **feature 41** (`saga.md` §2's missing `credit.release` row) and **backlog id 52** (§10.3 — the only one of the four still open, with the reason it is standalone work). **Also record what the two gate rulings cost**, in sessions and wall-clock, separately from the rest of the feature: `progress/history.md` compares #8's per-feature effort against #7's baseline, and group A's seven-file refactor plus group K have no #7 counterpart, so folding them into one number would corrupt the comparison this repository exists to produce.
- [x] **J4.** ⚑ **Countable claim.** Record which backlog entries this feature closes and the evidence for each — **five**: **id 50** (§10.1 — two production edits, two named tests, four armings), **id 51** (§10.2 — one helper, four instruments, four armings, three corrected sentences), **id 48** (§10.4 — one test, zero production edits, K1's two-halves arming), **id 53** (§10.5 — three tests, zero production edits, K2's three armings) and **id 54** (§10.6 — one documentation row, no test). Record **id 52 as deliberately still open**, with §10.3's reason, so a reader sees the decision rather than an omission. Do **not** set any of them to `done` — the reviewer does that.
- [x] **J5.** ⚑ **Countable claim.** `./init.sh` exit 0; `./quality.sh` exit 0; `git status` shows only this feature's files, and every change outside `src/Billing`/`tests/Billing.*` is one this task list named: the files of **A1 – A11**, `src/SharedKernel/Money.cs` and `tests/SharedKernel.UnitTests/MoneyTests.cs` (B13), `src/Fulfillment/Presentation/StockRpcResponder.cs` and `tests/Fulfillment.UnitTests/StockResponderShutdownTests.cs` (E7), the test files of G2/G3/G4, `.env.example` (F2), `tests/Orders.UnitTests/NatsSagaCommandsAdapterTests.cs` (K1), the three integration test files of K2, `specs/fulfillment_stock/{design,tasks}.md` (G3, K3) and `specs/shared/test-matrix.md`'s Status column (J1). **Walk the list against the diff and paste both** — a file in the diff that is not on the list is a finding, and so is a file on the list that is not in the diff.
- [x] **J6.** Set `billing_credit` to `in_review` in `feature_list.json` — **edit that single line; do not rewrite the file, and never run `git checkout --` on it** — and update `progress/current.md` in the same breath. Do not commit.
